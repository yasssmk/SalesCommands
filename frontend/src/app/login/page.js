import SignIn from 'views/auth/login';

// ==============================|| PAGE LOGIN ||============================== //

export default function SignInPage() {
  return <SignIn />;
}

// ==============================|| METADATA ||============================== //

export const metadata = {
  title: 'Login | SalesCommands',
  description: 'Sign in to your SalesCommands account to access the dashboard.',
};
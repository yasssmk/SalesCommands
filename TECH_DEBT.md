# Technical Debt Journal

Suivi des dettes techniques connues, leur statut, et le sprint qui les résoudra.

## Convention

| Statut      | Sens                                |
| ----------- | ----------------------------------- |
| OPEN        | Identifiée, pas encore planifiée    |
| PLANNED     | Sprint identifié pour la résolution |
| IN PROGRESS | Sprint en cours                     |
| RESOLVED    | Mergée                              |
| PATTERN     | Enseignement réutilisable (corrigé, rien à faire) |

## Liste

| ID    | Titre                                                                                                           | Découverte                                 | Statut                | Sprint résolution                                       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | --------------------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TD-1  | Absence de tests systémiques                                                                                    | Sprint B1                                  | RESOLVED              | Sprint B1 (pytest infra livré)                          | Convention pytest désormais obligatoire à chaque sprint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| TD-2  | Drift `source_activity` nullability sur Pain/Objective/Impact/TechStack                                         | Audit B1                                   | RESOLVED              | PR #7                                                   | Migration 0016 revert step F (source_activity nullable préservé)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| TD-3  | Collision migrations `standard_departments` (core_apps ↔ core_modules)                                          | Pytest B1                                  | RESOLVED              | PR #7                                                   | Option A — SeparateDatabaseAndState sur core_apps.0002 + core_modules.0001                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| TD-4  | Drift `campaign.stakeholders`                                                                                   | Sprint chore                               | RESOLVED              | PR #9                                                   | AlterField campaign.stakeholders — through_fields + AUTH_USER_MODEL swappable ref                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| TD-5  | Contrat sémantique `NextStepSignal.source_quote` = justification                                                | Sprint B2                                  | RESOLVED              | PR #14 (Sprint B4)                                      | Résolu : le prompt B4 v1 (`prompts/next_steps/next_steps_v1.py`) impose explicitement le contrat dans les EMISSION RULES + exemple concret DSI/AE. `source_quote` = justification extraite du transcript, pas un passage aléatoire.                                                                                                                                                                                                                                                                                                                                                                   |
| TD-6  | `BlockerSignal.contact` attribution déférée à la validation                                                     | Sprint B3                                  | OPEN                  | Sprint F5 (Signals Tab vue Grouped)                     | Le pipeline B3 n'extrait pas l'attribution `contact` (FK Contact, nullable) depuis le transcript — fuzzy mapping LLM "le DSI" → Contact UUID trop fragile. Le wizard de validation du Sprint F5 doit exposer un sélecteur de contact (AsyncContactSelect filtré par `account_id`) sur les BlockerSignal PENDING avant promotion en VALIDATED. Mirror du pattern Impact v1 (metric_text / human_impact deferred).                                                                                                                                                                                      |
| TD-7  | `NextStepSignal.suggested_contacts` attribution déférée à la validation                                         | Sprint B4                                  | OPEN                  | Sprint F6 (Next Steps Tab UI)                           | Le pipeline B4 n'extrait pas l'attribution `suggested_contacts` (M2M Contact) depuis le transcript — fuzzy mapping LLM "Jane" / "the CTO" → Contact UUID trop fragile. Le wizard de validation du Sprint F6 (Next Steps Tab) doit exposer un sélecteur multi-contacts pour les NextStepSignal PENDING avant matérialisation en Activity. Mirror du pattern TD-6 (Blocker contact) et Impact v1 (metric_text / human_impact deferred). **MAJ (sprint Next steps)** : `suggested_contacts` documenté comme capacité DORMANTE (commit f838310a) — câblé API (write serializers + affichage) mais JAMAIS alimenté (le prompt ne l'émet pas). Reste à construire : extraction/matching nom→Contact, possiblement via la mécanique de réconciliation People (TD-211).                                                                                                                                                                 |
| TD-8  | View-layer AI pipeline dedup filter ne discrimine pas par `pipeline_type`                                       | Sprint B4                                  | RESOLVED              | Sprint B5 (endpoint unifié `/activity-extraction/run/`) | Résolu : `ActivityExtractionView._find_existing_run` filtre par `(client_id, source_activity, input_hash, pipeline_type, status__in=DEDUP_STATUSES)`. Chaque sous-pipeline est dédupliqué indépendamment. L'ancien endpoint `/transcript-signals/extract/` conserve son filtre sans `pipeline_type` (acceptable car il ne lance que Qualification).                                                                                                                                                                                                                                                   |
| TD-9  | Prompt LLM quality evaluation v1 → v2                                                                           | Sprint B4                                  | OPEN                  | Sprint dédié post-F7                                    | Calibration empirique des 6 prompts LLM (Pain / Objective / Impact / TechStack / Blocker / NextStep) sur une batterie de 8-15 transcripts réels couvrant étapes/profils/densités/edge cases différents. Itération v2 sur la base des erreurs observées. À planifier ENTRE le sprint F7 (fin frontend post-call) et le démarrage des sprints Copilote (Decision Cycle Workspace / Prep Call).                                                                                                                                                                                                          |
| TD-10 | Old `/transcript-signals/extract/` endpoint deprecated in B5                                                    | Sprint B5                                  | OPEN                  | Chore sprint post-migration frontend                    | L'ancien endpoint reste fonctionnel mais retourne les headers `Deprecation: true` + `Sunset: 2026-12-01` + `Link: </module-ai-pipelines/activity-extraction/run/>; rel="successor-version"`. À supprimer dans un sprint chore dédié APRÈS que le frontend ait entièrement migré vers `/activity-extraction/run/`.                                                                                                                                                                                                                                                                                     |
| TD-11 | Cache replay leakage: signals returned are ALL matching `source_activity`, not only those from the specific run | Sprint B5                                  | RESOLVED              | DC Cleanup sprint (source_run FK)                       | Fixed: added `source_run` FK on BaseSignal pointing to AIPipelineRun. `_serialize_cached_qualif_signals` and `_serialize_cached_nextstep_signals` now filter by `source_run=run`. Both extractors inject `source_run` into signal data dicts. Migration 0019.                                                                                                                                                                                                                                                                                                                                         |
| TD-12 | N+1 on cached qualification signals serialization                                                               | Sprint B5                                  | OPEN                  | Chore perf sprint                                       | `_serialize_cached_qualif_signals` issues 5 separate DB queries (one per signal type) without `prefetch_related`. Should be consolidated or prefetched for production scale.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| TD-13 | Pre-existing migration drift: `CharField` without `max_length` → `varchar(None)`                                | Audit B5                                   | OPEN                  | Chore migration cleanup                                 | `apps/accounts/migrations/0001_initial.py` contains `CharField` fields without `max_length`, producing `varchar(None)` columns. Works on PostgreSQL but breaks SQLite test runs. Needs a data-safe migration to add explicit `max_length` constraints. **MAJ (Sprint Competitors)** : re-confirmé — toutes les suites backend du sprint tournent **Postgres-only** (cf. TD-52).                                                                                                                                                                                                                                                                                                                                                |
| TD-14 | npm audit — 15 vulnerabilities (1 critical Next.js, 4 high axios/lodash/lodash-es/picomatch, 10 moderate)       | Sprint F1 sub-step 0 (frontend infra test) | PARTIAL               | Dependabot batch PR #54                                 | 11/15 vulns resolved via PR #54: lodash (Code Injection + Prototype Pollution), form-data (CRLF injection), picomatch (ReDoS + Method Injection), lodash-es, postcss (partial), js-yaml (DoS), yaml (stack overflow), brace-expansion (DoS), protocol-buffers-schema (prototype pollution), prismjs/refractor (DOM Clobbering via react-syntax-highlighter v16 bump). **4 moderate remaining**, all blocked by INTERDIT major bumps: postcss (Next.js 16 required), uuid in next-auth (next-auth 3.x downgrade required). Will be resolved when Next.js 16 or next-auth migration becomes acceptable. |
| TD-15 | Strict tab/text assertions in ActivityWorkspace.test.jsx                                                        | Sprint F1 sub-step 6                       | OPEN                  | Sprint F2 (incremental tighten)                         | Tests use `getAllByText(...).length >= 1` which is weak. Resserrer en interrogeant explicitement le `<tablist>` MUI : `within(tablist).getAllByRole('tab')` puis comparer la liste exacte des labels. À faire en F2 quand on ajoutera des tests sur le vrai ActivityNotesTab.                                                                                                                                                                                                                                                                                                                         |
| TD-16 | Placeholder tab components mocked in ActivityWorkspace.test.jsx                                                 | Sprint F1 sub-step 6                       | OPEN                  | Couvert progressivement F2/F4/F6                        | ActivityNotesTab, ActivitySignalsTab, ActivityNextStepsTab sont mockés dans le test workspace pour éviter de tirer leurs dépendances (MUI date pickers, async selects). Le contenu réel des placeholders n'est donc pas vérifié par les tests actuels. Sera couvert par tests unitaires dédiés sur chaque composant lors de son implémentation réelle (F2 pour Notes, F4 pour Signals, F6 pour Next Steps).                                                                                                                                                                                           |
| TD-17 | Supabase direct connection (db.{ID}.supabase.co) dépréciée → Session Pooler                                     | Sprint F1 (incident infra DNS)             | RESOLVED (.env local) | À reporter sur CI + prod si applicable                  | Le hostname direct `db.{ID}.supabase.co` est IPv6-only et ne route plus correctement depuis fin 2024-début 2025. Migration vers Session Pooler : `aws-0-{region}.pooler.supabase.com:5432` + username `postgres.{ID}` (au lieu de `postgres`). Mot de passe identique. Fait sur le `.env` local. Action de suivi : vérifier que CI GitHub Actions et tout autre env (prod future) utilisent la nouvelle config. Pattern à documenter dans le README backend pour les futurs setups dev.                                                                                                               |
| TD-18 | `npm run build` cassé — diagnostic « divergence d'environnement » FAUX (cassé sur les deux OS, deux causes masquées) | Sprint F1 (debug post-merge)               | RESOLVED (PR #102, 2026-07-30)                  | Sprint build-health (PR #102)                  | `npm run build` échoue sur des warnings ESLint pré-existants liés à `dnd-kit` et `react-csv`. Aucun rapport avec les sprints F. Le critère de succès des sprints frontend est `npm test` (pas `npm run build`). À traiter dans un sprint chore frontend dédié, potentiellement combiné à TD-14 (npm audit). **MAJ (sprint campagne, PR #98-100) — ESCALADE BLOQUANT** : le build est resté cassé pendant tout le sprint (deps manquantes dont `@mui/x-tree-view`, casse `businessData`/`businessdata`, `userCSVValidation`), ce qui a PRIVÉ la garde `npm run build`. À REMONTER avant tout nouveau travail frontend (voir aussi TD-82 et la note build-health URGENT en phase Go-Live). **RÉSOLU (PR #102, 2026-07-30) — correction de diagnostic** : le diagnostic « cassé côté CC, OK côté local Yacine » (divergence d'environnement) était FAUX. Le build échouait sur LES DEUX environnements, pour DEUX causes distinctes qui se masquaient : (a) sur Linux (conteneur CC), webpack ne résolvait ni `react-csv` ni `@dnd-kit/core` et s'arrêtait AVANT l'étape ESLint ; (b) sur macOS (PO), webpack passait puis le build échouait sur 5 erreurs ESLint `react-hooks/rules-of-hooks`. Cause aggravante : un `node_modules` PARASITE à la racine du repo (non déclaré, non versionné) contenait les paquets manquants ET `@mui/x-tree-view` en v8 — Node remonte l'arborescence, donc l'env du PO résolvait des paquets fantômes ; il a été sorti du projet (voir TD-138). **Corrigé (4 commits)** : (1) déclaration de `react-csv` 2.2.2, `@dnd-kit/core` 6.3.1, `prop-types` 15.8.1 dans `frontend/package.json` (`prop-types` importé par 338 fichiers, ne résolvait que par hoisting d'un transitif `@mui/*`) ; (2) `@mui/x-tree-view` `^6.17.0` → `^8.18.0` (résolu 8.29.2) car le code utilise l'API `RichTreeView` absente de la v6, 8 props vérifiées présentes ; (3) 4 imports alignés sur la casse réelle (`businessdata`→`businessData` dans `techCatalog/list.jsx` ; `userCSVValidation`→`UserCSVValidation` dans `userCSVConfig.js` et `UserCSVImportModalOLD.jsx`), aucun fichier renommé ; (4) `useGetTeam` importé depuis `api/admin/teams` au lieu de `api/admin/users` où il n'existe pas ; + 5 identifiants préfixés `use` non-hooks renommés (espion `vi.fn` dans 4 fichiers de test, un paramètre dans `swrFetcher.js`), AUCUNE règle ESLint désactivée. **Validation** : `next build` exit 0 (23 routes générées), 728 tests vitest (= baseline), pytest inchangé, build + smoke validés en local par le PO.                                                                                                                                                                                                                                                                                           |
| TD-19 | Imports inutilisés dans ActivityNotesTab.jsx                                                                    | Sprint F2 sub-step 3                       | RESOLVED              | Already resolved before DC Cleanup                      | `updateActivity` and `displayErrorSnackbar` are no longer present in the current imports of ActivityNotesTab.jsx — confirmed during audit.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| TD-20 | Test "autosave skip pendant RUNNING" non écrit                                                                  | Sprint F2 sub-step 7                       | OPEN                  | Mini-PR post-F3                                         | Le critère Q6 ("disable autosave pendant qu'une extraction est en cours") est implémenté dans le code mais n'est couvert par aucun test explicite. Le test #19 vérifie ce comportement pour `isLocked=true` mais pas pour `extractionState=RUNNING`. Test à ajouter : mock `runActivityExtraction` avec une promise lente, déclencher une extraction, modifier le transcript pendant le RUNNING, advance timers 1.5s, assert que `onSave` n'a pas été appelé.                                                                                                                                         |
| TD-21 | Warnings MUI FocusTrap dans stderr des tests modal                                                              | Sprint F2 sub-step 6                       | OPEN                  | Cosmétique, non prioritaire                             | Le mock simplifié de `MainCard` ne forward pas les refs, ce qui trigger des warnings MUI `FocusTrap` lors des tests des modaux. Le vrai `MainCard` forward correctement en prod. Fix : utiliser `React.forwardRef()` dans le mock de `MainCard`. Mais cosmétique uniquement — les tests passent, les warnings n'affectent rien.                                                                                                                                                                                                                                                                       |

| TD-22 | Méthodes Signals stubed dans `CompanyAccountSerializer` (`get_*_data`) | Sprint F2.1 (test manuel post-merge) | PARTIAL | Sprint F (signals re-integration) | `CompanyAccountSerializer.get_profile_data()` appelait `obj.get_profile_data()` qui n'existe pas sur `CompanyAccount` (oubli lors de la migration `apps.signals` → `app_modules.signals`). Provoquait HTTP 500 à la création de Account. Fix appliqué 4 juin 2026 : stub `return None` + TODO, cohérent avec les 3 méthodes sœurs (`get_qualification_data`, `get_qualification_by_department`, `get_tech_stacks_data`) déjà stubed lors de la migration initiale. PR `fix/account-profile-data-method-stub` mergée. Reste à faire : reconnecter les 4 méthodes au `SignalDataService` une fois la migration `app_modules.signals` finalisée. Référence : commentaire `# A MODIFIER UNE FOIS CREER AVEC SIGNALS DANS APP_MODULE` en tête de `app_modules/accounts/models.py`. |
| TD-23 | Cache stale 60s sur `GET /decision_cycles/by-account/{uuid}/` après création d'un DC | Sprint F2.1 (reconstruction données dev) | RESOLVED | DC Cleanup sprint (cache invalidation) | Fixed: `_invalidate_cycle_caches(client_id)` now called in `create`, `update`, and `destroy` actions of `DecisionCycleViewSet`. Invalidates tags `decision_cycles`, `activities`, `accounts`. Pattern already existed for `close` and `reopen` actions. |
| TD-24 | Supabase Session Pooler saturé (15 connexions max sur free tier) | Sprint F2.1 (reconstruction données dev 4/6/2026) | RESOLVED | Fix immédiat 4 juin 2026 | Symptôme : déconnexions intempestives toutes les 1-2 minutes pendant le développement, avec `FATAL: (EMAXCONNSESSION) max clients reached in session mode - max clients are limited to pool_size: 15`. Cause : le `.env` pointait sur le **Session Pooler** (port 5432, 15 connexions max sur free tier) au lieu du **Transaction Pooler** (port 6543, ~200 connexions max). Migration accidentelle lors de TD-17 (3 juin 2026, fix DNS direct connection) — le bon pooler aurait été le Transaction. Fix appliqué 4 juin 2026 : changement port `5432 → 6543` dans `DATABASE_URL`. Limitations connues du Transaction Pooler : pas de prepared statements (Django les utilise peu), pas de session-level state (advisory locks, listen/notify, transactions très longues). Aucun impact pour ce projet. Lien : la résolution définitive viendra avec la migration vers Postgres local Docker (chore infra dédié, hors scope MVP). | |
| TD-25 | Wizard Step 1 ne ré-active pas les checkboxes après modification du transcript | Sprint F3+F4 smoke test 5 juin 2026 | OPEN | Sprint UX post-F8 ou bug fix dédié | Quand l'utilisateur modifie le transcript dans `EditableTextBlock` après une extraction réussie, le wizard Step 1 garde les checkboxes "Already extracted" grisées au lieu de les ré-activer. La détection se base sur `lastRun.input_hash` backend, pas sur un recalcul frontend du hash du transcript courant. **Acceptable MVP** car évite les re-runs accidentels coûteux en tokens LLM. Contournement : modifier suffisamment le transcript pour changer le hash (mais le wizard ne détectera pas la différence). À résoudre proprement quand on retravaillera le wizard. |
| TD-26 | ManagerNote migrations `0015`+`0016` non squashées | Step 7 (DC Workspace) | OPEN | Chore migration cleanup | `0015_managernote` (écrite à la main) + `0016_alter_managernote_client_id_and_more` (réconciliation `client_id`/`created_by`/`updated_by` générée par makemigrations) sont toutes deux sur main et appliquées. La 0015 manuelle ne matchait pas exactement `ModuleBaseModel`/`ClientScopeManager`, d'où la 0016. À squasher en une seule migration propre lors d'un cleanup (ne pas churner la DB dev déjà migrée). |
| TD-27 | Coaching manager-notes tenant-wide (non team-scoped) | Step 7 (DC Workspace) | OPEN | Revisite si policy org l'exige | `ManagerNoteViewSet._resolve_parent_cycle` résout le cycle parent par `client_id` uniquement (override local ; le mixin partagé `CycleScopedMixin` reste inchangé pour DealProduct). Conséquence : tout manager/admin du tenant peut poster/supprimer des notes sur n'importe quel cycle ; le role gate (manager/admin) est la seule garde. Isolation cross-tenant préservée (mismatch `client_id` → 404). Revisiter pour un scope team si besoin métier. |
| TD-28 | `DecisionStep.manager_notes` déprécié, laissé en DB | Step 7 (DC Workspace) | PARTIAL | DC Cleanup sprint (serializer write removed) | Partially resolved: `manager_notes` removed from `DecisionStepUpdateSerializer.Meta.fields` (no longer writable via API). Field still exposed read-only in `DecisionStepSerializer` for backward compatibility. DB column remains — drop in a future chore migration once non-regression confirmed in prod. |
| TD-29 | Front manager-notes hors intention produit | Step 7 (DC Workspace) | OPEN | Prochain sprint DC (recadrage) | `ManagerNotesThread.jsx` (style bulles/chat adapté de ChatHistory) + onglet Overview ne collent pas à la vision produit visée (flag PO). Backend (ManagerNote + endpoints + permissions + tests 16/16) solide et couvert ; chantier front-only à recadrer. Point de départ logique du prochain sprint. |
| TD-30 | DC Signals tab : polish différé | Step 7 (DC Workspace) | OPEN | Polish pass DC | Pas d'édition inline pour people/constraints (guard `EDITABLE_TYPES`) ; `SignalQuickDrawer.renderDetails` renvoie un corps vide pour ces deux types ; `getSummary` affiche « — » pour people alors que la card montre rôle+nom (incohérence mineure) ; `SignalTypeChip` : people & constraints partagent la couleur `default` avec blocker (7 couleurs de base pour 8 types). Tout cosmétique/non bloquant. |
| TD-31 | Zombie `test_postgres` connections bloquent le setup pytest | Step 7 (validation) | PARTIAL | Sprint A (TD-31 conftest zombie kill) | Partially fixed: `_kill_zombie_test_db_sessions()` in root `backend/conftest.py` terminates residual `test_postgres` sessions at session start (`pytest_configure`) and end (`pytest_unconfigure`); `CONN_MAX_AGE=0` injected for test runs to prevent persistent ORM connections. **Effective fix for `SystemExit: 2`** — tests now pass in isolation and in suite (192 passed, 0 errors on `tests/ai_pipelines/`). **Remaining**: cosmetic `PytestWarning` may still appear at end of suite (Django pool sometimes does not close before pytest-django DROP DATABASE). No functional impact. |
| TD-32 | `NextStepSignal(summary=…)` — champ inexistant, test rouge | Step 7 (run intégral, pré-existant) | RESOLVED | DC Cleanup sprint (test fix) | Fixed: replaced `summary='next step with quote'` with `suggested_title='next step with quote', suggested_activity_type='CALL'` in `test_nextstep_list_includes_source_quote_and_metadata`. |
| TD-33 | Throttle PROD `standard` rate drift (500/min vs 30/min) | Step 7 (run intégral, pré-existant) | PARTIAL | DC Cleanup sprint (AIRateThrottle added) | Partially addressed: added dedicated `AIRateThrottle(scope='ai')` with `60/min` DEV / `30/min` PROD, wired to `DealHealthRunView` and `ActivityExtractionView`. The `standard` scope rate drift (500/min PROD vs 30/min expected by test) remains open — investigate separately whether test or config is wrong. **MAJ (docs rattrapage, run post-#108, 2026-08-04)** : re-confirmé — `test_throttle_rates_configured` échoue dans LES DEUX fichiers (`test_throttling.py` + `test_throttling_2.py`) = les **2 échecs** de la « PARTIE 3.2 » du rattrapage. Toujours non tranché test vs config (env=500/min, test attend 30/min). Cette dette EST déjà le sujet 3.2 — pas de nouvelle entrée créée (réconciliation). |
| TD-34 | Suite throttling cassée (fixtures + reset) | Step 7 (run intégral, pré-existant) | OPEN | Chore test infra | 8 erreurs de fixtures : `UserRole() got unexpected keyword 'delete'` (×6), `Team() got unexpected keyword 'organization'` (×2) — kwargs absents des modèles. + `TestResetThrottle` ×3 (`assert True is False`). Même classe de bug que les fixtures de rôle corrigées en Step 7. Hors périmètre Step 7. **MAJ (sprint campagne)** : resurfacé au run intégral de ce sprint — mêmes fixtures périmées (`UserRole(delete=)`, `Team(organization=)`) + `TestResetThrottle` (`tests/integration/test_throttling*.py`, `test_end_users_user_view.py`). Le drift de rate 500 vs 30 attendu est suivi séparément en TD-33. À rafraîchir tests/fixtures. **MAJ (docs rattrapage, run post-#108, 2026-08-04)** : re-confirmé empiriquement — les **10 ERREURS** de la « PARTIE 3.1 » sont toutes `TypeError: UserRole() got unexpected keyword arguments: 'delete'` (`test_throttling.py` ×8 + `test_throttling_2.py` ×2). Preuve d'antériorité des champs : `UserRole.delete`→`can_delete` = migration `end_users/0009_rename_delete_userrole_can_delete` ; `Team.organization` retiré (champ commenté `end_users/models/team.py:24`, migration `0012_transition_to_client_account`) — les deux datées **2026-06-19** (merge #57 ; le rattrapage annonçait « PR #56 », l'historique dit #57 — écart mineur signalé). **TestResetThrottle** (« PARTIE 3.3 », 3 échecs) : passe VERT en ISOLATION (`3 passed`) au run 2026-08-04 ; l'échec « assert True is False » est donc ordre/état-dépendant (fuite d'état throttle en suite complète), pas déterministe — nuance vs le « 3 échecs » annoncé. Ces sujets (3.1 + 3.3) SONT déjà cette entrée — pas de nouvelle entrée créée (réconciliation). |
| TD-35 | Suppression user vs `Campaign.owner` PROTECT → 500 | Step 7 (run intégral, pré-existant) | RESOLVED (PR #105/#107, 2026-08-04) | Cycle de vie utilisateur (PR #105/#107) | `test_delete_only_admin_allowed` + `test_delete_superuser_allowed` : supprimer un user qui possède une campagne targeted auto-bootstrappée lève `ProtectedError` (FK `on_delete=PROTECT`) → HTTP 500. Le `destroy` devrait réassigner/nuller l'owner ou renvoyer un 409 propre. Symptôme lié : `targeted_campaign_bootstrap_failed` qui fire en boucle au setup. Hors périmètre Step 7. **MAJ (sprint campagne) — BLOQUANT, vrai risque prod** : même famille que C4/Lot1 — à résoudre en message élégant (réassigner les campagnes du user AVANT suppression). `Campaign.owner` en PROTECT lève une `IntegrityError` NON GÉRÉE au `destroy` (`end_users/views/user_view.py:790`). Re-détecté via `tests/integration/end_users/test_perm.py` (`test_delete_only_admin_allowed`, `test_delete_superuser_allowed`). **RÉSOLU (docs #105/#107, 2026-08-04)** : la suppression d'un user est désormais INTERDITE inconditionnellement (`_validate_user_deletion` + filet `ProtectedError` capturé + refus sur le bulk), donc le chemin `user.delete()` → `ProtectedError`/500 est fermé. Les deux tests cités ont été REMPLACÉS par des tests « refused » (`test_delete_refused_even_for_admin` / `_superuser`, `test_delete_refused_when_user_owns_a_deal`, `test_bulk_delete_refused`) — vérifiés VERTS au run post-#108 (2026-08-04). Le stub `test_list_superusers_allowed_all_roles` (sans rapport avec la suppression) reste ouvert → voir TD-36. |
| TD-36 | `/client/users/superusers/` → 400 | Step 7 (run intégral, pré-existant) | OPEN | Bug fix dédié | `test_list_superusers_allowed_all_roles` attend 200 pour tous les rôles ; l'endpoint renvoie 400 (validation_error dans le queryset/serializer superusers). Hors périmètre Step 7. **MAJ (sprint campagne)** : re-confirmé ce sprint (`test_list_superusers_allowed_all_roles` → 400 au lieu de 200, scope superusers end_users). À investiguer. **MAJ (docs rattrapage, run post-#108, 2026-08-04)** : re-confirmé encore (`test_list_superusers_allowed_all_roles` échoue). C'est le **1 échec** de la « PARTIE 3.4 » (gate admin superusers) — attente périmée d'un endpoint admin-only, SANS rapport avec le refus de suppression de #105/#107. Déjà couvert ici (et cité en TD-77 item 4) → pas de nouvelle entrée créée. |
| TD-37 | Faux positif PII logging + teams audit log | Step 7 (run intégral, pré-existant) | OPEN | Cleanup mineur | `PIILeakPreventionTests::test_cannot_leak_pii_via_dict_keys_iteration` : `'name'` matché comme sous-chaîne dans `'role_name'` → resserrer le matcher (word boundary). `teams::test_delete_team_with_audit_log` → `assert False` (audit log non émis/incomplet à la suppression d'équipe). Mineurs, hors périmètre Step 7. **MAJ (docs rattrapage, run post-#108, 2026-08-04)** : les DEUX re-confirmés au run 2026-08-04. **(3.5) PII** `test_cannot_leak_pii_via_dict_keys_iteration` = le **1 échec** PII (`'name'` sous-chaîne de `'role_name'`, matcher trop strict). **(3.6) audit log suppression d'équipe** `test_delete_team_with_audit_log` = le **1 échec** audit — **classé PRÉ-EXISTANT (PAS une régression de #104-#108)** sur preuve statique : le fichier de test (`tests/integration/test_teams_complete.py`) ET le code de suppression d'équipe (`end_users/views/team_viewset.py::destroy`, émission `audit_log`) ET `core/logging/audit.py` sont INCHANGÉS sur `3172655~1..e63e0d3` (dernière modif = merge #57) ; TD-77 le documentait déjà comme baseline. Cause racine probable : mismatch de cible de patch — le test patche `core.logging.audit.audit_log` alors que `team_viewset.py` a lié le nom à l'import (`from core.logging.audit import audit_log`), donc le patch ne re-lie pas la référence utilisée → `mock_audit.called` reste False bien que le vrai audit log parte. **Commande pour trancher définitivement l'antériorité (à lancer par le PO, non lancée ici)** : `git stash && git checkout 3172655~1 && (cd backend && python -m pytest tests/integration/test_teams_complete.py::TestTeamViewSet::test_delete_team_with_audit_log -x) ; git checkout main && git stash pop`. Si ROUGE avant #104 → dette antérieure confirmée ; si VERT → régression à traiter. Ces deux sujets (3.5, 3.6) SONT déjà cette entrée → pas de nouvelle entrée créée. |
| TD-38 | Filtres accounts stubés (`_filter_by_qualification` + `_filter_by_signals_freshness`) | DC Cleanup audit | OPEN | Sprint filtres dédié | `_filter_by_qualification` and `_filter_by_signals_freshness` are no-ops in `CompanyAccountViewSet`. Frontend sends `has_qualification` and `signals_since_days` params (`frontend/src/api/admin/accounts.js:116-123`) but backend ignores them. Implement deterministic filters by dimension/tech_stack/qualification/freshness, scoped to the Accounts list page (not account/activity workspace). |
| TD-39 | Themes should migrate from Deal Health to Signals tab | DC Cleanup audit | OPEN | Sprint Themes dédié | Themes are currently part of the Deal Health snapshot output. They should migrate to the Signals tab with floating toggles (style Activity Signals "Grouped"). Backend pipeline output `themes[]` should be derived from validated signals, not from the LLM deal-health diagnostic. |
| TD-40 | QA Rule 7 deferred (anti-duplication dimensions/themes) | DC Cleanup audit | OPEN | After TD-39 (Themes migration) | Rule 7 (anti-duplication of dimensions/themes across snapshot sections) should be added to the deal-health system prompt AFTER the Themes migration (TD-39) is complete, since the themes output structure will change. |
| TD-41 | BlockerSignal `target_department` FK missing | DC Cleanup audit | OPEN | Sprint dédié (spec §11.5) | BlockerSignal currently supports only contact-level attribution. Department-level attribution (`target_department` FK) should be added to align with PainSignal/ImpactSignal pattern (spec §11.5). |
| TD-42 | Stale `requirements.txt` + `requirements.lock.txt` duplicates at repo root | Cleanup post-Dependabot (PR #54 fallout) | RESOLVED | Cleanup post-Dependabot | Removed: stale duplicates at repo root (Django 5.1.11 instead of 5.1.14, missing typing_extensions bump, extra packages like `anthropic`, `click`, `httptools`). The single source of truth is `backend/requirements.txt`. Not referenced by any CI, Dockerfile, or script. |
| TD-43 | npm vulnerabilities partially resolved (4 moderate remaining) | Dependabot batch (PR #54) | OPEN | After INTERDIT major bumps lifted (Next.js 16, next-auth major) | Remaining vulns: postcss (Next.js-bound, fix requires Next.js 16), uuid in next-auth dependency chain (fix requires next-auth 3.x downgrade). All blocked by INTERDIT majors. Will be resolved when Next.js 16 or next-auth migration becomes acceptable. |
| TD-44 | `frontend-Model/` legacy MUI component template — ~124 vulns | Dependabot batch audit (PR #54) | OPEN | Cleanup sprint dédié (suppression du dossier) | Mantis MUI template v3.3.0, ancienne stack (draft-js, react-draft-wysiwyg, axios old, Next.js old). Not imported anywhere in `frontend/src/` or `.github/`. Kept as a component library reference for future MUI work, but ~124 known vulnerabilities (7 critical, 23 high, 25 moderate, 7 low). To delete in a dedicated cleanup sprint once the library is no longer consulted. |
| TD-45 | Verbose backend boot logs (`permissions.registry` DEBUG dump) | Cleanup post-Dependabot (observed) | OPEN | Sprint UX/logs futur | Each Django boot logs ~23 lines of `DEBUG logger=permissions.registry msg="  - <module>"` (one per registered module). Cosmetic but pollutes stdout. Reduce logger level to INFO for `permissions.registry` or remove the per-module loop in `permissions/registry/__init__.py`. |
| TD-46 | Multi-contact prep-call brief (target_contact single → multi) | Sprint Prep Call MVP (V8) | OPEN | V2 Prep Call | MVP: single optional `target_contact` in POST body (`contact_id`). V2: accept an array of contacts present at the meeting, generate per-person stakeholder focus and merged brief. Link: `app_modules/ai_pipelines/serializers/prep_call_serializer.py` (contact_id optional field). |
| TD-47 | Prep-call objectives feedback loop (visé/réalisé) | Sprint Prep Call MVP (V8) | OPEN | V2 Prep Call | MVP: objectives generated by LLM with `success_criteria`, no post-call comparison. V2: rep marks each objective as achieved/partial/missed after the call; feedback fine-tunes future briefs. Link: `app_modules/ai_pipelines/prompts/prep_call/brief_v1.py` (output schema `objectives[].success_criteria`). |
| TD-48 | Prep-call stakeholder_focus contact-level attribution | Sprint Prep Call MVP (V8) | OPEN | V2 Prep Call | MVP: `stakeholder_focus` filtered by target_contact's department. V2: contact-level signal attribution (who said/did what) for richer stakeholder analysis. Link: `app_modules/ai_pipelines/services/prep_call/input_pack_assembler.py`. |
| TD-49 | `Activity.primary_contact` FK absent | Sprint Prep Call MVP (V8) | OPEN | V2 Prep Call | Workaround MVP: `contact_id` passed in POST body to `runPrepCall`. V2: add `Activity.primary_contact` FK (migration + frontend propagation) so the contact is persisted on the activity model. Link: `app_modules/activities/models.py`. |
| TD-50 | Redis idempotence layer 1 for Prep Call pipeline | Sprint Prep Call MVP (V8) | OPEN | V2 Prep Call (if needed) | MVP: DB hash dedup layer 2 only (consistent with `DealHealthRunView`). V2: add `start_op`/`complete_op` Redis layer 1 to block concurrent rapid clicks and save LLM tokens. Link: `app_modules/ai_pipelines/views/prep_call_view.py`. |
| TD-51 | ESLint error: `useSWRConfig` called outside React component in `swrFetcher.js` | Smoke test Prep Call MVP | OPEN | Bug fix dédié (Hook misuse) | `frontend/src/utils/swrFetcher.js:406` — `useSWRConfig` est appelé dans `analyzeSWRCache()` qui n'est ni un composant React (pas de uppercase) ni un Hook custom (pas de prefix `use`). Soit renommer en `useAnalyzeSWRCache()` et changer les call sites, soit déplacer l'appel `useSWRConfig` dans le code consommateur. Pré-existant au sprint Prep Call. Bloque `npm run build` en mode strict (warning seulement aujourd'hui mais peut casser CI selon config). |
| TD-52 | Test suite Postgres-only (legacy `varchar(None)` migration breaks SQLite) | QA blockers sprint (test infra) | OPEN | Chore migration cleanup (confirms TD-13) | Confirmation empirique de TD-13 : lancer pytest sur SQLite échoue au `CREATE TABLE company_accounts` avec `sqlite3.OperationalError: near "None": syntax error`, car une migration legacy contient un `CharField` sans `max_length` → `varchar(None)`. La suite ne tourne donc que sur Postgres. `backend/tests/signals/conftest.py:59` strippe les `OPTIONS` postgres pour SQLite mais cela ne suffit pas. Corriger via la migration data-safe déjà prévue en TD-13. **MAJ (Sprint Competitors)** : re-confirmé tout le sprint — suites backend **Postgres-only**. |
| TD-53 | Arbre `sections/` hors périmètre du flat-config ESLint | QA blockers sprint (lint) | OPEN | Chore lint config | `frontend/eslint.config.mjs` — les fichiers sous `frontend/src/sections/**` (ex. `dc-workspace/OverviewTab.jsx`, `dc-workspace/ManagerNotesThread.jsx`) renvoient « File ignored because no matching configuration was supplied » : ils ne matchent aucun `files` glob de la config plate. Tout un pan de l'UI n'est donc jamais linté. Élargir les globs pour couvrir `src/sections/**`. |
| TD-54 | `mark_activities_generated` défini deux fois sur `CampaignContact` | QA blockers sprint (audit item 2) | OPEN | Cleanup mineur | `backend/app_modules/campaigns/models/campaign_contact.py:197` et `:202` — la méthode `mark_activities_generated` est déclarée deux fois à l'identique ; la seconde masque la première (aucun impact fonctionnel, corps identique). Supprimer le doublon. |
| TD-55 | Typo `"Authentication required33"` dans un message d'erreur core | QA blockers sprint (audit item 6) | OPEN | Cleanup mineur | `backend/core/error_messages.py:24` — `CoreErrorMessages.AUTH_REQUIRED = _("Authentication required33")` contient un `33` parasite exposé à l'utilisateur sur les 401. Retirer le suffixe. |
| TD-56 | `InlineImpactForm` envoie une clé `notes` non déclarée par le serializer Impact | QA blockers sprint (audit item 6) | OPEN | Cleanup mineur | `frontend/src/sections/activities/signals/wizard/forms/InlineImpactForm.jsx` (build du payload, ~:251) ajoute une clé `notes` au payload PATCH/POST, alors que `ImpactSignal{Create,Update}Serializer` ne déclarent pas ce champ (ImpactSignal n'a pas de champ `notes`). DRF l'ignore silencieusement — aucun effet aujourd'hui, mais payload trompeur. Retirer la clé `notes` du payload Impact. |
| TD-57 | Territoire CONTACT scope `team` non filtrable via param FK simple | QA blockers sprint (item 7, accepté comme follow-up) | OPEN | Sprint filtres territoires | `TerritoryContactsTab.jsx` mappe `contact_scope==='mine'` → `account_owner_id=<current user>`, mais `'team'` n'est pas exprimable via un simple filtre FK sur `ContactFilter` (il faudrait `account__account_owner__team_id=<user.team>`). Le count backend (`territories/views/views.py:560-562`) gère déjà `team` ; la liste du tab, elle, reste non scopée pour `team`. Ajouter un param `account_owner_team_id` (ou équivalent) sur `ContactFilter` + `buildUrlWithParams` pour aligner la liste sur le count. |
| TD-58 | `FormTerritoryEdit` — bloc « Owner Scope » dupliqué, rendu deux fois | QA blockers sprint (item 7) | OPEN | Cleanup UI | `frontend/src/sections/territories/FormTerritoryEdit.jsx:410-463` et `:465-516` — deux `RadioGroup` de scope propriétaire identiques (le second `SectionTitle` codé en dur « Account Owner ») sont rendus sans condition, donc le formulaire d'édition affiche la section deux fois. Supprimer le doublon (garder le premier, au label dynamique). |
| TD-59 | `standard_department` envoyé au lieu de `standard_department_id` (filtre contact no-op) | QA blockers sprint (item 7) | RESOLVED | Sprint BI Foundation (Palier 4b) | `frontend/src/api/businessData/contacts.js:71-73` envoie le param `standard_department`, alors que `ContactFilter` déclarait `standard_department_id` (`backend/app_modules/contacts/filters.py`). Le param ne matchait aucun filtre → filtre département silencieusement ignoré (page Contacts d'un compte ET tab territoire). **RÉSOLU** (Palier 4b) : le param backend est renommé `standard_department_id` → `standard_department` (`field_name='standard_department_id'` conservé, résout la colonne FK id) — le backend accepte désormais la clé app-wide que le front envoie déjà. Direction du rename confirmée empiriquement (probe 2 contacts : `standard_department` renvoyait TOUT = no-op, `standard_department_id` filtrait mais n'était envoyé par personne). Impact vérifié isolé à l'API contacts (aucun KPI : KPI 4 couverture passe par `AccountFilterService`, sans gestion département). Tests : 2 ajoutés (`tests/contacts/test_contact_filter.py`). |
| TD-60 | Endpoint mort `/company-accounts/<id>/tech-stacks/` (AttributeError) | Audit TechStack clustering removal + catalog linking | OPEN | Sprint Overviews | `backend/app_modules/accounts/views/views.py:810` appelle `account.get_tech_stacks_data(**filters)`, méthode inexistante sur le modèle `CompanyAccount` (seul un stub commenté renvoyant `None` subsiste dans `accounts/serializers.py:275-287`). L'endpoint lève `AttributeError` (capturée par `handle_exception`). Indépendant du clustering — jamais relié à `SignalClusterService`. À refaire proprement au sprint Overviews. |
| TD-61 | Référence legacy `apps.accounts.models.TechStack` dans filter_service | Audit TechStack clustering removal + catalog linking | RESOLVED | Sprint S10 (suppression catalogue tech) | `backend/app_modules/accounts/services/filter_service.py:302,307` importe et requête `apps.accounts.models.TechStack` — modèle du monolithe pré-modulaire (`apps/`), distinct de `TechStackSignal`. Le filtre `has_tech_stack` s'appuie donc sur l'ancien modèle. À migrer vers le modèle modulaire (ou retirer si le filtre est mort). **RÉSOLU (S10, commit `a621fcc`)** : `_filter_by_tech_stack` requête désormais `app_modules.signals.TechStackSignal` sur `tech_name_normalized` (match EXACT sur la forme normalisée ; les valeurs d'entrée passent par `TechStackSignal._normalize_tech_name`, le helper du modèle — réutilisé, pas ré-implémenté, sinon la clé de requête et la clé stockée dérivent). Sémantique liste/OR conservée, scoping tenant conservé ; statuts PENDING + VALIDATED comptés, REJECTED exclu. L'import `apps.accounts.models.TechStack` a disparu — `app_modules` ne référence plus ce modèle legacy nulle part (`filter_service` importe encore `apps.accounts.BuyingProcess` pour un AUTRE filtre : le pont legacy est rétréci, pas fermé). **CORRECTION DE LA DESCRIPTION CI-DESSUS** : la dette décrivait un filtre inerte (« s'appuie sur l'ancien modèle », donc ne matche rien). La repro rouge a prouvé PIRE — `accounts_techstack.account_id` est un `bigint` (FK vers l'`Account` legacy) alors que le queryset filtré est `CompanyAccount` en `uuid` : le sous-requête `Exists()` LEVAIT `ProgrammingError: operator does not exist: bigint = uuid`. Le bloc filtre de `accounts/views/views.py` n'attrape que `ValueError` → **toute requête portant `?has_tech_stack=` renvoyait un 500**, pas un no-match silencieux. Couverture : `backend/tests/territories/test_account_tech_stack_filter.py` (20 cas). |
| TD-62 | Index inert `tssig_account_canon_idx` sur TechStackSignal | Audit TechStack clustering removal + catalog linking | RESOLVED | Sprint S10 (suppression catalogue tech) | `backend/app_modules/signals/models/tech_stack_signal.py` — index composite `(account, canonical_key)` conçu pour le lookup cluster TechStack. TechStack n'étant plus clusterisable (canonical_key jamais calculé), l'index est inerte. Sa suppression nécessite une migration (reportée pour respecter la contrainte "pas de nouvelle migration" du sprint). **RÉSOLU (S10, commit `58c9103`)** : index composite `(account, canonical_key)` supprimé par la migration `module_signals/0024`, en même temps que `tssig_catalog_idx` (index sur la FK catalogue) et `painsig_account_techref_idx` (index sur `PainSignal.related_techstack`). La contrainte « pas de nouvelle migration » qui reportait ce nettoyage est levée : S10 écrivait de toute façon la migration de retrait des FK. |
| TD-63 | help_text stale (`canonical_key`) sur `TechStackSignal.tech_catalog_entry` | Audit TechStack clustering removal + catalog linking | RESOLVED | Sprint S10 (suppression catalogue tech) | `backend/app_modules/signals/models/tech_stack_signal.py` — le `help_text` du FK `tech_catalog_entry` mentionne encore « Drives canonical_key and cluster identity », faux depuis la suppression du clustering. Non corrigé dans le sprint car `help_text` fait partie de l'état de migration (`makemigrations` générait un `AlterField`). À corriger avec la prochaine migration TechStack légitime. **RÉSOLU (S10, commit `58c9103`)** : le FK `tech_catalog_entry` lui-même est supprimé (migration `module_signals/0024`), donc son `help_text` avec lui — le problème d'état de migration qui bloquait la correction disparaît avec le champ. Docstrings stale du chemin tech nettoyées dans la foulée (`tech_stack_signal.py`, `tech_stack_serializer.py`, `filters.py`, `signal_data_service.py`, `base_serializer.py`, `constants.py`) : plus aucune mention « drives canonical_key / cluster identity » sur TechStack. `canonical_key` reste sur `BaseSignal` (Pain/Objective/Impact en dépendent) et reste NULL sur TechStack. |
| TD-64 | Mentions tech (ex. HubSpot) manquées par l'extraction LLM | Audit TechStack clustering removal + catalog linking | OPEN | S7 (LLM prompt tuning) | L'extraction transcript techstack (`backend/app_modules/ai_pipelines/prompts/transcript_signals/techstack_v1.py` + `services/transcript_signal_extractor.py`) mappe correctement matched→FK et unmatched→`pending_tech_name` (XOR) — le pipeline est sain. Une mention manquée = le LLM n'émet rien (ou classe l'outil comme « seller's tool » et le drop) → problème de prompt/classification, pas de pipeline/mapping. Classé prompt→S7. |
| TD-65 | `anthropic` importé par ai_pipelines mais absent de `requirements.txt` | Sprint Notifications MVP (bootstrap env) | RESOLVED | Sprint Permissions & Visibility (Item 1) | `backend/app_modules/ai_pipelines/providers/claude_provider.py:46` fait `from anthropic import (...)`, chargé au démarrage via la résolution d'URLs (`ai_pipelines/views/__init__.py`). Le paquet `anthropic` n'est pas listé dans `backend/requirements.txt` (seul `openai` l'est) → sur un environnement vierge, `pip install -r requirements.txt` puis tout `manage.py`/pytest échoue en `ModuleNotFoundError: No module named 'anthropic'`. Vrai bug d'install (casse un env neuf), pas cosmétique. Ajouter `anthropic` (avec version pin) à requirements.txt. **RÉSOLU** : `anthropic==0.49.0` ajouté (version contemporaine du pin `openai==1.63.2` ; deps `anyio/distro/httpx/jiter/pydantic/typing-extensions` toutes satisfaites par les pins existants). Vérifié : `pip install -r` résout sur env vierge (venv isolé) et `manage.py check` charge `ai_pipelines`/anthropic sans erreur. |
| TD-66 | Pin `python-environ==0.4.54` introuvable + doublon `environ==1.0` dans `requirements.txt` | Sprint Notifications MVP (bootstrap env) | RESOLVED | Sprint Permissions & Visibility (Item 1) | `backend/requirements.txt` épingle `python-environ==0.4.54`, version inexistante sur PyPI (`ERROR: No matching distribution found for python-environ==0.4.54`). Le module `environ` utilisé par `settings.py` provient en réalité de `django-environ` (déjà listé, 0.11.2). La ligne `python-environ` et le doublon `environ==1.0` cassent `pip install -r requirements.txt` tel quel. Vrai bug d'install. Retirer `python-environ==0.4.54` et `environ==1.0`, garder `django-environ`. **RÉSOLU** : `python-environ==0.4.54` et `environ==1.0` retirés, `django-environ` conservé. `environ==1.0` confirmé comme distribution PyPI distincte exposant elle aussi un module `environ`, qui shadowe celui de `django-environ` utilisé en `settings.py:14`. Vérifié : `import environ` résout bien `django-environ` et l'install vierge est propre. |
| TD-67 | Notif tech-inconnue non câblée sur le chemin API directe de création de signal | Sprint Notifications MVP (scope) | RESOLVED | Sprint S10 (suppression catalogue tech) | L'émetteur E4 (tech inconnue → admins) est câblé uniquement sur le chemin d'extraction transcript (`backend/app_modules/ai_pipelines/services/transcript_signal_extractor.py`, hook `_maybe_notify_unknown_tech` après `SignalManager.create`). Le chemin API directe (`backend/app_modules/signals/views/base_views.py:281` `BaseSignalViewSet.perform_create` → `SignalManager.create`) ne notifie pas. Hors périmètre MVP : un TechStackSignal PENDING sans catalogue provient exclusivement du LLM en usage réel (création manuelle de signaux gelée par décision produit). À câbler si la création manuelle est réactivée. **RÉSOLU / OBSOLÈTE (S10, commits `31ea059` puis `58c9103`)** : la prémisse de la dette a disparu. « Tech inconnue » signifiait « non appariée au TechCatalog du tenant » ; sans catalogue, plus rien ne peut être inconnu. L'émetteur a d'abord été rendu DORMANT en sous-étape 2 (`31ea059`) — sans quoi la condition `tech_catalog_entry IS NULL` devenait vraie pour TOUT signal extrait, et chaque outil de chaque call aurait notifié tous les admins — puis SUPPRIMÉ en sous-étape 5a (`58c9103`) avec `_maybe_notify_unknown_tech`, son call site dans `persist_stage`, l'endpoint `/tech-stack/detected/` + sa route, et le membre `NotificationCategory.UNKNOWN_TECH_DETECTED` (migration `notifications/0004`). Si le produit veut plus tard un « nouvel outil vu sur ce compte », c'est un NOUVEL émetteur clé sur le nom normalisé, pas une résurrection de celui-ci. |
| TD-68 | Pas d'endpoint `mark-all-read` pour les notifications | Sprint Notifications MVP (scope) | OPEN | Sprint notifications V2 | Le MVP notifications expose le mark-read par notification (`POST /notifications/<id>/read/`) uniquement. Pas de `mark-all-read` (marquer toutes les notifs du caller comme lues en une requête), fonctionnalité UX courante d'un menu cloche, différée hors MVP. Ajouter un `POST /notifications/mark-all-read/` scopé recipient+client quand le besoin se confirme. |
| TD-69 | Deep-link des notifications non câblé (front) | Sprint Notifications MVP (scope) | OPEN | Sprint notifications V2 (produit) | Le modèle Notification porte `related_object_type`/`related_object_id`/`payload` pour permettre un deep-link vers l'objet source (cycle, activité, signal tech). Le composant `<Notification/>` (`frontend/src/layout/DashboardLayout/Header/HeaderContent/Notification/index.jsx`) marque lu au clic mais ne route pas vers l'objet — inventer 4 routes sans vérifier le routing frontend était hors scope-guard MVP. Câbler un resolver type→route au clic (V2). Le MVP « voir + marquer lu » est livré. |
| TD-70 | `permissions/scoping.py` (`build_q` / `_build_mine_filter`) = doublon mort non aligné sur l'héritage account-owner | Sprint Permissions & Visibility (audit C6) | OPEN | Chore permissions (si un jour rebranché) | Le filtrage mine/team réellement appliqué par les ViewSets est le builder inline de `ScopedQuerysetMixin.get_queryset` (`backend/permissions/mixins.py`). `backend/permissions/scoping.py` (`build_q`, `_build_mine_filter`, `apply_scope_filter`) est exporté dans `permissions/__init__.py` mais n'est appelé par aucun ViewSet — il n'apparaît que dans ses propres docstrings/exemples. Lors de ce sprint (C6), la clé d'héritage `account_owner_user` (`account__account_owner_id`) a été ajoutée au builder actif (`mixins.py`) mais PAS à ce doublon (hors chemin actif → minimal diff). Risque : si `scoping.build_q`/`apply_scope_filter` était un jour rebranché sur un ViewSet, il n'appliquerait pas l'héritage account-owner (un propriétaire de compte n'atteindrait pas les enfants créés par d'autres sur son compte) → divergence de sécurité silencieuse. À réconcilier (aligner sur `mixins.py` ou supprimer le doublon) si jamais rebranché. |
| TD-71 | `OWNERSHIP_MAP['pipelines']` + `['templates']` — attnames `_id`/notation `.` dormants sur modèle LEGACY | Sprint BI Foundation (balayage OWNERSHIP_MAP) | OPEN | Sprint 12 (legacy decommission) — **SUPPRIMER, PAS corriger** | `OWNERSHIP_MAP['pipelines']` (owner_user `opportunity.deal_owner_id`, owner_team `opportunity.team_id`, created_by `created_by_id`) et `['templates']` (created_by `created_by_id`) pointent sur `apps/opportunities` (`OpportunityPipeline`, `PipelineTemplate`) — modèles legacy du monolithe pré-modulaire, remplacés par `decision_cycles`. Les valeurs sont des attnames `_id` / notation `.` que `permissions.scope_filter._is_valid_field` REJETTE → `apply_role_scope` renverrait `none()` (mine) / sur-exposerait (team) SI atteint. **NE PAS corriger** (ce serait maintenir du code mort) : à SUPPRIMER avec les modèles au Sprint 12. Même classe de bug que territories/sales_quotas/accounts (corrigés ce sprint) mais sur du legacy. |
| TD-72 | `OWNERSHIP_MAP` — entrées cassées dormantes sur modèles VIVANTS (non atteintes / inertes) | Sprint BI Foundation (balayage OWNERSHIP_MAP) | OPEN | À normaliser SI un futur KPI/consumer les route (pas avant) | Balayage complet de l'`OWNERSHIP_MAP` (règle `_is_valid_field` : attname `_id` ou notation `.` → terme de scope rejeté → `apply_role_scope` peut renvoyer `none()`). Entrées cassées sur modèles vivants MAIS non atteintes par `apply_role_scope` aujourd'hui : **(a)** `organizations`, `sales_plans`, `sales_milestones` — aucun ViewSet ne pose `module='<clé>'` (leurs ViewSets utilisent `ClientScopeManager.ViewMixin`, scope client-only) → entrées orphelines ; **(b)** `roles`, `tech_catalog`, `product_catalog`, `signals`, `ai_pipelines` — `created_by: 'created_by_id'` (attname rejeté) mais `ownership_type='none'` → fallback client, terme jamais invoqué (inerte) ; AUCUN impact fonctionnel actuel (entrées non atteintes ou inertes). **NB** — les entrées ATTEINTES à termes de scope droppés (`teams` = sur-exposition, `users`/`contacts` = mineurs) NE sont PAS couvertes ici : voir **TD-76**. **Tracé pour qu'un futur KPI ne tombe pas dessus par surprise** — exactement comme territories/quotas/accounts l'ont été quand la BI les a touchées (`none()` silencieux → corrigés ce sprint). Cause racine systémique : `created_by` est un FK nommé `created_by` sur les 2 bases (`ModuleBaseModel`/`BaseModelApp`) → `created_by_id` est l'attname, toujours rejeté. À normaliser (attname→nom de champ FK nu, `.`→`__`) UNIQUEMENT si un KPI/consumer route par ces modules, avec iso-comportement prouvé + test dédié dans `tests/permissions/test_scope_filter.py` (recette rodée). **NB (Sprint 5) — `organizations` : ne PAS copier-coller la recette teams.** Quand on le normalisera, la valeur `created_by` doit être re-dérivée contre le `_meta` PROPRE d'Organization : Organization n'étend PAS le même base model que `Team` (Team = `core.models.BaseModel`, sans `created_by` → `'-'`). La MÉTHODE à reproduire est la vérif AVANT de toucher : lire `<Model>._meta.get_fields()` et poser `'-'` si le champ n'existe pas — c'est exactement le piège évité ce sprint en vérifiant `Team._meta` avant de fixer `teams`. Idem `owner_user`/`assigned_to_user` = `manager_id` (attname) → nom de champ FK nu réel sur Organization. |
| TD-73 | [Ops] Index perf C5/todo — `AddIndex` bloquants (CREATE INDEX non-concurrent) | Sprint BI Foundation (Palier 4a) | OPEN (note ops, pas un TD code) | Décision déploiement selon volume prod | Les migrations 4a (`module_notif_todo_exists_idx` sur `module_notifications`, `act_outcome_idx` + `act_completed_at_idx` sur `module_activities`, `dc_outcome_date_idx` sur `decision_cycles`) sont des `AddIndex` → `CREATE INDEX` **bloquant** (verrou écriture sur la table le temps du build). Correct et testé pour le sprint. Sur tables volumineuses en prod, envisager `AddIndexConcurrently` + `Migration.atomic = False` au déploiement (`CREATE INDEX CONCURRENTLY`, sans verrou long). **Note ops, pas un TD code** : on garde `AddIndex` simple (correct + testé) ; le CONCURRENTLY est une décision de déploiement selon le volume réel. |
| TD-74 | `DealProduct.discount_percent` sans contrainte de domaine 0-100 | Sprint BI Foundation (Palier 2, KPI 7 — champ discount_percent) | RESOLVED | Sprint C (Produit & Finance) | `DealProduct.discount_percent` (DecimalField, migration `0017`) n'a aucune contrainte 0-100 (ni `CheckConstraint` ni validators). Une valeur hors bornes (négative ou >100) fausserait le roll-up produit `line_total` = `Σ quantity × prix × (1 − discount%/100)` (KPI 7 `$ pipeline`/`$ result`). Ajouter un `CheckConstraint(0 ≤ discount_percent ≤ 100)` + validators DRF au sprint UI Produit (quand le formulaire produit sera câblé). **RÉSOLU Sprint C (commit `1327fe7c`)** : `CheckConstraint(0 ≤ discount_percent ≤ 100)` nommée `deal_product_discount_percent_bounds` (`models.py` Meta + migration `0020`, qui CLAMPE d'abord les lignes hors bornes via `RunPython` puis pose la contrainte — applicable sur n'importe quel environnement, aucune donnée à reprendre) + validation DRF `validate_discount_percent_range` partagée par les serializers create/update (400 métier propre, pas d'`IntegrityError` fuité). **Effet de bord assumé** : `discount_percent` n'était exposé par AUCUN serializer — la remise était impossible à saisir ; elle est désormais writable, bornée des deux côtés. Tests : `tests/decision_cycles/test_deal_product_discount_bounds.py` (26). |
| TD-75 | Réconciliation `DecisionCycle.estimated_value` (manuel) vs product roll-up | Sprint BI Foundation (Palier 2, KPI 7) | RESOLVED | Sprint C (Produit & Finance) | `DecisionCycle.estimated_value` est un champ manuel saisi indépendamment, distinct du roll-up produit calculé par KPI 7 (`Σ` sur `deal_products`). Les deux peuvent diverger (l'un manuel, l'autre dérivé des lignes). Décider au Sprint C lequel fait foi (roll-up dérivé recommandé) et réconcilier UI/serializers. KPI 7 utilise le roll-up, PAS `estimated_value` (champ manuel laissé tel quel). **Rattaché (audit S8a)** : `PIPELINE_VALUE` et `REVENUE_WON` — objectifs de campagne ET quotas personnels — sont aujourd'hui bloqués à 0 parce que leur calcul somme `estimated_value`, qu'AUCUN chemin runtime ne peuple (le montant saisi va dans `DealProduct.line_total` sans roll-up vers `estimated_value`). Ces deux métriques dépendent donc de CETTE réconciliation ; à débloquer au Sprint C. **RÉSOLU Sprint C.** (1) Montant DÉRIVÉ au read-time, source UNIQUE : la formule est déclarée une seule fois dans `decision_cycles/services/deal_value_sql.py` (`_line_value`), liée de deux façons — `DEAL_VALUE_SUM` (forme jointure, `.aggregate()`) et `annotate_deal_value()` (sous-requête corrélée isolée, agrégats de masse). `DecisionCycle.total_deal_value` en est l'accesseur mono-instance (gratuit sur un queryset annoté, 1 requête sinon) ; KPI 7 consomme la MÊME expression (commit `fb105d16`). À noter : `line_total` n'a JAMAIS été une colonne stockée côté `app_modules` (la colonne stockée n'existe que dans le legacy `apps/opportunities`) — le paradigme read-time est désormais VERROUILLÉ par introspection de table + tests de fraîcheur (commit `f6cb869d`). (2) TOUTES les agrégations de montant DC repointées sur le roll-up : `bi/metrics/sales_metrics` (pipeline_value / revenue_won), `campaign_analytics_service` (agrégat groupé), `campaign_objective_progress` (page LISTE campagnes), `bi/quota` (SalesQuota legacy) — 5 sites, aucun point central unique, donc chacun repointé et testé (cross-coverage TD-125). `grep "Sum('estimated_value')"` = ZÉRO dans `app_modules/` + `end_users/` (commit `1ca74aa8`). `estimated_value` est CONSERVÉ en base mais n'est plus lu par aucun KPI (reste dans le payload liste + les packs IA). (3) Sémantique CRM : `PIPELINE_VALUE` = Σ DC OUVERTS (`outcome IS NULL`), `REVENUE_WON` = Σ DC GAGNÉS (`outcome=WON`) — pipeline EXCLUSIF, un deal gagné ou perdu en est SORTI ; le critère est le champ `outcome` : `outcome IS NULL` = DC ouvert (dans le pipeline) ; tout DC portant un `outcome` (WON, LOST, ou ON_HOLD) est SORTI du pipeline. `REVENUE_WON` filtre `outcome=WON` (commit `9027342c`). (4) Moteur de réalisé des quotas personnels TYPÉ (pipeline borné par la clôture attendue, won borné par `outcome_date`), scope rep = ses DC / manager = sous-arbre d'équipe via le helper de scoping existant `team_subtree_ids` (commit `a420a260`). **Débloque** les objectifs de CAMPAGNE, les objectifs PERSONNELS et les QUOTAS personnels, tous structurellement à 0 auparavant. |
| TD-76 | `OWNERSHIP_MAP` — entrées ATTEINTES à termes de scope droppés (`teams` sur-exposition, `users`/`contacts` mineurs) — restantes du 🟢 CORRIGER | Sprint BI Foundation (balayage OWNERSHIP_MAP) | PARTIAL | `teams` RESOLVED (S5) ; `users`/`contacts` OPEN | **✅ Sprint 5 (Home & Overviews) — `teams` RESOLVED** (commit `a1fdfb7`) : `owner_user`/`assigned_to_user` `manager_id`→`manager`, `created_by_id`→`created_by` **vérifié : `Team` étend `core.models.BaseModel` qui n'a PAS de `created_by` → `created_by='-'` (PAS `'created_by'`)**, et `client_account_fk` stale `'organization.client_account_id'`→`'client_account'`. Fuite cross-tenant écartée par le mécanisme : le terme dotté du mixin était skippé, mais `BaseAPIView.filter_queryset_by_client` applique l'isolation tenant une couche plus tôt via le FK `client_account` réel (52 tests d'intégration teams le confirment). Iso prouvé : suite permissions 15→18 passed (+3 guardrails `TestTeamOwnerScope` dont la garde de sur-exposition `team` ≠ toutes-les-équipes). **`users`/`contacts` restent OPEN** (mineurs — `mine` marche déjà via `owner_user='id'` / traversées account ; même recette, priorité basse, non recréer l'imprécision). — Le balayage classait **4** entrées en 🟢 CORRIGER (vivantes ET atteintes par `apply_role_scope`) ; seule **`accounts`** a été corrigée ce sprint. Les 3 restantes sont ATTEINTES (ViewSets = `ScopedQuerysetMixin`) et NON couvertes par TD-72 (qui ne trace que le non-atteint/inerte). **(1) `teams` — SÉVÈRE / TIER 1** : `owner_user='manager_id'` + `created_by='created_by_id'` + `assigned_to_user='manager_id'` tous rejetés par `_is_valid_field` → `mine` = `none()` (deny total) ET `team` = queryset **INCHANGÉ = TOUTES les équipes du tenant** (SUR-EXPOSITION intra-tenant : `_apply_team_scope` renvoie le queryset non filtré quand aucun terme n'est valide). Atteint (`TeamViewSet` = `ScopedQuerysetMixin`, `ownership=user`). **Pré-existant, PAS une régression du sprint.** → à corriger TÔT : `manager_id`→`manager`, `created_by_id`→`created_by`, `assigned` `manager_id`→`manager`, avec iso-comportement prouvé + test dédié (`tests/permissions/test_scope_filter.py`), comme `accounts`. **(2) `users` — MINEUR** : `owner_team='team_id'` + `created_by='created_by_id'` rejetés → perte partielle de termes, mais `owner_user='id'` valide → `mine` marche (soi-même), pas de `none()`. **(3) `contacts` — MINEUR** : `created_by='created_by_id'` rejeté → perte du terme créateur, mais les traversées `account__account_owner_id`/`account__team_owner_id` (mine/team) marchent. Fix `users`/`contacts` = même recette, priorité basse. |
| TD-77 | Baseline pré-existant : 4 échecs permissions/end_users indépendants du scope | Sprint 5 (bootstrap env de test) | OPEN | Chore end_users (FK `on_delete`) | Baseline iso-comportement de la suite permissions/integration, documenté pour ne pas le redécouvrir chaque sprint. Les **4** échecs, confirmés AVANT toute modif du Sprint 5 : (1) `tests/integration/test_teams_complete.py::TestTeamViewSet::test_delete_team_with_audit_log` (`assert mock_audit.called` — audit log non émis à la suppression d'équipe) ; (2-4) `tests/integration/end_users/test_perm.py::{test_delete_only_admin_allowed, test_delete_superuser_allowed, test_list_superusers_allowed_all_roles}` → **ProtectedError/500 sur `user.delete()`** (FK `on_delete=PROTECT`, type `SalesQuota.assigned_by` référençant le user). Fix = décider la politique de suppression (SET_NULL / réassignation / cascade contrôlée) sur les FK PROTECT pointant User, dans un chore end_users. Non lié à la BI/Home. **MAJ (docs rattrapage #105-#108, 2026-08-04)** : le baseline a bougé. **Items (2) et (3)** [`test_delete_only_admin_allowed`, `test_delete_superuser_allowed`, ProtectedError/500 sur `user.delete()`] → **RÉSOLUS** : #105/#107 interdisent inconditionnellement la suppression user (voir TD-35) ; ces deux tests n'existent PLUS (remplacés par `test_delete_refused_*`, verts). **Item (1)** `test_delete_team_with_audit_log` → toujours OUVERT, re-classé PRÉ-EXISTANT (code+test inchangés sur `3172655~1..e63e0d3`, cause = mismatch de cible de patch) — détail + commande de vérif en TD-37. **Item (4)** `test_list_superusers_allowed_all_roles` → toujours OUVERT (gate superusers, voir TD-36). Il ne reste donc que les items (1) et (4). |
| TD-78 | Manager Home — attainment par personne = boucle de N KPIs scalaires | Sprint 5 (item 4) | OPEN | Si une équipe atteint ~100 membres | `TeamQuotaGroup` boucle `quota_attainment(quota_id)` par membre (batch chunké au cap 20). OK à 5-15/équipe (cache par params du S4 fait le travail). À ~100 membres, remplacer par UN KPI owner-dimensionné (`quota_attainment_by_owner`, breakdown `dimension=user`) — l'archi le permet en UNE déclaration (cf. `todo_team_by_owner`). Gain marginal à l'échelle actuelle → différé. |
| TD-79 | Manager Home bloc 1 — les personnes à 0 tâche ouverte ne sont pas listées | Sprint 5 (item 4) | OPEN | Si le manque se fait sentir à l'usage | `todo_team_by_owner` compte les activités OUVERTES (PLANNED/ON_HOLD) ; une personne à 0 ouverte n'apparaît pas → impossible de distinguer "a tout fini" de "n'avait rien" sans un `todo_team_done_by_owner` (compte des COMPLETED du jour par owner). Choix assumé ce sprint : on montre qui a ENCORE du travail (= l'actionnable), pas un appel nominal complet. Voie de sortie : UNE déclaration de plus (breakdown owner sur les COMPLETED), l'archi le permet. À traiter si un besoin de "tableau de présence" complet émerge à l'usage. |
| TD-80 | Deux KPIs de comptage todo coexistent (`todo_my_activities` par bucket + `todo_my_windows` par fenêtre) | Sprint 5 (I7b) | OPEN | Sprint 12 (une fois `todo_my_activities` confirmé sans consumer) — **SUPPRIMER** | `todo_my_activities` (breakdown par `due_bucket` : overdue/today/upcoming/no_date) et `todo_my_windows` (breakdown par fenêtre : overdue/today/this_week/this_month) lisent TOUS DEUX `build_todo_population` (même population single-source → pas de divergence possible). La Home Rep consomme `todo_my_windows` (les 4 tuiles) ; `todo_my_activities` (buckets) n'est plus câblé côté Home mais reste enregistré. Coexistence assumée (le single-source garantit qu'ils ne peuvent pas diverger). **Supprimer `todo_my_activities` au S12** UNE FOIS confirmé qu'aucun consumer ne le lit — pas avant (risque de casser un appel résiduel). Même prudence que TD-71 (supprimer, pas maintenir). |
| TD-82 | [Front/deps] `react-csv` et `@dnd-kit/core` importés par le barrel `components/third-party/react-table` mais ABSENTS de `package.json`/`node_modules` | Sprint 5 (I7c — smoke fix, en montant le vrai ReusableTable en test) | RESOLVED (PR #102, 2026-07-30) | Sprint build-health (PR #102) | `components/third-party/react-table/CSVExport.jsx` importe `react-csv` (`CSVLink`) et `DraggableRow.jsx` importe `@dnd-kit/core` (`useDraggable`/`useDroppable`) ; les deux packages ne sont NI dans `package.json` NI dans `node_modules`. L'app tient aujourd'hui parce que Next compile en lazy par route et que ces deux composants ne sont **jamais rendus** (`ReusableTable` importe le barrel mais n'utilise ni CSVExport ni DraggableRow) → le chemin d'import n'est pas exercé au runtime. **Risque** : un `npm ci` sur environnement vierge, un build de prod, ou tout futur import eager (ou premier rendu de CSVExport/DraggableRow) casserait sur `Failed to resolve import`. Pendant frontend de TD-65/66 (deps réellement manquantes côté install). **À trancher** : soit ajouter `react-csv` + `@dnd-kit/core` (+ `@dnd-kit/sortable`/`utilities` si DraggableRow les tire) aux deps, soit RETIRER `CSVExport`/`DraggableRow` du barrel s'ils sont du code mort (vérifier les consumers dans tout `src/` AVANT de supprimer). **Découvert** en montant le vrai `ReusableTable` dans le test d'intégration `TodoActivityTable.integration.test.jsx` : vitest résout le graphe de modules **eagerly** (contrairement à Next), d'où l'échec sur ces imports → 2 alias de stub ajoutés dans `vitest.config.js` (`react-csv`, `@dnd-kit/core` → `src/__tests__/_stubs/`). **Ces alias sont un échafaudage de test à RETIRER si les deps sont ajoutées au projet.** — **ADDENDUM (Sprint 5, item 4c-2)** : le TD n'est PAS que théorique. `react-csv` est aussi importé par `components/third-party/react-table/TableHeaderActions.jsx:17` (`import { CSVLink } from 'react-csv'`), et **`TableHeaderActions` est rendu par TOUTES les tables du repo** via `ReusableTable` (il est monté inconditionnellement dans la toolbar). Donc `react-csv` est sur le **chemin de rendu de chaque page-liste** (accounts, users, roles, campaigns, …), pas dormant : un `npm ci` propre ou un build de prod casserait au **premier rendu de n'importe quelle table**. Priorité relevée en conséquence. (`@dnd-kit/core` reste, lui, dans le cas dormant : `DraggableRow` n'est pas rendu.) **Note annexe** : `TableHeaderActions.jsx:42` contenait un `console.log('🔵 …')` de debug pré-existant qui s'exécutait à chaque rendu de chaque table du repo. **RÉSOLU (Sprint 5, item A4)** : retiré en même temps que le revert `enableExport` (on était déjà dans le fichier — pas un détour). **RÉSOLU (PR #102, 2026-07-30, via TD-18)** : `react-csv` et `@dnd-kit/core` sont désormais DÉCLARÉS dans `frontend/package.json` — le risque « `npm ci` propre / build de prod casse au premier rendu de table » est fermé (build vérifié exit 0). Question annexe tranchée : `@dnd-kit/sortable`/`utilities` ne sont importés NULLE PART (voir TD-133), donc non ajoutés. **Reliquat** : les 2 alias de stub test dans `vitest.config.js` (`react-csv`, `@dnd-kit/core` → `src/__tests__/_stubs/`) — échafaudage que ce TD prévoyait de RETIRER une fois les deps ajoutées — n'ont PAS été retirés par PR #102 (à nettoyer dans un chore test, non bloquant). |
| TD-86 | [BI/produit] `campaign_progress` et le dashboard campagne divergent sur le sens de STOPPED — deux lectures voulues | Sprint 5 (smoke item B) | OPEN (note de sémantique) | À réconcilier si un besoin d'alignement émerge | Depuis le fix smoke B, `campaign_progress` (KPI 2, `bi/definitions/campaigns.py`) compte un CampaignAccount **STOPPED comme DONE** : `value = (COMPLETED + STOPPED) / total-brut`. Règle produit (PO) : un STOPPED a été **travaillé** (contacté → no-response/opt-out), c'est un aboutissement terminal, pas une annulation. Effet : ce KPI est le **complément exact** de `campaign_coverage` (KPI 3) au niveau compte — `progress% = done/total = final/total`, `coverage% = active/total = non-final/total`, `final + non-final = total` ⟹ **`progress% = 100% − coverage%`**. **MAIS** le **dashboard campagne historique** (`app_modules/campaigns/…`, endpoint séparé) affiche toujours `completion_rate = COMPLETED / total-brut` (STOPPED ni au numérateur, ni exclu) — « % de la campagne traitée ». **Deux consommateurs, deux lectures assumées** : la Home dit « ce qu'il te reste à faire » (STOPPED = fait), le dashboard dit « % complété stricto sensu ». Non bloquant, documenté pour qu'un futur passage ne « corrige » pas l'un en croyant l'autre buggé. À réconcilier seulement si le produit veut UNE définition unique de l'avancement campagne. Front Home intouché (lit `accounts_completed`/`accounts_total`) ; meta enrichi de `accounts_completed_only` (COMPLETED strict) + `accounts_stopped` pour la décomposition. |
| TD-87 | (NEW) Convergence RepHome / ManagerHome en UN seul Home paramétré par scope | Sprint 5 (item E — paradigme unique rep/manager) | OPEN | Prochain sprint (end-state, pas une refonte en fin de sprint) | Depuis l'item E, `RepHome` et `ManagerHome` sont le **même écran** (4 tuiles → tableau filtrable → progression → quota), la seule différence étant le **scope** (`mine` vs `team`) + des dimensions en plus côté manager (colonnes/filtres Team + Owner). Les blocs sont **DÉJÀ partagés** : `TodoBlock`, `ProgressBlock`, `TodoActivityTable` (via Rep/TeamActivityTable), `QuotaBlock`/`TeamQuotaGroup`. Donc la convergence en **un seul composant Home paramétré par `scope`** (les blocs prennent `scope` + les colonnes Owner/Team en option) est du **câblage, PAS une réécriture** — deux composants qui divergent aujourd'hui uniquement par leurs hooks de données et l'état de filtre. Différé volontairement (option (a) de l'audit E) : le faire en fin de sprint 5 = refonte à risque ; l'objectif produit (« même écran, scope élargi ») est déjà atteint via l'alignement structurel (option (b)). À collapser au prochain passage Home. **Le bloc DC côté manager reste TD-84** (Option 3 : STALLED équipe en tête) — non dupliqué ici. |
| TD-88 | [Test/infra] Vitest : un run mono-fichier peut échouer la résolution d'alias (`sections/…`) là où le run répertoire passe | Sprint 5 (série smoke, observé plusieurs fois) | OPEN (note d'ergonomie) | Chore infra test si ça gêne | En lançant `vitest run <un-seul-fichier>` certains tests Home ont échoué avec `Cannot find package 'sections/home/…'` (résolution d'alias non appliquée), ALORS QUE le même test passe dans le run répertoire (`vitest run src/__tests__/views/home/`) et le run complet. La résolution des alias de `vitest.config.js` se comporte différemment selon le graphe chargé (mono-fichier vs projet). **Conséquence** : un changement sain peut *paraître* cassé si on ne teste qu'un fichier isolé — piège de diagnostic. **Contournement** : la source de vérité est le run répertoire/complet ; ne pas conclure « imports cassés » sur un run mono-fichier. À investiguer (deps optimizer / ordre de chargement de la config) si l'ergonomie devient bloquante. Non bloquant : la suite complète (462) est verte. **ADDENDUM (item 3.3)** : variante voisine — lancer `npx vitest` depuis la **racine du repo** (`/home/user/SalesCommands`) au lieu de `frontend/` charge **AUCUNE** config (ni alias ni environnement jsdom → « Cannot find package 'sections/…' » + « document is not defined » sur TOUS les fichiers). Après un redémarrage de container le cwd peut dériver vers la racine (les commandes `git` s'y font). **Règle** : toujours `cd frontend` avant `npx vitest`. |
| TD-89 | **[PRIORITAIRE — régression]** Les activités de séquence de campagne ne glissent plus dans le todo du jour → elles disparaissent silencieusement | Sprint 5 (smoke test) | **OPEN — PRIORITAIRE** | **Candidat ouverture du prochain sprint** (comme TD-65/66 au S3, TD-76 au S5) — le PO le veut corrigé avant la fin des sprints | **Contexte produit** : une campagne est une séquence d'activités RELATIVES (ex. call J0 → email J+2 → call J+2, où l'offset court depuis la date de la dernière activité faite). **Comportement ATTENDU (existait avant)** : quand la prochaine activité de la séquence arrive à échéance, elle apparaît dans le « todo today » (Home + campagne) ; si le rep ne la fait pas ce jour-là, sa date SE MET À JOUR pour qu'elle reste dans le todo today jusqu'à ce qu'elle soit faite. **Comportement ACTUEL (régression)** : la date reste FIGÉE à sa valeur initiale → l'activité sort du « today » dès le lendemain → ET comme il n'y a PAS de bucket overdue côté campagne, elle **DISPARAÎT complètement** de la vue du rep. Il ne la rate pas par négligence : le produit la lui CACHE. **Impact** : perte de travail commercial SILENCIEUSE, au cœur du produit (organiser + inciter à agir + ne rien laisser filer). **Deux pistes à investiguer (NE PAS corriger dans ce sprint)** : (1) le mécanisme de glissement de date a été cassé par un changement — c'est une régression, la logique existait ; chercher côté `activity_sequence_service` / signaux de campagne, identifier le commit fautif si possible. (2) « Pas d'overdue en campagne » est peut-être le vrai fond : le todo Home a un bucket overdue ; si les activités de campagne y remontaient, elles ne disparaîtraient pas même sans le glissement. **À arbitrer** : réparer le glissement, exposer l'overdue campagne, ou les deux ? |
| TD-90 | [Perf/BI] `DecisionCycleListSerializer` — N+1 préexistant sur `steps_count` / `validated_steps_count` (une requête PAR CYCLE) | Sprint 5 (item 3.1, découvert en testant le no-N+1 owner/team) | RESOLVED | S7c (sous-étape 2) | `DecisionCycle.steps_count` et `validated_steps_count` sont des `@property` MODÈLE qui font `self.steps.count()` (et pour validated, `list(self.steps.prefetch_related('activities').all())` + dérivation) → **une requête par cycle** quand la liste les sérialise. `.count()` ignore le prefetch `steps` du `get_queryset`, d'où le N+1. **Invisible aujourd'hui** : les listes DC sont courtes (par compte, ou le bloc Home cappé). **Deviendra un coût réel avec la VUE DC DÉDIÉE** (page complète, potentiellement beaucoup de cycles — voir le (NEW) vue DC au chore de clôture). **Fix** : annoter `steps_count=Count('steps')` et `validated_steps_count=Count('steps', filter=<validated>)` sur le queryset de liste plutôt que via des properties (ou dériver en bulk comme `by_account`). **Découvert** en testant le no-N+1 des champs `owner`/`team` ajoutés (item 3.1) : le test total-budget montrait +1 requête/cycle → investigation → cause = ces properties, sans rapport avec owner/team (qui, eux, viennent du `select_related`, zéro requête). Ne bloque rien aujourd'hui. **MAJ (audit S7c)** : la « voie A » décidée pour S7c (dérivation d'état en SQL, source unique — voir ROADMAP S7c) fera tomber d'elle-même la MOITIÉ de ce N+1 : `validated_steps_count` fait ~2 requêtes par cycle parce qu'il appelle `derive_bulk` en Python ; une fois la dérivation annotée en SQL, cette moitié disparaît. `steps_count` (le `.count()`) restait à annoter séparément. **RÉSOLU (S7c sous-étape 2)** : les DEUX compteurs passent désormais par annotation dans le serializer de liste ; un test à `assertNumQueries` prouve que le compte de requêtes reste CONSTANT avec le nombre de cycles. (Rappel : la voie A / dérivation SQL avait déjà fait tomber la moitié du N+1 — `validated_steps_count` n'appelle plus `derive_bulk` par cycle.) |
| TD-91 | (NEW) VUE DC DÉDIÉE — page complète des decision cycles, en remplacement de l'Action Center | Sprint 5 (contexte PO, item 3/4 de la série smoke) | OPEN | Sprint dédié (cadré après le merge) | **Vision produit** : une **page complète** listant les DC, accessible depuis le menu **Go-to-Market**, en **REMPLACEMENT de l'Action Center** (qui n'est plus d'actualité). **Filtres** : **Mine / Team / All** (le même paradigme que territoires & campagnes — `owner_scope`) + **date de fin**, **statut**, **avancement**, **financier** (montant deal / pipeline). **Usage SDR** : la vue leur sert AUSSI — pour un SDR, **activité + DC = un lead** (organiser son travail par deal, pas seulement par activité). **Impact Home** : une fois cette page en place, le bloc DC de la Home se **limiterait à ~10 DC triés par urgence / date de fin**, avec un **« See all »** redirigeant vers la vue (pré-filtrée `dc=actif`). **Dépendance** : **TD-90** (N+1 `steps_count`/`validated_steps_count` sur la liste) devra être traité **AVEC cette vue** — une page à fort volume de cycles rendra le coût réel. C'est un chantier de sprint, pas un item ; cadrer la maquette + les filtres avec le PO d'abord. |
| TD-92 | (NEW) Exposer le bouton FILTRE aussi aux non-managers (rep/SDR) quand des filtres les concerneront | Sprint 5 (contexte, item E — le filtre est aujourd'hui manager-only) | OPEN | Sprint Filtres & Recherche | Aujourd'hui le panneau de filtres (team + owner) et son bouton ne s'affichent que côté MANAGER (`ManagerHome` / `TeamTodoFilterPanel`), parce que les dimensions actuelles (équipe, personne) n'ont de sens que pour lui. Quand on ajoutera des filtres **pertinents pour les non-managers** (dates, statut, fenêtre autre que les tuiles, contexte…), le **bouton filtre devra être exposé au rep/SDR** sur leur Home. À traiter au **sprint Filtres & Recherche**, avec le même composant de panneau (paramétré par les dimensions disponibles selon le rôle). Non bloquant aujourd'hui : le rep n'a pas encore de filtre à exposer. |
| TD-93 | [Archi/décision] Celery envisagé pour le recalcul BI (couverture territoire) — ÉCARTÉ au profit d'un TTL lazy à la lecture | Sprint 5 (audit échelle Home manager) | RESOLVED (décision) | — | **Décision (PO)** : ne PAS introduire Celery pour le recalcul des agrégats BI. **Pourquoi** : (1) Celery = infra lourde (broker + workers permanents + beat + monitoring + déploiement), disproportionnée pour un recalcul toutes les ~2h ; (2) ça casse la décision produit tenue depuis le S2 (« polling, pas push, pas de queue ») et créerait deux mondes (synchrone partout / async pour la BI) ; (3) inutile ici — la vue n'est pas consultée plusieurs fois par jour. **Solution retenue** : **snapshot avec TTL lazy À LA LECTURE (2h)** — la couverture (num/den par territoire) est stockée ; à la lecture, si le snapshot a >2h il est recalculé, sinon lu ; le premier lecteur après péremption paie UNE requête lourde (pas 200), les suivants lisent le stockage pendant 2h. Zéro infra nouvelle. **À réévaluer** si un vrai besoin d'async apparaît : calculs longs (minutes), retries, planification stricte indépendante du trafic. |
| TD-83 | [Front/UI] Deux tables de couleurs contradictoires pour le statut `STALLED` d'un decision cycle | Sprint 5 (item 4d — bloc DC rep) | OPEN | Sprint d'homogénéisation UI | `frontend/src/api/accounts/decisionCycles.js` expose DEUX mappings couleur pour le même vocabulaire de statut : `CYCLE_STATUS_COLORS.STALLED = 'warning'` (orange, ligne 205, statut *dérivé au niveau cycle*) vs `STATUS_COLORS.STALLED = 'error'` (rouge, ligne 68, statut *au niveau step*). Le même mot « Stalled » s'affiche donc orange ou rouge selon le composant qui l'importe — incohérence visuelle. **Décision produit (item 4d)** : STALLED = rouge (`error`), le levier, cohérent avec l'overdue-rouge des tuiles todo et de la Home Manager. **En attendant l'homogénéisation** : `DecisionCyclesBlock.jsx` override LOCALEMENT (`const STATUS_COLOR = { ...CYCLE_STATUS_COLORS, STALLED: 'error' }`) plutôt que muter la table partagée `CYCLE_STATUS_COLORS` (consommée ailleurs → blast radius). Son test épingle `.MuiChip-colorError` ET `NOT .MuiChip-colorWarning`, donc un futur « nettoyage » vers la table partagée fait tomber le test. **À réconcilier** : choisir UNE source de vérité couleur-par-statut (probablement `error` pour STALLED partout), retirer l'override local une fois la table partagée alignée. |
| TD-84 | (NEW) Bloc « Decision cycles » côté MANAGER — non construit | Sprint 5 (item 4d — arbitrage e) | **RESOLVED (Sprint 5, item 3.3 de la série smoke)** | Livré | **RÉSOLU** : le bloc DC manager est livré. `ManagerHome` réutilise `DecisionCyclesBlock` en scope équipe (`owner_scope=team&outcome__isnull=true` → liste, `dc_cycle_state` scope=`team` dans le batch existant), avec la prop `showOwner` qui ajoute la ligne **Owner · Team** (« qui porte ce deal »). Visibilité data-driven (≥1 cycle ouvert d'équipe → bloc, sinon rien ET aucune requête tirée). Le serializer de liste expose désormais `owner`/`owner_email`/`team` (item 3.1, `owner__team` en select_related). Note : l'idée initiale « STALLED en tête » n'a PAS été implémentée comme tri dédié — le bloc liste les cycles ouverts avec le statut dérivé (STALLED en rouge via l'override local, cf. TD-83) ; un tri/priorisation STALLED explicite pourra venir avec la vue DC dédiée (voir TD-91). |
| TD-85 | [Front] `AccountActivitiesTab` — barre de recherche visible mais jamais câblée au hook (search mort) | Sprint 5 (item 4d, en auditant les patterns de liste) | OPEN | Sprint Filtres & Recherche | `frontend/src/sections/accounts/activities/AccountActivitiesTab.jsx` tient un état `search` (`useState('')` l.56, `setSearch` via `handleSearchChange` l.116) et branche `onSearchChange={handleSearchChange}` sur la table (l.217) — MAIS l'appel `useGetActivitiesByAccount(accountId, { page, pageSize, ordering, filters: {} })` (≈l.94-99) **ne passe jamais `search`**. Conséquence : la barre de recherche capture la saisie, remet `page=1`, mais **ne filtre rien** côté serveur (le hook supporte `search` — cf. `TerritoryActivitiesTab` qui, lui, le passe l.125). Préexistant, hors scope de l'item 4d. **Fix** : ajouter `search` (débounced) à l'objet options passé au hook, comme les autres tabs. Vérifier au passage les autres consommateurs de tables par-compte pour le même oubli. |
| TD-81 | [Archi] Le « filtre équipe » du manager est servi par le endpoint BI team-scopé, PAS par un filtre sur la liste activités | Sprint 5 (I7c) | OPEN (note d'archi) | Sprint Filtres & Recherche — **règle à respecter** | Le besoin manager (narrower le todo par équipe/personne) est servi par `/bi/todo/team/` (`TeamTodoListView`, population `todo_team_by_owner` = `TODO_STATUSES` + `apply_role_scope('activities','team')`, filtre `team`→`get_all_descendant_team_ids`→`get_team_member_ids`→`owner_id__in` + filtre `owner`), **PAS** par un filtre `team` sur `ActivityFilter` / la liste activités. **POURQUOI** : `activities read = client` pour TOUS les tiers (décision Sprint 3, broad-read) → un filtre `team` posé sur la liste client-scopée ne peut PAS faire tenir l'invariant hiérarchique « manager EMEA filtrant Sales US (hors hiérarchie) → ZÉRO » : la liste voit déjà tout le tenant, le filtre ne ferait que masquer, sans frontière réelle. Le endpoint BI le tient PAR CONSTRUCTION : `apply_role_scope('team')` réduit d'abord à la hiérarchie managée, puis le filtre `team` intersecte → hors-hiérarchie = ensemble vide (test NÉGATIF `test_team_todo_list.py`). **RÈGLE pour le sprint Filtres & Recherche** : si un filtre équipe est un jour ajouté à la liste activités, il doit être un *narrowing UX AU-DESSUS* du role scope, JAMAIS une frontière de sécurité. C'est la Proposition B (filtre `team` sur la liste) qui avait été approuvée puis INVALIDÉE en cours d'I7c précisément pour cette raison — cette note existe pour qu'un futur passage ne reparte pas dessus. |
| TD-94 | [Archi/BI] Couverture territoire agrégée par équipe — snapshot lazy read-TTL 2h (implémentation de la décision TD-93) | Sprint 5 (audit échelle Home manager, câblage territoires) | RESOLVED (livré) | — | **Implémentation** de la décision TD-93 (Celery écarté → snapshot lazy). Les territoires n'ont **AUCUNE appartenance stockée** (chaque `filter_definition` est un WHERE dynamique différent) → l'agrégat par équipe ne peut PAS être un `GROUP BY` sur une table de liaison comme les campagnes. On **matérialise** donc num/den par territoire, puis on `GROUP BY owner__team`. **4 colonnes nullables sur `Territory`** (migration `0003`) : `coverage_numerator`/`coverage_denominator`/`coverage_period_key`/`coverage_computed_at` (null = jamais calculé = périmé). **Cœur partagé** `territories/services/coverage.py::compute_territory_coverage_counts` — la MÊME définition (num/den, 2 requêtes) consommée par le KPI live par-territoire ET le snapshot, ils ne peuvent pas diverger (iso prouvé : le KPI par-territoire passe inchangé, `assertNumQueries(3)` tenu). **Service de refresh** `territories/services/snapshot.py` : périmé = `computed_at` NULL / `> TTL 2h` / `period_key != courant` ; refresh NULL/plus-vieux d'abord, plafonné au **budget K=50** par lecture (garde-fou burst), `FOR UPDATE OF self SKIP LOCKED` (le second lecteur saute la ligne verrouillée et lit le périmé), persist via `bulk_update`. **`period_key`** (année fiscale) : mismatch = recalcul forcé → le bug « FY25 lu en FY26 » **impossible par construction** (bascule FY = recompute de masse borné par le budget). **KPI** `bi/definitions/territory.py::territory_progress_by_team` (BREAKDOWN, `allowed_scopes=('team','client')`) : scope via `apply_role_scope('territories')`, refresh borné, `GROUP BY owner__team` avec `Sum(coverage_numerator)/Sum(coverage_denominator)` filtré sur `coverage_period_key==courant`. **Helpers d'agrégat partagés** extraits dans `bi/definitions/aggregates.py` (`pct`/`team_labels`/`person_labels`/`managed_team_ids`), consommés par campagnes ET territoires (iso campagnes prouvé). **Un seul owner par territoire** (pas d'executor) → un seul bucket, **`global == Σ lignes`** (contraire aux campagnes) → PAS de mention « counts once » côté front (ce serait faux). **Cache `('territories',)` seulement** : le tag est certes pollué par les écritures compte/activité (via les `invalidation_sources` du KPI live `territory_coverage`), donc le cache 30s de l'agrégat est court — **sans gravité** : la partie chère (les counts par territoire) est protégée par le TTL 2h du snapshot, indépendant des tags, et le recompute de l'agrégat coûte ~2 requêtes (SELECT de refresh vide + un GROUP BY). Un tag dédié gagnerait 30s de cache sur une opération bon marché → pas rentable. **Anti-explosion prouvée** : `assertNumQueries` constant de 1 à N territoires quand les snapshots sont frais. **À réévaluer (async)** si un vrai besoin apparaît : calculs longs (minutes), retries, planification stricte indépendante du trafic. Commits `0f32e6b`/`5b8dbe3`/`cdd864b`/`f499413`. |
| TD-95 | [Produit/modèle] Les `filter_definition` de territoire ne sont PAS auto-scopés à l'owner → double-comptage inter-territoires **intrinsèque au modèle** | Sprint 5 (découvert en construisant l'agrégat territoire par équipe) | OPEN (question produit) | Sprint Vues de travail (vue Territoires) | Le `filter_definition` d'un territoire matche des comptes **tenant-wide**, pas les comptes de son owner : `{classification: SMB}` matche **TOUS** les comptes SMB du tenant. Deux territoires au même filtre couvrent donc **exactement les mêmes comptes**, quels que soient leurs owners → le **double-comptage inter-territoires est INTRINSÈQUE AU MODÈLE**, pas un artefact de l'agrégat (deux reps peuvent « couvrir » le même compte sans le savoir). Concrètement, l'agrégat par équipe somme num/den PAR territoire : un compte dans deux territoires d'une équipe compte **deux fois** dans le total d'équipe — **exactement comme aujourd'hui** (chaque `territory_coverage` par-territoire le compte déjà indépendamment), donc **pas une régression** (prouvé par `test_account_in_two_territories_counts_twice_non_regression`). En pratique les filtres incluent sans doute une géographie qui partitionne, mais **rien ne l'impose**. **Question produit à trancher** : les territoires doivent-ils être disjoints (partition) ou peuvent-ils se recouvrir ? Découvert en construisant l'agrégat par équipe (les fixtures à filtre identique se recouvraient). À traiter au **sprint Vues de travail (vue Territoires)**. |
| TD-96 | [Front/produit] Drill-down agrégat → vue filtrée par équipe non câblé (les lignes d'équipe des blocs manager ne sont pas cliquables) | Sprint 5 (smoke — audit avant câblage) | OPEN | Sprint Vues de travail | **MAJ (audit S7c/S7d) — entrée À MOITIÉ PÉRIMÉE.** (1) Le filtre backend par équipe nommée EXISTE déjà, contrairement à ce qu'affirme le texte d'origine ci-dessous : `team=<id>` sur `CampaignFilterSet` (`campaign_views.py:77,90-95`, `Q(owner__team_id) \| Q(executor__team_id)`) et sur `TerritoryFilterSet` (`territories/views/views.py:49,60-63`, `owner__team_id` seul), équipe EXACTE sans descente hiérarchique. Ne manque que la LECTURE d'URL côté vue. (2) Correction sur le hook : les listes campagnes et territoires utilisent `useCampaignListFilters` / `useTerritoryListFilters` (seed par tier, SANS localStorage), PAS `useOwnerScope` ; l'absence de lecture d'URL, elle, est CONFIRMÉE. (3) Risque à vérifier SI un drill-down par équipe est un jour construit : le filtre campagne matche owner OU executor, alors que le KPI `campaign_progress_by_team` groupe sur `owner__team` — un lien pourrait ouvrir une liste contenant PLUS de campagnes que la ligne n'en comptait. (4) S7d a rendu ce drill-down INUTILE : décision PO, « See all » seulement, pas de nom d'équipe cliquable côté manager. — _Texte d'origine (partiellement rectifié ci-dessus), conservé pour historique :_ Les lignes d'équipe des blocs **Team campaigns** / **Team territories** (Home manager) **devraient** être cliquables vers la vue correspondante **filtrée sur cette équipe** (ex. ligne « EMEA 62 % » du bloc campagnes → vue campagnes filtrée team=EMEA). **Non câblé** parce que les vues ne le permettent pas aujourd'hui : `views/campaigns/list.jsx` et `views/territories/list.jsx` pilotent leur scope via `useOwnerScope` qui lit le **localStorage** (+ défaut rôle), **jamais l'URL** ; les seuls query params gérés sont `action`/`filter_type`/`filter_classification`/`filter_owner` (**pas** `team`, **pas** `owner_scope`), et `owner_scope=team` montre **TOUTE la hiérarchie** du manager, pas une équipe nommée. Un lien mènerait donc à une liste **non filtrée** (ou au scope mémorisé en localStorage) — **pire que pas de lien** (critère PO). **Nécessite** : (1) support d'un `?team=<id>` par URL dans ces vues, (2) un filtre **backend par équipe nommée** (aujourd'hui l'API n'expose que `owner_scope` mine/team/all, pas un `team=<id>` descendant au niveau d'une équipe précise). À traiter au **sprint Vues de travail** (qui retravaille justement ces vues). En attendant, les lignes restent non-cliquables (assumé). |
| TD-97 | [Campagnes] Renumérotation des chaînes callback déjà collisionnées en base | Sprint callback (audit D3) | RESOLVED (création) / réparation stock non nécessaire (QA) | — | **Création corrigée par D3b** (`fix(campaigns): renumber sequence on callback insertion to avoid position collision`) : à l'insertion d'un callback, un `UPDATE` ensembliste décale de +1 les `sequence_position >= N` du contact avant d'insérer le callback à N → positions contiguës et uniques pour toute **nouvelle** chaîne. **Réparation du stock existant NON faite, volontairement** : les chaînes déjà collisionnées (ex. Bulma) sont des **données de test recréables** ; une migration de recalage aveugle risquerait de corriger des lignes déjà correctes. Décision PO : forward-only, pas de data migration. **Requalifié** : rien à réparer côté stock ; si un jour des données réelles collisionnées apparaissent, écrire une migration idempotente scopée aux seules paires `(campaign_contact, position)` en doublon (détection décrite dans l'audit D3). |
| TD-99 | [Campagnes/erreurs] Constante `INVALID_STATUS_TRANSITION` absente de `CampaignModuleErrorMessages` → le chemin d'erreur lui-même crashe | Sprint callback (audit transitions CampaignAccount) | OPEN | Sprint Gestion d'erreur (cohérence messages) | `campaign_account.py:122` (`_validate_transition`) fait `CampaignModuleErrorMessages.INVALID_STATUS_TRANSITION.format(current=…, new=…)`, mais **cette constante n'existe pas** dans la classe (`core/error_messages.py:292`). Quand une transition invalide survient, au lieu de la `StandardizedValidationError` attendue on lève un **`AttributeError`** (le chemin d'erreur crashe). La classe expose bien `CAMPAIGN_TRANSITION_INVALID` (l.302) mais avec des placeholders **différents** (`{from_state}`/`{to_state}`, pas `current`/`new`) → l'appelant **déroge à la convention** de `core/error_messages.py` (nom + kwargs qui ne correspondent à aucun membre réel). **Fix** : soit ajouter `INVALID_STATUS_TRANSITION = _("Cannot transition from '{current}' to '{new}'")`, soit rebrancher l'appelant sur `CAMPAIGN_TRANSITION_INVALID` en alignant les kwargs. Aucun test ne couvre ce chemin d'erreur aujourd'hui (d'où le crash silencieux). Aggrave TD-101 (le crash de re-demande de callback passe par ce même chemin). |
| TD-100 | [Front/UI] Cards « completed » affichaient la date **prévue** + une horloge (au lieu de la date de complétion) | Sprint callback (D6) | RESOLVED (D6) | Livré | **RÉSOLU par D6** (`fix(campaigns): show completion date and success state on completed cards`) : une card d'activité terminée affiche désormais la **date de complétion** (`completed_at`, format relatif) avec une **coche verte** (`CheckCircleOutlined`, `success.main` — toujours vert, l'issue est portée par le chip d'outcome, pas la coche), au lieu de la `scheduled_date` + horloge (`ClockCircleOutlined`) d'une card à venir. Bug adjacent corrigé au passage (`Bug 2`, commit `56c8848`) : la card terminée portait à tort le chip « Callback pending » (le garde `isCallbackPending` manquait `!isCompleted`). Tests dans `PlaylistActivityCard.test.jsx`. |
| TD-101 | [Campagnes] Re-demander un callback sur un contact déjà `CALLBACK_PENDING` crashe (transition `PENDING→PENDING` interdite) au lieu de mettre à jour la date | Sprint callback (découvert en écrivant `test_fresh_callback_requested_stays_pending`) | OPEN | À traiter | `request_callback` fait `IN_PROGRESS → CALLBACK_PENDING` via `_transition_to`. Si le contact est **déjà** `CALLBACK_PENDING`, la transition `CALLBACK_PENDING → CALLBACK_PENDING` **n'est pas dans la table** autorisée → elle est refusée. Comportement voulu : re-demander un callback devrait simplement **mettre à jour `callback_date`** (déplacer le rappel), pas échouer. Aggravé par **TD-99** : le refus passe par le chemin d'erreur cassé (`INVALID_STATUS_TRANSITION` absent) → **`AttributeError`** au lieu d'un message propre. **Fix** : autoriser `CALLBACK_PENDING → CALLBACK_PENDING` comme mise à jour idempotente de la date (analogue au no-op terminal→terminal déjà présent dans `_transition_to`), et corriger TD-99 en même temps. Contournement actuel : un contact déjà en attente ne peut pas voir son callback re-planifié via ce chemin (le test isole explicitement le cas comme hors scope). |
| TD-102 | [Produit/todo] Un step `ON_HOLD` d'une campagne en pause peut apparaître dans l'« upcoming » du todo | Sprint callback (note, à arbitrer) | OPEN (question produit) | À trancher avec le PO | Observation : un step `ON_HOLD` (campagne mise en pause) remonte dans la fenêtre « upcoming » du todo lorsque la `planned_end_date` de la campagne est proche (≈ moins de 4 semaines), via la logique de fenêtre du todo. **Question produit** : une campagne **en pause** doit-elle rester **visible** dans le todo du rep (rappel qu'elle existe / à reprendre) ou être **masquée** tant qu'elle est en pause ? Rappel de la contrainte de sprint : **jamais de suppression/grisage** — donc si on masque, c'est un filtre de fenêtre todo, pas un grisage de l'activité. Non tranché : dépend de si « en pause » = « invisible » ou « visible mais différée » côté produit. À cadrer avant d'y toucher. |
| TD-103 | [Env/tests] Reproductibilité des suites : sessions Postgres résiduelles + pooler pgBouncer | Sprint callback (mise en place de l'env de test jetable) | OPEN (note d'exploitation) | — | **`DuplicateDatabase` / « 1 other session is using the database »** au relancement des suites : une session psql/pytest précédente tient encore la `test_*` → **terminer les sessions sur `test_postgres`** (fermer les shells psql, `--reuse-db` pour éviter le drop/recreate, ou `pg_terminate_backend` sur la base de test) avant de relancer avec `--create-db`. **Les tests exigent Postgres en DIRECT sur le port 5432** (constructions `CREATE DATABASE`, `SET`, extensions) — **jamais le pooler pgBouncer 6543** (mode transaction/statement → `CREATE DATABASE` et commandes de session cassent). `DATABASE_URL` de test doit pointer 5432 direct. Env de test jetable (worktree détaché + Postgres local + Redis local `REDIS_URL` configuré pour les tests BI de cache) documenté ici pour le prochain passage. |
| TD-104 | [Bulk/permissions] Edge « protected item hors scope » dans le bulk delete | Sprint S7b commit 4 (alignement Territory/Campaign bulk-delete) | OPEN (light, non bloquant) | Sprint UI (audit bulk ops) | Un item protégé (Territory `is_system` / Campaign TARGETED) n'atteint le chemin gracieux skip-and-report QUE s'il est dans le scope de l'appelant. Hors scope, `get_objects_for_bulk` lève un 403 au niveau requête AVANT que la branche « protected item » ne s'exécute, avortant tout le bulk. Actuellement INATTEIGNABLE depuis l'UI (les deux vues liste excluent les items protégés de la sélection) — purement théorique, ne se manifeste que si la garde de sélection frontend est contournée. |
| TD-105 | [Permissions/tests] Le garde-fou 403 « scope au-delà du tier » n'a AUCUNE couverture end-to-end vivante | Sprint S7b (audit permissions, post-`30bc820`) | OPEN (light, non bloquant) | Réévaluer au sprint Admin & Objectifs (activation d'un module tier-borné) | Le mécanisme « demander un scope au-delà de son tier → 403 avant compute » existe dans le code (`scope_within` + la branche 403 de la vue) mais AUCUN module ENABLED ne le déclenche end-to-end depuis que `30bc820` a passé `teams.read` à `client` pour tous les tiers. Les trois modules encore tier-bornés (`sales_quotas` / `sales_plans` / `sales_milestones`) sont désactivés dans `config.MODULES` → fail-open vers `client` → pas de 403. Les tests bi l'exercent via un monkeypatch activant `sales_quotas` en test (commit `189298a`). Réévaluer quand un module tier-borné sera réellement activé (probablement Admin & Objectifs / quotas), moment où le garde-fou retrouvera une couverture end-to-end naturelle. Lié à l'audit fail-open du durcissement sécurité (ROADMAP G1). |
| TD-106 | [Campagnes/UI] Campagne annulée sans timestamp | Sprint S7b 5c (smoke cartes) | OPEN (light, non bloquant) | Commit futur (stamp `cancelled_at`) | `CampaignStatus.CANCELLED` ne pose aucune date dans `Campaign.transition_to`, donc une carte de campagne annulée peut n'afficher aucune date (la carte montre un « Cancelled » nu). Un commit futur devra stamper `cancelled_at`. Léger, non bloquant. |
| TD-107 | [Permissions] Un manager ne peut pas exécuter les transitions de cycle de vie sur les campagnes de son équipe (403) | Sprint S7b (audit permissions) | OPEN | Passe « scellage du modèle de permissions » (ROADMAP G1) | Les cinq actions start/pause/resume/complete/cancel portent une `action_policy` plate `{'crud':'update','scope':'mine'}` (`campaign_views.py:139-144`) qui contourne le registry tier-aware → un manager voit l'objet d'un AE de son équipe sortir du scope et reçoit un 403. Détail complet dans la section permissions de ROADMAP G1 (couche `action_policies` plate vs registry). Gravité : trou fonctionnel AVEC contournement (l'édition PATCH normalise en `update` → team → autorisé) ; pré-existe le sprint permissions (commit `85ec926`, 3 juin 2026, jamais fonctionnel) ; à corriger DANS la passe modèle de permissions, pas au coup par coup. |
| TD-108 | [Front/pattern] Slot `toolbarActions` sur `ReusableTable` | Sprint cibles (commit 1, toolbar onglet cibles) | PATTERN — à réutiliser | — | Nouveau prop node optionnel rendant des contrôles supplémentaires dans la toolbar de la table (miroir du prop existant `advancedFilterPanel` ; défaut `null` → les treize autres appelants intacts). Pas une dette : à réutiliser pour toute future table plutôt que de reconstruire une barre d'actions séparée au-dessus de la table — l'erreur que ce slot a corrigée. |
| TD-109 | [BI/DRF] Un `ordering` par défaut sur un viewset s'applique à TOUTE action passant `filter_queryset`, pas seulement `list` | Sprint cibles (régression section campagnes de la Home) | PATTERN — à réutiliser | RÉSOLU (fold `my_campaigns` dans la branche annotée) | Le tri priorité-de-statut des campagnes était posé en config alors que l'annotation `_status_priority` n'existait que sur la branche `list` → `GET /campaigns/my-campaigns/` levait `FieldError: Cannot resolve keyword '_status_priority'` — un 400 qui cassait la section campagnes de la Home. Corrigé en foldant `my_campaigns` dans la branche annotée (ce qui a aussi réparé une dégradation pré-existante : cette branche servait `accounts_count` via un count par-ligne et `targets_total` à null). Leçon : toute annotation dont dépend l'`ordering` par défaut doit couvrir CHAQUE branche qui subit `filter_queryset` — et le test d'ordering doit taper ces endpoints, pas seulement la liste principale. |
| TD-110 | [ORM/pattern] `exclude()` sur une FK nullable est NULL-safe ; le piège est le filtre POSITIF sans branche `isnull` | Sprint cibles (filtre `active_sequence`) | PATTERN — à réutiliser | RÉSOLU (forme correcte + test) | `exclude()` de Django à travers une FK nullable émet `AND col IS NOT NULL` (les lignes orphelines survivent) ; le piège est le filtre POSITIF sans branche `isnull`, qui les fait silencieusement disparaître. Le filtre `active_sequence` est donc écrit `Q(campaign_contact__isnull=True) | Q(status__in=non_final)`, avec un test épinglant que les activités sans campaign contact sont CONSERVÉES. Forme correcte enregistrée pour qu'elle ne soit pas « simplifiée » plus tard. |
| TD-111 | [Sécurité/IA] Aucune sanitization de transcript avant envoi au SDK | Audit S7c/S7d | OPEN | Go-Live (G1/G5) | Prouvé absent d'`ai_pipelines/` (grep `sanitiz`/`anonymiz`/`redact`/`pseudonym` → seuls des commentaires renvoyant au filtre de LOGGING). Le transcript brut part verbatim au SDK ; seule protection : il n'est JAMAIS persisté (seul le SHA-256 `input_hash` l'est). Sujet Go-Live (durcissement G1 + stratégie données d'entraînement G5). |
| TD-112 | [Front/Signals] Route `/module-signals/pain-impacts/` appelée côté front, inexistante côté back (404) | Audit S7c/S7d | OPEN | Sprint PainImpact sunset | Le front appelle une route que le back n'expose pas (`api/signals/painImpacts.js:38-41`), montée dans `AddPainImpactDialog.jsx:64`, `AccountSignalsTab.jsx:53,61,486`, `SignalClusterDetailDrawer.jsx`. `PainImpact` est LEGACY, remplacé par `ImpactSignal` servi à `/impact/`. Tout create/edit/delete par ce dialog part en 404 aujourd'hui. Décision PO : à neutraliser au sprint PainImpact sunset. |
| TD-113 | [Signals] `ConstraintSignal` — identité de cluster morte | Audit S7c/S7d | OPEN | Sprint Signals | `ConstraintSignal` calcule un `canonical_key` `"constraint:…"` et pose `invalidate_cluster_tag=True` (`constraint_signal_views.py:45`), mais `'constraint'` est ABSENT de `SignalClusterType` et de `_SUPPORTED_CLUSTER_TYPES` (`signal_cluster_service.py:520-524`) → aucun cluster n'est jamais constitué ni invalidé pour ce type : l'identité de cluster est morte. |
| TD-114 | [Signals/archi] `SignalDataService` de `app_modules/signals` sans aucun caller externe | Audit S7c/S7d | OPEN | Sprint F (signals re-integration) | `SignalDataService` de `app_modules/signals` n'a AUCUN caller externe ; c'est le LEGACY `apps/signals/services` qui a des callers vivants (`apps/accounts/models/accounts.py:14,257,284,302,319,337`). Change la nature de TD-22 : reconnecter les `get_*_data` ne peut pas viser le service `app_modules` tant qu'il est mort — le service vivant est le legacy. |
| TD-115 | [Signals/imports morts] Imports vers du néant dans `historical_tracking.py` | Audit S7c/S7d | OPEN | Chore cleanup | `SignalCreationService` (fichier inexistant) et `from apps.signals.models import Signal` (modèle supprimé en migration `0003`) sont importés par `historical_tracking.py:117`. Imports vers du néant — casseraient à l'exécution du chemin qui les touche. |
| TD-116 | [API/pagination] Divergence de taille de page selon la classe de vue | Audit S7c/S7d | OPEN | Chore API | `StandardResultsSetPagination` = `page_size` 10 (`apps_shared_methods.py:35-38`) vs DRF global `PAGE_SIZE` = 20 (`settings.py:438-439`). La taille de page dépend donc de la classe de pagination de la vue — incohérence à uniformiser. |
| TD-117 | [BI/front] 8 KPI enregistrés jamais consommés par le frontend | Audit S7c/S7d | OPEN | À arbitrer | `todo_my_activities`, `campaign_coverage`, `campaign_progress_by_owner`, `dc_cycles_by_outcome`, `dc_pipeline_value`, `dc_won_value`, `leads_dc_created`, `leads_activities_created` sont enregistrés mais aucun écran ne les consomme. À arbitrer (garder / retirer). NB : `dc_pipeline_value` et `dc_won_value` sont de la MATIÈRE PREMIÈRE pour les filtres financiers de la vue DC (S7c) — ne pas retirer sans vérifier ce besoin. |
| TD-118 | [Sécurité/API] `handle_exception` expose `str(exc)` brut sur les 500 | Audit S7c/S7d | OPEN | Sprint Gestion d'erreur | `handle_exception` renvoie `str(exc)` brut dans la réponse sur les 500 (`apps_shared_methods.py:566-570`) → fuite potentielle de détails internes. Cas réel : « column decision_steps.status does not exist » + SQL Postgres complet vu par un utilisateur via le filtre produit de la liste DC (corrigé PR #92). À masquer avant exposition client — rattaché au **Sprint Gestion d'erreur** (revue BE + FR, dernier avant Go-Live). |
| TD-119 | [Permissions] `grant_superuser` déclaré deux fois dans les `action_policies` | Audit S7c/S7d | OPEN | Chore cleanup | `grant_superuser` est déclaré DEUX fois dans les `action_policies` (`user_view.py:79` et `:118`) — doublon à résoudre (la seconde écrase silencieusement la première). |
| TD-120 | [Front/UI] Pas de dialog de confirmation partagé dans le repo | Audit S7c/S7d | OPEN | Sprint UI (« tous les modals ») | Prouvé absent : les confirmations sont composées inline en primitives MUI, sans composant partagé. À traiter au sprint UI qui finit « tous les modals » — un dialog de confirmation partagé unique. |
| TD-121 | [Repo] Fichier parasite `backend/HGETALL` (0 octet) | Audit S7c/S7d | OPEN | Chore cleanup | Fichier vide `backend/HGETALL` (0 octet) — artefact accidentel (probable redirection shell). À supprimer. |
| TD-122 | [Front/note légère] `ProgressBlock.jsx` = composant ET module de primitives partagées | Audit S7d | OPEN (note légère) | Sprint d'homogénéisation UI | Depuis S7d, `ProgressBlock.jsx` est à la fois un composant ET un module de primitives partagées (`PROGRESS_TOP_N`, `RowsZone`, `SeeAll` consommés par `TeamAggregateBlock`). Acceptable (une seule implémentation de la règle), à réexaminer au sprint d'homogénéisation UI (extraire les primitives dans un module dédié). |
| TD-123 | [IA/test] Smoke sur VRAIE sortie LLM du fix `is_current` (étape courante) reporté | Audit S7c (sous-étape 4a) | OPEN | Sprint S11 (Signals UX / AI Signal) | Le fix `DecisionStep.is_current` (S7c 4a — réalignement sur l'étape courante DÉRIVÉE) est prouvé par repro rouge/verte + 66 tests IA, MAIS le smoke sur sortie LLM RÉELLE (prep call / deal health affichant la bonne étape courante) a été REPORTÉ. À exécuter au sprint qui touche les signaux/IA (S11 Signals UX), groupé avec le reste des vérifications IA. |
| TD-124 | [BI/produit] Filtre PAR MONTANT de la liste DC reporté | Audit S7c (facette non construite) | OPEN | Sprint Filtres & recherche transverses | La facette « montant » du drawer de la liste DC n'a PAS été construite en S7c : la colonne Amount lit `estimated_value` (saisi à la main), non fiable comme critère de filtre. Bloqué sur un montant fiable — dépend de la réconciliation `estimated_value` / roll-up produit (voir **TD-75**) et de la gestion des devises/unités. À construire au Sprint C, une fois la colonne Amount basculée sur la source de vérité. **DÉBLOQUÉ + REDIRIGÉ (Sprint C)** : la colonne Amount de la liste DC lit désormais `total_deal_value` (roll-up dérivé, fiable) avec la devise du tenant, et le tri de la colonne porte sur l'annotation SQL (commit `4cca2238`). Le blocage « pas de montant fiable » est donc LEVÉ ; la FACETTE de filtre reste à CONSTRUIRE — reportée au **Sprint Filtres**, pas au Sprint C (affichage seulement). Un test garde le périmètre : `test_dc_list_amount_column.py::TestNoAmountFilter` échoue si une facette montant apparaît sans décision produit. |
| TD-125 | [Méthode/ORM] Supprimer une colonne n'est PAS prouvé sûr par un `git grep` du nom de colonne | PR #92 (500 prod post-suppression `DecisionStep.status`) | PATTERN — à appliquer | Toute future suppression de colonne | Un manager custom peut injecter une colonne IMPLICITEMENT via `select_related` / `only` / `values` sur un chemin corrélé — invisible à la recherche textuelle du nom. Cas réel : la suppression de `DecisionStep.status` (S7c sous-étape 4b, migration `0019`) a laissé un lecteur latent dans le `default_manager` de `DealProduct`, déclenché SEULEMENT par la sous-requête `Exists` du filtre produit → 500 en production (corrigé PR #92). **Principe à appliquer à toute suppression de colonne** : couverture CROISÉE systématique de chaque chemin qui touche le modèle (ici : chaque facette de filtre × schéma réel), pas seulement le chemin nominal. |
| TD-126 | [Campagnes/UI] Le masquage des « completed » déborde sur les campagnes OUTBOUND | Audit tracking objectifs (PR #96) | RESOLVED | Sprint corrections cycle de vie campagne (PR #98/#99, item A) | La décision produit d'origine (un contact target « completed » sort de la liste, ses activités completed sortent de la section completed) vaut pour les campagnes TARGETED. Elle s'appliquait AUSSI, à tort, aux campagnes OUTBOUND — où TOUS les completed doivent rester visibles. **RÉSOLU (PR #98/#99, item A)** : la règle de masquage des completed est désormais CONDITIONNÉE au type de campagne — OUTBOUND montre tout, TARGETED masque les séquences finies (accordéon completed = run courant seulement, socle `sequence_run`). |
| TD-127 | [Front/libellé] Libellé « Revenue Won » trompeur → « Deals Won » | Audit tracking objectifs (PR #96) | RESOLVED | Sprint C (Produit & Finance) | Le LIBELLÉ d'affichage « Revenue Won » est trompeur ; le renommer « Deals Won » (ou « Won Value »). C'est le LIBELLÉ d'affichage, PAS le nom technique de la métrique (`REVENUE_WON` reste inchangé). À traiter au Sprint C (avec la réconciliation montant) ou au sprint UI. **RÉSOLU Sprint C (commit `4cca2238`)** : le libellé de la métrique de VALEUR est « Won Value » sur les 5 surfaces visibles — `api/campaigns/campaigns.js`, `utils/locales/en.json` (clé i18n conservée), `sections/objectives/metricLabels.js`, `campaigns/constants.py` (qui alimente `get_objective_type_display()` dans les payloads objectif) et `campaigns/config/settings.py` ; `bi/metrics/keys.py` suit pour que le vocabulaire ne dérive pas. Clé technique `REVENUE_WON` INCHANGÉE partout (9 occurrences). Migrations `campaigns/0021` + `quotas/0002` = `AlterField` de libellé uniquement. **Précision** : aucune surface n'affiche aujourd'hui un COMPTE de deals gagnés — il n'y avait donc rien à distinguer sous « Deals Won » ; si un tel compteur est ajouté plus tard, c'est ce libellé qu'il doit porter. |
| TD-128 | [Campagnes/UX] Message d'enrôlement imprécis dans le cas MIXTE | Sprint corrections cycle de vie campagne (PR #98/#99) | OPEN (mineur — cosmétique fonctionnel, VALIDÉ PO) | Amélioration UX ultérieure | Dans le cas MIXTE (contacts injoignables + déjà-actifs), les deux catégories sont comptées ENSEMBLE dans `unreachable_count` → le message ne distingue pas les deux causes. Cosmétique fonctionnel, VALIDÉ tel quel par le PO. À affiner si besoin UX ultérieur. |
| TD-129 | [Campagnes/API] Endpoints bulk contacts dormants (débranchés de l'UI) | Sprint corrections cycle de vie campagne (PR #98/#99) | OPEN (mineur) | Nettoyer ou ré-exposer selon besoin | La barre bulk contacts de l'onglet Targets a été retirée de l'UI, mais les endpoints bulk contacts BACKEND sont conservés DORMANTS (débranchés). À nettoyer ou ré-exposer selon besoin futur. |
| TD-130 | [Campagnes/modèle] Invariant « TARGETED jamais terminée » sans `CheckConstraint` DB | Mini-sprint fiabilisation (PR #100, C3) | OPEN (mineur — choix assumé) | — | L'invariant « une campagne TARGETED n'est jamais terminée » est garanti au niveau MODÈLE (C3) et aux frontières, mais PAS par un `CheckConstraint` DB. Choix assumé : le filet modèle + frontières est jugé suffisant. Noté pour traçabilité. |
| TD-131 | [Audit] Constats MOYENS/MINEURS additionnels de l'audit de consolidation campagne+territoire non détaillés ici | Audit consolidation campagne+territoire (PR #98-100) | OPEN (à reprendre) | À reprendre depuis le rapport d'audit | Les constats MOYENS non traités (dont le « Lot 3 quasi vide ») et les 4 constats MINEURS de l'audit exhaustif ne sont pas détaillés ici, faute d'accès au rapport de session (non versionné dans le repo). À reprendre depuis l'**audit de consolidation campagne+territoire** quand le rapport est disponible. |
| TD-132 | [Front/produit] Hook `useGetOrganization` inexistant — import cassé (warning build) | Audit build-health (PR #102) | OPEN (décision produit) | Décision produit (créer le hook OU retirer les routes) | `layout/ResourceGuardLayout.jsx:11` importe `useGetOrganization` depuis `api/admin/users`, où il n'existe pas ; il n'existe NULLE PART dans le repo → vaut `undefined`. Il alimente la table `RESOURCE_HOOKS_MAPPING` route→hook pour `/organizations/` (`:93`) et `/admin/organizations/` (`:94`). Le build émet un « Attempted import error » (WARNING, pas bloquant). `ResourceGuardLayout` est le layout de segment de `app/(auth)/users/[id]/` (`layout.jsx:2,7`), atteignable via la page change-password (`app/(auth)/users/[id]/change-password/page.jsx`). Créer le hook OU retirer ces routes est une DÉCISION PRODUIT, pas un fix de build. |
| TD-133 | [Front/deps] Trois paquets importés mais non déclarés, consommateurs tous orphelins | Audit build-health (PR #102) | OPEN | Trancher : déclarer ou élaguer le code mort | Ne cassent pas le build car aucune page ne les atteint. (1) `react-map-gl` (absent de `package.json`) importé par `components/third-party/map/MapMarker.jsx`, `MapControl.jsx`, `PopupStyled.jsx` — tout le dossier `map/` est orphelin. NB : contrairement à l'audit initial, `MapPopup.jsx` n'importe PAS `react-map-gl` (il importe `PopupStyled`) et n'a aucun importateur. (2) `react-number-format` (absent) importé par `components/third-party/index.js:3` et `sections/admin/users/ExpandingUserDetail.jsx` — aucun des deux n'est importé. (3) `react-timer-hook` (absent) importé par `views/maintenance/coming-soon.jsx`, orphelin — la route `/coming-soon-wip` (`app/coming-soon-wip/page.jsx`) est autonome et n'importe pas ce fichier. Choix à trancher : déclarer, ou élaguer le code mort. |
| TD-134 | [Front/code mort] Imports cassés en code non atteignable, laissés en place volontairement | Audit build-health (PR #102) | OPEN (mineur — assumé) | Chore code mort | Cibles inexistantes, dans du code orphelin, laissées telles quelles (règle : pas de suppression prouvée par grep seul). `api/cart` (n'existe pas) importé VIF par `components/cards/e-commerce/ProductCard.jsx:24` et `FloatingCart.jsx:12` (fichiers orphelins). `hooks/useUser` (n'existe pas ; seul `hooks/useUserPermissions.js` existe) importé VIF par `NavUser.jsx:50`, et en COMMENTAIRE dans `SimpleLayout/Header.jsx:34`. `sections/apps/customer/AddCustomer` (dossier absent) en COMMENTAIRE dans `DashboardLayout/index.jsx:22`. `handlerDrawerOpen` importé par `ThemeMenuLayout.jsx:16` n'est PAS un export de `hooks/useMenuState.js` — c'est un `useCallback` LOCAL (`:91`) retourné dans l'objet du hook (`:141`) ; le sous-arbre `Customization/` n'est pas branché sur le header rendu (`Header/HeaderContent/index.js` ne rend que Search/FullScreen/Notification/Profile). |
| TD-135 | [Front/DC] `DecisionStepDetail.jsx` orphelin + import relatif cassé — À ÉLUCIDER avant Sprint B | Audit build-health (PR #102) | OPEN (à élucider — module travaillé) | Sprint B (overviews) | `sections/accounts/decision-cycles/DecisionStepDetail.jsx:56` importe `./hooks/useDecisionStepEdit`, or le dossier `./hooks/` n'a JAMAIS existé à côté ; le hook existe à `src/hooks/useDecisionStepEdit.js` (jamais supprimé) et le symbole est réellement UTILISÉ dans le corps (`:134`). MAIS le composant est ORPHELIN : personne ne l'importe, et le barrel `decision-cycles/index.js:20` qui le ré-exporte n'a lui-même aucun importateur (dead). Question ouverte : pourquoi ce composant existe-t-il sans être branché ? À élucider avant le Sprint B (overviews) qui touchera ces écrans. |
| TD-136 | [Front/code mort] `UserCSVImportModalOLD.jsx` (903 lignes) sans importateur | Audit build-health (PR #102) | OPEN (mineur) | Chore code mort | `sections/admin/users/UserCSVImportModalOLD.jsx` (903 lignes) n'a AUCUN importateur ; remplacé par `UserCSVImportModal.jsx` (43 lignes, importé par `views/admin/users/list.jsx:33`). Sa casse d'import a été corrigée au sprint build-health mais le fichier n'a PAS été supprimé (règle : pas de suppression prouvée par grep seul). |
| TD-137 | [Front/nommage] `UserCSVValidation.jsx` : logique pure en `.jsx` PascalCase | Audit build-health (PR #102) | OPEN (mineur/cosmétique) | Chore nommage | `sections/admin/users/UserCSVValidation.jsx` est un module de LOGIQUE PURE (aucun JSX, n'importe pas React) mais porte l'extension `.jsx` et un nom en PascalCase, à côté de `userCSVConfig.js`. Cosmétique, mais c'est la CAUSE RACINE des deux imports cassés du sprint (casse ambiguë). NB : l'en-tête du fichier (`:1`) écrit `userCSVValidation.jsx` (minuscule) alors que le fichier sur disque est `UserCSVValidation.jsx` — le commentaire lui-même diverge de la casse réelle. |
| TD-138 | [Front/env] Reproductibilité de l'environnement frontend — `node_modules` parasite | Audit build-health (PR #102) | OPEN | Lié à G2 (Environnements + CI/CD) | Un `node_modules` PARASITE à la racine du repo (non déclaré, non versionné) a masqué des paquets manquants pendant des mois : Node remonte l'arborescence, donc `frontend/package.json` ne décrivait PAS réellement les besoins de l'app. C'est désormais le cas (TD-18 résolu, parasite sorti du projet), mais AUCUN garde-fou n'empêche la situation de revenir. À rattacher à G2 (Environnements + CI/CD). |
| TD-139 | [Front/CI] Le build seul est une garde faible — outils d'audit non versionnés | Audit build-health (PR #102) | OPEN (question ouverte, non tranchée PO) | Question à trancher à G2 | Sur les 12 défauts cartographiés par les audits, `next build` n'en a surfacé que 2 : il s'arrête au premier mur et ne parcourt que le graphe atteignable depuis les pages. Les 10 autres ont été trouvés par TROIS scripts d'analyse (divergences de casse, paquets non déclarés, noms importés non exportés) qui vivent dans `/tmp` et sont donc PERDUS. Question OUVERTE, non tranchée par le PO : les committer comme outil du repo et en faire un job CI au moment de G2. Noté comme QUESTION, pas comme décision. |
| TD-140 | [Front/ESLint] Backlog d'avertissements ESLint désormais visibles et assumés | Audit build-health (PR #102) | OPEN (mineur) | Chore ESLint | Une fois le build réparé, warnings assumés. `react-hooks/exhaustive-deps` sur `api/businessData/contacts.js`, `hooks/useActivityAllSignals.js`, `hooks/useAuth.js`, `hooks/useDCAllSignals.js`, `hooks/useMenuState.js`, `utils/csvImportLogic.js` (le nombre d'occurrences par fichier — dont « 6 lignes » sur `useMenuState` — n'a PAS été re-vérifié par un run ESLint). `import/no-anonymous-default-export` : l'audit initial annonçait 9 fichiers ; le comptage réel en trouve ≥12 non-test (`resolvers.js`, `AccountCSVValidation.jsx`, `utils/errorHandler.js`, `utils/formErrorHandler.js`, `utils/validators.js`, etc.) — écart signalé. 2 directives `eslint-disable-next-line no-console` dans `config/auth.js:21,129` (utilité à confirmer par un run ESLint). NB : NON couvert par TD-97/99/101/102/103 (dette S6 backend/campagne, sans rapport). |
| TD-141 | [Campagnes] `delete_activities_for_contact` supprime ENCORE les activités PLANNED au retrait d'un contact — non aligné sur la règle « ne plus effacer d'activités » (#104) | Docs rattrapage #104-#108 (audit `delete_activities_for_contact`) | OPEN | À aligner sur #104 (annuler+détacher, pas supprimer) | **COMPORTEMENT à revoir** (pas un test). `app_modules/campaigns/services/campaign_execution_service.py:249-266` : `delete_activities_for_contact` fait `Activity.objects.filter(campaign=…, campaign_contact=cc, status=PLANNED).delete()` puis `cc.delete()`. Appelé depuis `toggle_contact` (`campaign_account_views.py:463`) au retrait d'un contact — le commentaire de l'appelant dit « Cancel PLANNED activities » mais le code SUPPRIME. #104 (never-delete-activities) N'A PAS touché ce fichier (ses changements étaient dans `campaign_lifecycle_service` / `campaign_bulk_views` / `campaign_views` / territoires). C'est donc un QUATRIÈME chemin de fin de vie resté sur l'ancienne règle. **Gravité MOYENNE** : incohérence de règle + perte de donnée d'activité (contraire à l'objectif corpus). Fix = annuler + détacher (comme #104) plutôt que supprimer. |
| TD-142 | [Deals] Backfill des DecisionCycle orphelins `owner` null ET `created_by` null (données de test) | Docs rattrapage #106 | OPEN | UPDATE ponctuel si besoin réel | La migration préexistante `app_modules/decision_cycles/migrations/0010_decisioncycle_owner.py` (`RunPython backfill_owner_from_created_by`, `:14-22`) a DÉJÀ posé `owner=created_by` pour les cycles à `owner` null mais `created_by` non-null. Résiduel non couvert : les DC ayant `owner` ET `created_by` null (données de test sans champs d'audit). #106 (owner=créateur) ferme la création future, pas ce stock. **Gravité FAIBLE** : `UPDATE` ponctuel scopé si un besoin réel de propriété sur d'anciens DC émerge. COMPORTEMENT (donnée), pas un test. |
| TD-143 | [Comptes] `CompanyAccount.account_owner` peut être null → `team` dérivé null | Docs rattrapage #105-#107 (dérivation team) | OPEN (mineur) | Non traité | `account_owner` = FK nullable `on_delete=SET_NULL` (`app_modules/accounts/models.py:144`) ; `team`/`team_id` sont des `@property` dérivées (`:178-186`) → owner null ⇒ team null (et team_id null). Se produit sur des comptes de test créés sans owner (la suppression d'un owner-user est désormais INTERDITE via #105/#107, donc plus une source runtime). Aucun propriétaire par défaut posé. **Gravité FAIBLE** : à cadrer si un compte sans owner doit être rattrapé (COMPORTEMENT/donnée). |
| TD-144 | [Campagnes/enrôlement] Le comportement « échec EN BLOC si certains contacts n'ont ni email ni téléphone » est DÉCRIT mais NON TROUVÉ côté backend — à re-diagnostiquer | Docs rattrapage (PARTIE 4, vérif enrôlement) | OPEN (cause NON établie — à re-diagnostiquer) | Sprint enrôlement dédié | Le backend d'enrôlement (`app_modules/campaigns/views/campaign_account_views.py` : `enroll_target` `:750-1089`, `bulk_add` `:612-680`, `toggle_contact` `:414+`) fait DÉJÀ du skip-and-enroll-rest : filtre de joignabilité `Q(email__isnull=False) \| Q(phone_number__isnull=False)` puis `.exclude(email='' & phone_number='')` (`:880-937`) ; les injoignables sont COMPTÉS (`unreachable_count`, `:1067`) et remontés en WARNING non fatal (`:1082-1087`) — les joignables sont enrôlés. Le SEUL blocage est `strict=True` ET (opt-out OU AUCUN joignable restant) ; `strict` défaut `False`. Le comportement « échec en bloc sur injoignables PARTIELS » décrit n'existe donc PAS dans ce chemin backend. Si le symptôme est réellement observé, il vient d'ailleurs (traitement frontend du `warning`/`unreachable_count`, ou autre chemin) → **à RE-DIAGNOSTIQUER avant tout fix** (signalé conformément à la consigne « si tu ne trouves pas le comportement décrit, dis-le »). COMPORTEMENT à établir. |
| TD-145 | [Campagnes/UI] CTA parasite « Account stopped… » sur des activités ACTIVES | Smoke PO (séquences → E9b) | RESOLVED | S13 (branche `feat/s13-activity-objective`) | Le CTA « Account stopped — All contacts stopped — no successful outcome » s'affiche sur des activités ACTIVES (contact `IN_PROGRESS`, activité récente) dans la carte playlist. Le champ notes/`call_to_action` n'est pas réinitialisé quand le compte/contact redevient actif, ou est dérivé au mauvais niveau. À corriger au sprint enrichissement carte activity (front). **MAJ (audit sprint β — root cause prouvé, FIL B sorti du sprint carte activity, renvoyé à S13)** : le CTA affiché est `Activity.call_to_action`, un champ STOCKÉ figé à la génération de l'activité (`ActivityListSerializer` l'expose en champ brut, pas de dérivation read-time). Pour une campagne TARGETED sans CTA de step, il retombe sur `campaign_account.notes` — funnel unique `campaigns/services/campaign_execution_service.py:622-623` (`_create_activity`). Or `campaign_account.notes` a un DOUBLE USAGE : journal de statut (écrasé à chaque transition — d'où « Account stopped… » via `CampaignAccount.mark_stopped`) ET raison d'enrôlement attendue par le CTA → collision sémantique = CAUSE RACINE. Ce n'est PAS un défaut d'affichage : la carte rend fidèlement un champ empoisonné (`_create_followup` ne copie pas `call_to_action`, donc les follow-ups ne sont pas touchés). Le vrai fix relève du MODÈLE D'INTENTION (S13) : intention COMMUNE au niveau campagne pour OUTBOUND, intention PAR COMPTE pour TARGETED — pas un correctif local d'affichage. **Statut : reste OPEN**, non corrigé dans ce sprint (message trompeur encore visible sur activités TARGETED actives jusqu'à S13). **RÉSOLU (S13, branche `feat/s13-activity-objective`)** : l'objectif d'activité est désormais porté par un champ PAR CONTACT `CampaignContact.objective` (saisi à l'enrôlement, jetable par run — réinitialisé par `reactivate()`). `_create_activity` lit `campaign_contact.objective` À LA PLACE de `campaign_account.notes` (`campaigns/services/campaign_execution_service.py`) — la lecture de `campaign_account.notes` est SUPPRIMÉE de la résolution du CTA. `campaign_account.notes` reste le journal de statut, mais son texte (« Account stopped… ») ne peut plus fuiter dans le CTA d'une activité active, même sur re-chase (reactivate() nulle aussi l'objectif). Tests : `tests/campaigns/test_targeted_contact_objective.py` (dont la non-régression TD-145 : re-chase avec nouvel objectif, jamais le texte de statut). |
| TD-146 | [Permissions/archi] `get_object()` du mixin permissions écarte les annotations de queryset | InactB.2 | OPEN (piège) | À généraliser si un détail annoté est requis | Le `get_object()` du mixin de permissions refait un `model.objects.get()` SANS les annotations → toute annotation de queryset est PERDUE sur les endpoints de DÉTAIL. Contourné localement pour `is_inactive` (override `get_object` dans `CampaignViewSet`). Piège pour tout futur besoin d'annotation sur un détail : prévoir un override ou corriger le mixin à la racine. |
| TD-147 | [Perf] Régression d'emballement de requêtes / dégradation progressive → 408 | Navigation (nouveau, post-module campagne) | RESOLVED (partiel) | Sprint Timeout (5 fils) ; reste → Finalisation Home (territory, TD-153) + sprint dédié (steps, TD-154) | **RÉSOLU (partiel) — Sprint Timeout.** L'hypothèse initiale « boucle de revalidation SWR » est **INVALIDÉE par l'audit** : la cause n'était PAS une boucle de revalidation front mais le **COÛT UNITAIRE backend (N+1)** de plusieurs endpoints, dont la latence croissait avec le volume en navigation. Résolu par 5 fils mergés sur main, chacun avec repro rouge/verte CHIFFRÉE en requêtes SQL : (1) batch KPI `dc_cycle_state` (groupage `bulk_compute_fn`, 44 req/11 cycles → 3, pente 0) ; (2) objectifs de campagne (calcul groupé au point partagé `calculate_objective_values`, 3 chemins) ; (3) dashboard campagne (groupage exécuteurs + dédup COUNT, 31→14 pour 2 exécuteurs) ; (4) liste client-accounts (annotation `Count`, pente 1/ligne → 0) ; (5) sérialiseur d'activité `/module-activities/` + `/by-account/` (prefetch/select_related + Prefetch filtré + annotations, ~185 req page 20 → ~15, pente ~9/activité → 0 — LE plus gros N+1 du sprint). **Smoke PO** : 0 timeout `408` en navigation réelle post-fix ; endpoints corrigés < 1s (batch 347ms, dashboard 278ms, module-activities 301ms, playlist 272ms). RESTE (sortis du scope, sprints dédiés) : `territory_progress_by_team` (→ Finalisation Home, refonte métrique — TD-153) et `/decision_cycles/steps/` (→ sprint dédié — TD-154). |
| TD-148 | [Process/git] « Fix fantômes » : commits rapportés poussés mais absents de la branche cible | E9b | OPEN | Cause racine à comprendre | Pendant E9b, des commits rapportés comme poussés étaient ABSENTS de la branche cible (poussés sur une branche de session, ou push halluciné). Contournement EN VIGUEUR : CC pousse via `git push origin HEAD:<branche cible>` ET le PO vérifie `git log origin` AVANT smoke (le rapport de CC sur git ne fait PAS foi). Cause racine à comprendre. Purger les vieilles branches `claude/*`. |
| TD-149 | [Contacts] Unicité email/téléphone `'' vs NULL` SOLDÉE ; LinkedIn non traité | Module campagne (PR #116) | RESOLVED (email/phone) / OPEN (linkedin) | Revoir si une contrainte d'unicité LinkedIn est ajoutée | Le conflit d'unicité sur email/téléphone VIDES est SOLDÉ (PR #116 : normalisation `'' → NULL` dans `Contact.save()` + migration + `IntegrityError` gérées par le handler standard). LinkedIn n'a PAS de contrainte d'unicité aujourd'hui → non concerné. À revoir SI une contrainte d'unicité LinkedIn est un jour ajoutée (appliquer la même normalisation `'' → NULL`). |
| TD-150 | [Campagnes/séquences] Littéral « LinkedIn Message » en DESCRIPTION d'étape de séquence | Sprint β (A-4) | OPEN | À cadrer | `app_modules/sequences/outbound_sequence.py` et `app_modules/sequences/targeted_sequence.py` définissent `'description': 'LinkedIn Message'` pour l'étape LinkedIn — distinct du LABEL de type d'activité (déjà relabelé « LinkedIn » à la source en sprint β, A-4). Ces descriptions alimentent `Activity.description` (et le titre d'étape), PAS `activity_type_display` : elles ne sont donc pas couvertes par le relabel du type. À revoir si l'on veut un libellé cohérent partout. |
| TD-151 | [Front/i18n] Famille de clés de locale « activity-type-* » morte dans `en.json` | Sprint β (A-5/A-5bis) | OPEN | Chore nettoyage locales | Les clés `activity-type-*` (react-intl, `frontend/src/utils/locales/en.json`) n'ont aucun consommateur (aucun `id` statique, aucune clé construite dynamiquement). La clé `activity-type-linkedin` a été supprimée en sprint β (A-5bis, PO validé) ; le reste de la famille (`call`/`email`/`meeting`/`task`/`other`) subsiste, également sans lecteur. À nettoyer d'un bloc lors d'un chore locales. |
| TD-152 | [Front/pattern] Types d'activité en DUR dans `PlaylistActivityCard.jsx` | Sprint β (A-2) | OPEN | À cadrer (centraliser les constantes de type d'activité front) | La carte compare `activity.activity_type === "CALL" / "EMAIL" / "LINKEDIN"` en littéraux au lieu d'une constante centralisée — entorse au paradigme « constantes centralisées ». L'idiome littéral EXISTANT du fichier a été suivi en sprint β (A-2) pour cohérence locale (le fichier utilisait déjà `=== "CALL"`, `ONE_CLICK_TYPES`, etc.). À cadrer : centraliser un enum de types d'activité côté front et le réutiliser (la constante `ACTIVITY_TYPE_LABELS` existe déjà dans `api/accounts/activities.js` mais ne couvre que les libellés). |
| TD-153 | [Perf/BI] `territory_progress_by_team` lent (7,4s froid) + double-comptage des comptes multi-territoires | Sprint Timeout (audit vague 2) | PLANNED | Sprint « Finalisation Home + performance » (refonte métrique par personne) | Deux problèmes cumulés. **(1) PERF** : recalcul EN LECTURE de snapshots de couverture périmés (budget 50 snapshots/requête, 2 COUNT par snapshot, TTL 2h) → jusqu'à 7,4s à froid. **(2) MÉTRIQUE** : les comptes présents dans PLUSIEURS territoires sont comptés PLUSIEURS fois → couverture « par territoire » trompeuse. Résolution au sprint Finalisation Home : passer d'une métrique « par territoire » à « par personne » (comptes du user touchés cette période / total), le territoire redevenant un REGROUPEMENT stratégique et NON une base de calcul BI. Attention perf des requêtes BI. Branche `perf/territory-coverage-snapshot` GARDÉE (porte la repro rouge : chaud 11 req / froid 112 req) — à modifier ou supprimer au futur sprint. |
| TD-154 | [Perf] `/decision_cycles/steps/` lent (3,3s) et possiblement MORT | Sprint Timeout (audit vague 2) | RESOLVED (mort/vivant tranché) | Sprint DC-step élagage (`feat/dc-step-elagage`) | **RÉSOLU — la question « mort ou vivant » est TRANCHÉE : l'endpoint est VIVANT.** La forme cycle-scopée est consommée par les pickers d'étape (`ActivityModal`, `ActivityOverviewTab`) via `?cycle_id=` ; seule la forme non bornée (`useGetDecisionSteps`) reste sans consommateur, mais l'endpoint est partagé → **CONSERVÉ, ni supprimé ni optimisé.** L'élagage du sprint DC-step était UI (suppression de la PAGE per-step workspace), PAS perf. La LENTEUR 3,3s (N+1 par step, aucune annotation queryset) n'a donc PAS été traitée : elle reste ouverte et distincte, tracée dans **TD-179**. Rappel : un nettoyage passé a déjà retiré la colonne `DecisionStep.status`. |
| TD-155 | [Perf] Playlist campagne non bornée | Sprint Timeout (audit vague 2) | OPEN | À borner si le volume grandit (bénin aujourd'hui, < 300ms) | `CampaignExecutionService.get_playlist` charge TOUT le `PLANNED`+`ON_HOLD` de la campagne (250 pour un affichage de 20), tri de priorité en Python, `limit` appliqué APRÈS en Python (côté vue). CAS 1 (traduisible en SQL) faisable mais le tie-break `Activity.Meta.ordering` est obligatoire à reproduire ; volume borné en pratique (~500-5000, plafonné davantage par le futur cap 3 contacts/compte/campagne — voir ROADMAP mini-sprint). BÉNIN aujourd'hui. Résolution : à borner (bufferisation SQL) SI le volume grandit. Branche `perf/campaign-playlist-bounded` GARDÉE (repro rouge). |
| TD-156 | [Campagnes] Asymétries re-chase (non atteignables via UI, à durcir un jour) | Sprint Timeout (lecture zone d'ombre) | OPEN | À durcir un jour (non bloquant) | Deux asymétries de code dans le re-chase de contact, AUCUNE atteignable via l'UI actuelle : (1) l'action `mark-completed` d'un contact ne clôture PAS sa chaîne d'activités ouvertes (contrairement à `mark-stopped`) — API-only, non câblée front ; (2) la complétion `NO_ANSWER` en fin de séquence ne teste que les activités `PLANNED` et ignore les `ON_HOLD` survivantes. **PROUVÉ** : le total `PLANNED`+`ON_HOLD` est STABLE d'un re-chase à l'autre (pas d'empilement) sur tous les chemins réels. À durcir un jour (`mark-completed` devrait appeler `_cancel_chain_for_contact` ; le test `NO_ANSWER` devrait inclure `ON_HOLD`). |
| TD-157 | [Front] Doublon de query param `territory_id` sur `/company-accounts/` | Sprint Timeout (audit vague 2) | OPEN | Chore nettoyage front (assemblage d'URL) | L'URL de `/company-accounts/` reçoit `territory_id` DEUX fois, identique (ex. `...&territory_id=X&territory_id=X`). Bug d'assemblage d'URL côté front. Sale mais NON bloquant (les deux valeurs sont identiques). À nettoyer. |
| TD-158 | [Front/perf] Requêtes dupliquées en navigation + invalidation de cache trop large | Sprint Timeout (audit vague 2) | PLANNED | Sprint « Doublons de requêtes + efficacité cache » | En navigation (Network) des requêtes IDENTIQUES partent en double. Par ailleurs l'invalidation de cache est trop LARGE : chaque écriture invalide les KPIs de TOUT le tenant → cache souvent froid, coût recalculé souvent. Décisions produit déjà prises : invalidation CIBLÉE par objet (pas par tenant) + dédup des KPIs partagés ; rafraîchissement AUTO conservé (pas de refresh manuel). Commence par un audit. |
| TD-159 | ⚠️ [Perf/Cache/Scalabilité] Invalidation du cache BI TENANT-WIDE (non ciblée) — À FAIRE AVANT DÉPLOIEMENT | Audit sprint Doublons + cache (reporté, pré-launch) — résultat de l'audit annoncé en TD-158 | OPEN | Sprint Doublons + cache (reporté, pré-launch) | **⚠️ À FAIRE AVANT DÉPLOIEMENT (scalabilité multi-tenant, cible ~1000 users / ~100 tenants).** **CAUSE (prouvée)** : une seule écriture (ex. `Activity`) bumpe l'UNION des `cache_tags` de tous les KPI qui listent ce modèle en `invalidation_sources` → invalide QUASI TOUS les KPI du tenant, tous users / scopes / périodes. Pour `Activity` l'union vaut `{activities, notifications, sales_quotas, decision_cycles, territories, accounts, campaigns}`. Preuve : `build_model_tag_map` + récepteurs `post_save`/`post_delete` câblés depuis le registre (`backend/app_modules/bi/signals/cache_invalidation.py:40-89`, connectés en `BIConfig.ready()` `bi/apps.py:24-25`) ; incrément via `invalidate_tag` → `cache_client.incr` (`backend/core/cache_utils.py:166,206`). **STRUCTURE DE CLÉ** : le compteur de version est identifié par `crm:tag_version:c{client_id}:{namespace}` (`core/cache_utils.py:166`) — identité `(client_id, tag)` SEULEMENT, aucune dimension owner/objet ; la clé KPI `build_kpi_cache_key` (`bi/cache.py:61-90`) n'inclut pas non plus owner/objet (seuls `client_id`, `user_id` lecteur, scope, period, params). **CONSÉQUENCE** : cache TTL 30s (`bi/cache.py:46`) constamment INVALIDÉ dans un tenant actif → cache quasi toujours FROID → recalculs BI répétés ; c'est la cause de la variabilité froid/chaud (ex. `territory_progress_by_team` 7,4s à froid, cf. TD-153). **EFFORT NON trivial** : cibler par owner/objet exige de RESTRUCTURER le namespace de tag (`core/cache_utils.py:166`) ET le constructeur de clé (`bi/cache.py:61-90`) — pas un simple changement de paramètre. **DÉCISION PRODUIT À TRANCHER au cadrage** : granularité d'invalidation PAR FAMILLE de KPI — perso (par owner), équipe/manager (par équipe : une écriture d'un membre DOIT rafraîchir la vue manager — un ciblage par owner naïf CASSERAIT cette fraîcheur), tenant (large). Zones sensibles : KPI `scope=team` (`todo_team_windows`, `territory_progress_by_team` `backend/app_modules/bi/definitions/territory.py:200-208`), badge `unread-count`. **IMPACT SCALABILITÉ** : bénin à faible charge (perf actuelle < 1s), mais à ~1000 users / 100 tenants le cache froid permanent = recalculs BI massifs + workers web bloqués sur requêtes longues → à corriger avant montée en charge. **PRÉMISSES FAUSSES ÉCARTÉES par l'audit (note anti-régression, ne pas y revenir)** : (1) les clés SWR ne sont PAS instables — dédup active, provider SWR unique en tête (`frontend/src/app/ProviderWrapper.jsx:331`), aucune clé instable trouvée (les listes stabilisent leur URL via `buildUrlWithParams`) ; (2) les vrais doublons simultanés sont DÉJÀ dédupliqués par SWR (même clé) → rien à corriger côté front sur ce point ; (3) l'invalidation large est 100% BACKEND, pas front — côté front seul `respondToNotification` fait `revalidateByPrefix('/bi/')` (`frontend/src/api/notifications/notifications.js:191`), les écritures activité/contact/campagne n'invalident PAS `/bi/`. Doublons front réels (mineurs) tracés en TD-160/161/162. Perf froid liée : TD-153. Umbrella sprint : TD-158. |
| TD-160 | [Front/SWR] Revalidation au remontage (revalidateIfStale + dedupingInterval 2s) → même clé re-demandée à chaque revisite | Audit sprint Doublons + cache (reporté, pré-launch) | OPEN | Sprint Doublons + cache (reporté, pré-launch) — priorité FAIBLE, PAS avant launch | Config SWR globale `revalidateIfStale: true` (`frontend/src/app/ProviderWrapper.jsx:116`) + `dedupingInterval: 2000` (`:112`) → au remontage d'un composant, SWR revalide la MÊME clé dès que la donnée a plus de 2s → même endpoint re-demandé à chaque revisite de page en navigation. **Les « 2-6× » observés en Network ne sont PAS des double-montages simultanés** (ceux-là sont dédupliqués par SWR, clé identique) NI des re-renders (clés stables, aucune clé instable trouvée) — ce sont ces revalidations de remontage, plus le focus/polling des rares hooks concernés. Fix CENTRAL possible via la config SWR (throttle/dedup plus large ou `revalidateIfStale` ajusté). **Priorité FAIBLE** : gaspillage réseau, pas une douleur utilisateur (perf < 1s). **Décision produit** : niveau de fraîcheur acceptable au remontage/revisite d'une page. Re-fetch temporel assumé par conception (hors de ce TD) : `useUnreadCount` (`frontend/src/api/notifications/notifications.js:124,128` — `revalidateOnFocus:true` + `refreshInterval:60000`) et `useGetPlaylist` (`frontend/src/api/campaigns/campaigns.js:671-675`). Umbrella : TD-158. |
| TD-161 | [Front] Provider React Query MORT (QueryClientProvider sans aucun useQuery) | Audit sprint Doublons + cache (reporté, pré-launch) | OPEN | Sprint Doublons + cache (reporté, pré-launch) — cosmétique | `frontend/src/layout/DashboardLayout/index.jsx:28-37,60` monte un `QueryClientProvider` (`@tanstack/react-query`) mais il n'existe ZÉRO `useQuery`/`useMutation` dans tout `frontend/src` → 0 requête générée, provider inerte. À retirer (dépendance + provider) au sprint. Cosmétique, non bloquant. Umbrella : TD-158. |
| TD-162 | [Front] Hooks « choices » morts (aucun consommateur) | Audit sprint Doublons + cache (reporté, pré-launch) | OPEN | Sprint Doublons + cache (reporté, pré-launch) — cosmétique | Deux hooks SWR de choices n'ont AUCUN consommateur vivant dans `frontend/src` : `useGetTerritoryChoices` (`frontend/src/api/territories/territories.js:265`) et `useGetObjectiveChoices` (`frontend/src/api/campaigns/campaigns.js:861`). Code mort à retirer. Cosmétique. NB : `/company-accounts/choices/` via `useGetAccountChoices` est bien VIVANT (7 consommateurs, clé partagée stable → co-montages dédupliqués). Umbrella : TD-158. |
| TD-163 | [Frontend] `FormTerritoryEdit` — multi-select départements émet `{value: undefined}` | Sprint S13 (audit front 6b) | OPEN | Chore front (à planifier) | `sections/territories/FormTerritoryEdit.jsx` mappe `standardDepartments` via `.map(d=>({value: d.id, label: d.display_name \|\| d.name}))`, mais `/contacts/choices/` renvoie déjà des items façonnés `{value: id, label}` (backend `app_modules/contacts/views/views.py:326`) → `.id`/`.display_name` sont `undefined`, donc le filtre départements du FORMULAIRE D'ÉDITION est cassé (émet des valeurs `undefined`). `FormTerritoryAdd.jsx` passe la liste brute (`options={standardDepartments}`) correctement. Sévérité MEDIUM. Fix : passer la liste brute (mirror `FormTerritoryAdd`) ou mapper depuis les vraies clés (`value`/`label`). |
| TD-164 | [Frontend] Label/placeholder « Activity Objective » hard-codés par fichier (pas de seam i18n) | Sprint S13 (6a-6e) | OPEN | Sprint i18n (à planifier) | La clé locale `activity-call-to-action` (`frontend/src/utils/locales/en.json`) est ORPHELINE (lue nulle part) ; le label + les placeholders objectif/description sont des littéraux PAR FICHIER sur toutes les surfaces (`ActivityModal`, `ActivityOverviewTab`, `StepObjectiveMembers`, `AddToCampaignModal`, `AddTargetToCampaignModal`). Sévérité LOW. Fix : câbler react-intl ou une constante de copy partagée quand l'i18n sera traité (voir aussi TD-151, famille `activity-type-*` morte). |
| TD-165 | [Frontend] `MultiSelectFilter` propTypes `value: arrayOf(string)` alors que des ids entiers transitent | Sprint S13 (6b) | OPEN | Cosmétique | `components/filters/MultiSelectFilter.jsx` déclare `value: PropTypes.arrayOf(PropTypes.string)` et `options[].value` string, mais des ids ENTIERS `StandardDepartment` transitent (`target_department_ids` de la création campagne, formulaires territoires) → warning propType console INOFFENSIF (pré-existant côté territoires). Sévérité MINOR. Fix : élargir à `PropTypes.arrayOf(PropTypes.oneOfType([PropTypes.string, PropTypes.number]))`. |
| TD-166 | [Backend] `app_modules.tech_catalog` conservé dans `INSTALLED_APPS` en app « migrations-only » | Sprint S10 (5a — suppression catalogue) | OPEN | Squash migrations signals (post-0023) | Le paquet ne contient plus QUE `migrations/` (modèle, vues, serializers, urls, constants, admin supprimés en `58c9103`), mais l'app reste ENREGISTRÉE : cinq migrations historiques de `module_signals` (`0008`, `0009`, `0013`, `0014`, `0023`) déclarent `('tech_catalog', '0001_initial')` dans leurs `dependencies`. La désenregistrer rendrait ces dépendances irrésolvables et casserait `migrate` depuis zéro — donc TOUTE construction de base de test, pas seulement un déploiement. Éditer les migrations historiques pour retirer la dépendance corromprait tout environnement où elles sont déjà appliquées : écarté. Sévérité LOW (aucune table, aucun modèle, aucune route, aucune permission). `app_modules/tech_catalog/apps.py` documente la situation et nomme la condition de sortie. Fix : retirer de `INSTALLED_APPS` + supprimer le paquet une fois les migrations `module_signals` squashées au-delà de 0023. |
| TD-167 | [Frontend] Commentaires de filiation du catalogue produit pointant vers des fichiers supprimés | Sprint S10 (5b — suppression UI catalogue) | OPEN | Chore front (cosmétique) | Le module product-catalogue documente sa filiation de design par des commentaires citant le catalogue tech, désormais supprimé : « Mirrors TechCatalogModal » (`sections/businessData/products/ProductCatalogModal.jsx:27,53,80`), « calqué sur la table TechCatalog » et « the TechCatalog pattern » (`views/businessData/products/list.jsx:67,74`), « same shape as techCatalog list » (`views/businessData/products/list.jsx:137`), « mirrors techCatalog.js » (`api/businessData/productCatalog.js:13`). Aucun impact runtime : commentaires uniquement, zéro import (build Next vert). NON corrigés en S10 — la sous-étape 5b était périmétrée sur le catalogue tech, et réécrire trois fichiers d'un module non concerné aurait élargi le diff sans nécessité. Sévérité MINOR. Fix : reformuler ou retirer ces références en une passe. |
| TD-168 | [Currency] Devise du tenant : défaut EUR (valeur de test) + sélecteur de devise à la création du tenant NON construit | Sprint C (Produit & Finance, sous-étape 6) | OPEN | Sprint Admin Client (provisioning tenant) | Sprint C a posé la devise AU NIVEAU DU TENANT (`ClientAccount.currency`, renommé depuis `default_quota_currency`, migration `end_users/0013`) et l'a estampillée sur les payloads (KPI monétaires via `unit='currency'`, listes DC et lignes produit via `TenantCurrencySerializerMixin`). DEUX manques assumés au cadrage : (1) le **défaut du champ est `EUR`** (`end_users/models/client_account.py:52`, miroité par `DEFAULT_CURRENCY` `backend/core/currency.py:26`) — c'est une valeur de TEST, à passer à `USD` avant la mise en production ; (2) **aucun sélecteur de devise à la création du tenant** — le sprint a explicitement exclu toute UI de provisioning (décision PO), donc un tenant créé aujourd'hui hérite silencieusement du défaut. Aucune conversion, aucun taux de change : un tenant = une devise, les montants ne sont jamais sommés entre devises (voir le docstring de `core/currency.py`). Sévérité MEDIUM (unité affichée fausse pour un tenant non-EUR, pas de perte de données). Fix : basculer le défaut sur `USD` (champ + `DEFAULT_CURRENCY`, une migration `AlterField`) et exposer le choix (`core.constants.CURRENCY`) dans le parcours de création de tenant. |
| TD-169 | [Legacy] Branche `pipeline` du quota LEGACY `SalesQuota` NON fenêtrée par la période | Sprint C (Produit & Finance, sous-étape 5b) | OPEN | Nettoyage avant déploiement | `compute_quota_current_value` (`backend/app_modules/bi/quota.py:52-59`) traite `target_type='pipeline'` comme un STOCK : la valeur est agrégée sur TOUS les cycles ouverts du user, sans filtre `period_start`/`period_end`, alors que la branche `closed_won` juste en dessous fenêtre bien sur `outcome_date`. C'est la convention STOCK/FLUX du sprint (`dc_pipeline_value` est lui aussi un stock à `default_period='all'`), donc le comportement est COHÉRENT avec la BI moderne — mais il diverge de la règle « un quota a une période » portée par le modèle `end_users.SalesQuota`, qui expose `period_start`/`period_end` et laisse croire que les deux branches sont fenêtrées. Comportement ÉPINGLÉ par un test de caractérisation (`backend/tests/quotas/test_quota_attainment_amounts.py:479-` `TestLegacySalesQuotaIsNotWindowedOnPipeline`) : la divergence est documentée, pas subie. Sévérité LOW (chemin legacy, le quota moderne `quotas.Quota` n'est pas concerné). Fix : trancher la sémantique produit (stock assumé → renommer/documenter, ou flux → fenêtrer) au moment de retirer `SalesQuota`. |
| TD-170 | [Legacy] Plan de vente / milestones toujours branchés sur les tables `opportunities_*` (modèle mort) | Sprint C (Produit & Finance, sous-étape 5) | OPEN | Nettoyage avant déploiement | `end_users/services/sales_planning_service.py` calcule l'avancement des milestones (`PIPELINE_VALUE`, `REVENUE`, `OPPORTUNITIES_CREATED`…) depuis un dictionnaire `performance_data['opportunities']` (`:49,60-82`) produit par `UserPerformanceService`, dont le SQL brut lit `opportunities_opportunity` + `opportunities_opportunityfinancialsummary` (`end_users/services/user_performance_service.py:117,122,334,336`) — l'ancien modèle, PAS `decision_cycles.DecisionCycle` ni la valeur dérivée canonique (`decision_cycles/services/deal_value_sql.py`). Ce chemin est donc resté INTOUCHÉ par TD-75 : il ne lit ni `estimated_value` ni le roll-up produit, il lit un autre monde. **Second défaut sur le même chemin** : `_validate_user_access_secure` filtre `User.objects.get(..., client_id=client_id, ...)` (`user_performance_service.py:469-471`) alors que `User.client_id` est une **property**, pas un champ (`end_users/models/user.py:175`) → `FieldError` à l'appel, donc le service lève avant même d'atteindre le SQL. Découvert lors de la cartographie des agrégations montant (sous-étape 5) et REPORTÉ tel quel : le repointer aurait dépassé le périmètre du sprint. Sévérité LOW (surface morte en pratique — elle échoue avant de produire un chiffre). Fix : repointer plan de vente + milestones sur `DecisionCycle` et la valeur dérivée, ou supprimer la surface avec les tables `opportunities_*`. |
| TD-171 | [Legacy] Moteur quota/personnel LEGACY à retirer (`bi/quota.py`, `SalesQuota`, `quota_attainment`, `user_performance_service`) | Sprint C (wiring, audit de code mort) | OPEN | Nettoyage avant déploiement | Depuis le Sprint C wiring, le moteur d'objectifs MODERNE (`/quotas/quotas/` → `bi/metrics/sales_metrics.py`, périmètre déclaré dans `bi/metrics/attribution_scope.py`) est la **source unique** du chiffre personnel. Restent en place quatre vestiges de l'ancien chemin, à retirer **ensemble** : (1) **`backend/app_modules/bi/quota.py`** — un SECOND moteur personnel, avec ses propres règles ANTÉRIEURES au Sprint C (il ne connaît ni l'union `owner ∪ created_by ∪ account.account_owner`, ni la règle créateur de `DECISION_CYCLES`/`MEETINGS`) : deux moteurs, deux réponses possibles à la même question. (2) **Modèle `SalesQuota` + ses migrations + `SalesQuotaViewSet`**. (3) **KPI `quota_attainment`** — endpoint TOUJOURS VIVANT mais SANS consommateur UI depuis le retrait de `QuotaBlock`/`TeamQuotaGroup` des deux Home (`41bd456`) ; même famille que TD-117 (KPI enregistrés jamais consommés) — **ne pas dupliquer, arbitrer avec lui**. (4) **`end_users/services/user_performance_service.py`** — SQL brut, alimente les surfaces legacy « plan de vente ». **Liens** : TD-169 (branche `pipeline` de `SalesQuota` non fenêtrée — devient sans objet si le modèle disparaît) et TD-170 (milestones sur `opportunities_*`, servis par ce même `user_performance_service`). Retirer les trois d'un bloc, sinon TD-169/TD-170 seront corrigés sur du code destiné à mourir. |
| TD-172 | [Legacy] Application campagne LEGACY `apps/campaign/*` — installée, routée, avec son propre `ObjectiveType` (dont `LEADS`) | Sprint C (wiring, purge LEADS) | OPEN | Nettoyage avant déploiement | Surface produit legacy PARALLÈLE au module moderne `app_modules/campaigns` : elle est **installée et routée**, porte sa PROPRE énumération `ObjectiveType` — `LEADS` compris, alors que la métrique `LEADS` a été retirée du moteur moderne et les objectifs stockés purgés ce sprint (`quotas/0004_purge_leads_quotas`) — et ses propres analytics. Elle **n'atteint aucune UI moderne** : rien dans `frontend/src/` ne la consomme. Conséquence tant qu'elle vit : `LEADS` reste lisible dans le code et dans le schéma via ce chemin, ce qui contredit la purge et peut ré-introduire le vocabulaire par copie. À retirer dans le nettoyage legacy, **avec** TD-171 (même famille : vestiges d'un produit antérieur encore branchés). |
| TD-173 | [Frontend] Décalage d'un jour à l'affichage de `outcome_date` (`DCWorkspaceHeader.jsx:302`) | Sprint C (wiring, sous-étape 2 — puce de date de clôture) | OPEN | Chore front (cosmétique, mais visible) | `outcome_date` est un `DateField` (`decision_cycles/models.py:138`), donc sérialisé `"AAAA-MM-JJ"`. `new Date("2026-05-20")` interprète cette chaîne comme **minuit UTC** : dans tout navigateur à décalage NÉGATIF (Amériques), `format()` rend le **jour PRÉCÉDENT**. Une date de clôture affichée peut donc varier d'un jour selon le fuseau du lecteur. **Fix = `parseISO`** (lit une date calendaire LOCALE), exactement le correctif déjà appliqué ce sprint à `effective_close_date` dans le même fichier, et le motif déjà en place à `config/formatters.js:21`. **PRÉ-EXISTANT et HORS PÉRIMÈTRE du sprint** : le brief ne portait que sur la puce « Est. close » ; la ligne `outcome_date` a été délibérément laissée telle quelle plutôt que d'élargir le diff. Signalé au PO au moment de la livraison. |
| TD-174 | [Produit] `estimated_timeline_days` CONSERVÉ mais plus branché à aucune surface | Sprint C (wiring, sous-étape 2 — puce de date de clôture) | OPEN | Sprint Workspace / DC (3bis) | Le champ **reste servi** (`decision_cycles/serializers.py:394`) et n'est **volontairement PAS supprimé** : le PO le garde pour une fonctionnalité à venir « **jours restants avant la date de clôture attendue** » (nudge d'action). Ce sprint a seulement cessé de **DÉRIVER la date de clôture** de `created_at + estimated_timeline_days` — une DURÉE estimée n'est pas une date convenue, et les deux divergeaient à l'écran (éditer la date de clôture ne déplaçait pas la puce). État actuel : champ vivant, **aucun consommateur**. À **re-câbler** quand la fonctionnalité « jours restants » sera construite, sinon il redeviendra du code mort par oubli. Ne pas le retirer sans arbitrage PO. |
| TD-175 | [Vocabulaire] Renommage backend `quotas` → `objectives` (produit « objectif », module « quotas », api front « objectives » tapant `/quotas/quotas/`) | Sprint C (wiring) | OPEN | Chore de renommage dédié | Le MÊME concept porte **trois noms** selon la couche : le **produit** dit « objectif », le **module backend** s'appelle `quotas` (`app_modules/quotas`, route `/quotas/quotas/`), et l'**api frontend** s'appelle `objectives` tout en tapant `/quotas/quotas/`. Ambiguïté de vocabulaire coûteuse à la lecture — et aggravée par la coexistence du modèle LEGACY `SalesQuota` (TD-171), qui rend « quota » ambigu jusque DANS le backend : le lecteur ne sait pas si « quota » désigne l'objectif moderne ou le vestige. **Ordre suggéré** : retirer d'abord le legacy (TD-171), le renommage devient alors non ambigu. Renommage mécanique mais transverse (module, routes, api front, tests) — à faire dans un chore dédié, pas en marge d'un sprint fonctionnel. |
| TD-176 | [Données] Migration corrective pour les `source_campaign` historiquement mal posés (ancien backfill) | Sprint C (wiring, sous-étape attribution campagne) | OPEN | À ARBITRER — suivre, NE PAS faire | L'attribution campagne « born-from » repose sur `DecisionCycle.source_campaign`, désormais estampillé **à la NAISSANCE du cycle et jamais rétro-appliqué** (`f1436f4`). Un **backfill antérieur** avait re-parenté des DC **préexistants** vers des campagnes qui ne les avaient pas fait naître : sur ces lignes-là, le born-from revendique un deal que la campagne n'a pas créé. Le **code** est corrigé ; la **donnée historique** ne l'est pas. **Décision : TRACER, NE PAS FAIRE.** Les données de test sont jetables (une base reconstruite n'a pas le problème) ; une migration corrective n'est nécessaire **que si de la donnée RÉELLE est concernée** au moment du déploiement. À ré-arbitrer à l'étape « Data Structure Review » (pré-Go-Live). |
| TD-177 | [BI] Branche `new_logos` campagne de `sales_metrics` (attribution par pivot `CampaignAccount`) probablement MORTE | Sprint C (wiring, sous-étape NEW_LOGOS) | OPEN | Vérifier puis retirer si confirmé | `sales_metrics.new_logos` porte une branche d'attribution **campagne** passant par le pivot `CampaignAccount`. Or un objectif **personnel** ne peut pas être scopé à une campagne (**confirmé par le PO**) : le paramètre qui active cette branche ne serait alors jamais fourni sur ce chemin, et le code serait **inatteignable**. Le `NEW_LOGOS` campagne est de toute façon servi ailleurs depuis ce sprint, par la logique de TRANSITION + attribution born-from ∪ touched-by (`campaigns/services/campaign_dc_attribution.py::campaign_new_logos`). **NON VÉRIFIÉ — à prouver avant de retirer** : tracer les appelants réels de la branche ; si aucun ne peut passer un scope campagne, supprimer. Le retrait doit se faire **fichier par fichier avec preuve grep** (cf. TD-171 : `CampaignAccount` est mort dans un fichier et VIVANT dans un autre — un balayage global casserait le second). |
| TD-178 | [Perf] Agrégation des KPI de valeur **par campagne** — une requête par campagne, pas de `GROUP BY` mutualisé | Sprint C (wiring, sous-étape attribution campagne) | OPEN | Chore perf (si le nombre de campagnes croît) | Les KPI monétaires par campagne sont calculés **campagne par campagne** sous l'union d'attribution : N campagnes → N requêtes d'agrégation, là où un seul `GROUP BY campaign_id` suffirait. **Non bloquant au volume actuel** ; le coût est LINÉAIRE en nombre de campagnes du tenant, donc à optimiser si ce nombre grandit. **Nuance vs TD-159** (invalidation de cache BI tenant-wide, à faire avant déploiement) : les deux touchent la performance BI mais tirent en sens INVERSE — TD-159 veut **moins** d'invalidation, tandis que la correction de fraîcheur de ce sprint a délibérément **ÉLARGI** la sensibilité de la clé campagne (quatre tags pliés au lieu d'un, `CAMPAIGN_CACHE_TAGS`) pour cesser de servir un chiffre périmé. Arbitrage correction-avant-performance assumé : **à réconcilier au sprint cache pré-launch**, pas à « corriger » isolément. |
| TD-179 | [Perf] `/decision_cycles/steps/` ~3,3s non traité (N+1 par step, aucune annotation) | Sprint DC-step élagage (audit + TD-154) | OPEN | Sprint perf / cache pré-launch | Séparé de **TD-154** (mort/vivant, RESOLVED) car DISTINCT : l'endpoint est vivant mais lent, et la lenteur n'a pas été touchée par l'élagage (UI, pas perf). `DecisionStepViewSet` n'a AUCUNE annotation queryset — chaque champ dérivé coûte des requêtes PAR ligne : `derived_status` via `StepStatusDerivationService().derive` par step (`serializers.py:554-570`), `.count()`/`.exists()` par step, activités selon le prefetch (`views/views.py:1138-1147`). Cible : dériver en bulk / annoter, comme le fait déjà la forme by-account (`derive_bulk` + annotations + contexte injecté). Cadre : sprint perf ou cache pré-launch. |
| TD-180 | [Code mort] `DecisionStepDetail.jsx` LEGACY injoignable (conservé annoté) | Sprint DC-step élagage (S3.2) | OPEN | Sprint nettoyage code (POST-launch / Homogénéisation) | Composant orphelin total : son seul import est le barrel `decision-cycles/index.js`, lui-même importé NULLE PART. Depuis le sprint DC-step, c'est l'UNIQUE consommateur de `useDecisionStepEdit` (`hooks/useDecisionStepEdit.js`). Marqué en tête `// LEGACY: remplacé par StepDetailDrawer, conservé temporairement` (décision PO : garder + annoter, ne pas supprimer maintenant). À retirer avec `useDecisionStepEdit` au sprint nettoyage code. |
| TD-181 | [Code mort] `handleCycleClick` mort dans `ActivityOverviewTab.jsx` (`CycleStepSubsection`) | Sprint DC-step élagage (S2.2, constat) | OPEN | Sprint nettoyage code | Handler préexistant NON référencé en JSX (~`ActivityOverviewTab.jsx:1884`), ancien pattern `?tab=decision-cycle`. Il n'a PAS été rendu mort par l'élagage (il l'était déjà) → laissé en place, hors périmètre S2.2. S'il est un jour recâblé, viser le DC workspace (`/accounts/{id}/dc/{cycleId}?tab=timeline`), PAS la liste / l'onglet compte. |
| TD-182 | [Données] Doublon « manager notes » : `DecisionStep.manager_notes` vs modèle cycle-level `ManagerNote` | Sprint DC-step élagage (audit, ambiguïté non résolue) | OPEN | À trancher (produit) | Deux stockages coexistent : `DecisionStep.manager_notes` (TextField, `models.py:487`, exposé `serializers.py:718`) et le modèle cycle-level `ManagerNote` (`migrations/0015`, routes `urls.py:101-109`, UI `ManagerNotesThread`). Le sort du step-level (migrer / fusionner / abandonner) n'est PAS tranché. |
| TD-183 | [Données] Désaccord d'ordering des steps (4 sources divergentes) | Sprint DC-step élagage (audit) | OPEN | À documenter / unifier | `DecisionStep.Meta.ordering=['created_at']` (`models.py:528`) vs viewset `['order','created_at']` (`views/views.py:1104`) vs prefetch DC-retrieve `.order_by('order')` (`views/views.py:344`) vs re-tri front par `order` (`DecisionCycleTimeline.jsx`). Source autoritaire non documentée. |
| TD-184 | [Bug latent] `owner_scope` FieldError LATENT sur `/decision_cycles/steps/` | Sprint DC-step élagage (audit, ambiguïté) | OPEN | À corriger si `owner_scope` activé sur les steps | `DecisionStepViewSet` pose `module='decision_cycles'` et appelle `apply_owner_scope_filter`, qui résout `OWNERSHIP_MAP['decision_cycles']['owner_user']='owner'` et filtre `owner_id` SANS garde d'existence de champ (`permissions/owner_scope.py:166-172`, contrairement à `scope_filter.py:127` qui garde via `_is_valid_field`). Or `DecisionStep` n'a PAS de champ `owner` → FieldError. LATENT : aucun caller ne passe `owner_scope` à cet endpoint aujourd'hui (les 2 hooks vivants n'envoient que `?cycle_id=`). Steps owner-scopés ou non : non tranché. |
| TD-185 | [Code mort] Hook `useGetDecisionStepWithContext` inutilisé | Sprint DC-step élagage (audit) | OPEN | Sprint nettoyage code | `decisionCycles.js:567`, purpose-built pour le step workspace (SUPPRIMÉ au S3), fait 2 appels SWR (step + cycle). Sans aucun consommateur. Abandonné ou staged : à trancher ; retirer au nettoyage si abandonné. |
| TD-186 | [Front/UI] Architecture onglet Flat/Grouped à reconsidérer (bascule dans un onglet unique) | Sprint Bloc IA / Qualification (C6) | OPEN | Fin Bloc IA | La bascule **Grouped / Flat** vit dans un onglet unique (`ActivitySignalsTab` + surfaces Compte/DC). L'ergonomie de deux vues très différentes derrière un même onglet est jugée INCOHÉRENTE à l'usage : la vue plate mérite probablement son PROPRE onglet plutôt qu'un toggle. Décision d'architecture à trancher en fin de Bloc IA, une fois que Tech Stack + Objection auront rempli la vue groupée et qu'on saura ce que chaque vue porte réellement. Ne pas figer avant. **MAJ (Sprint Bloc IA / Contrainte)** : **rattaché au sprint UX Signals** (décision onglet Flat/Grouped **partagé ou non**), traité **avec** l'homogénéisation UI Activity/DC (**TD-204**) et les modaux Edit (**TD-205**). Reste **OPEN**. |
| TD-187 | [Code mort] `ScopeLevel.PERSONAL` inutilisé — valeur d'enum + branche `Objective.clean()` + usage `target_contact` à retirer | Sprint Bloc IA / Qualification (A1, audit scope) | OPEN | Chantier dédié (audit + suppression) | Le sprint a introduit `scope_level` (BUSINESS / DEPARTMENT) sur le signal ; la valeur `ScopeLevel.PERSONAL` reste déclarée dans l'enum mais **n'est produite ni consommée par aucun chemin vivant**. Traînent avec elle : la branche `personal` de `Objective.clean()` et l'usage de `target_contact` qui l'accompagnait. Retrait à faire **d'un bloc** (valeur d'enum + branche `clean()` + `target_contact` + migration de nettoyage) dans un chantier dédié — pas en marge d'un sprint fonctionnel, pour ne pas mélanger suppression d'enum et logique métier. Vérifier grep par fichier avant retrait (une valeur d'enum peut être morte à un endroit et référencée à un autre). |
| TD-188 | [Front/UI] Passe UI esthétique de fin de bloc (Qualification) reportée au Sprint UI | Sprint Bloc IA / Qualification (fin de bloc) | OPEN | Sprint UI | Homogénéisation esthétique **délibérément différée** : remplacer les bordures blanches de section par le traitement clair du projet, polish des cartes de signal, **multi-select département** dans le filtre (le backend est PRÊT — périmètre OR sur `target_department ∈ liste` ; l'UI ne propose qu'un choix unique aujourd'hui, cf. TD-163), passe UI du drawer (cluster + signal). Même famille que la dette « Sprint UI / homogénéisation » existante — **rattacher à TD-83, TD-120, TD-122, TD-30, ne pas dupliquer**. Le fond (structure, filtres, endpoint) est livré ; il ne reste que le vernis. |
| TD-189 | [Front/UI] Filtres Tech Stack + Objection — familles présentes mais VIDES (placeholders) | Sprint Bloc IA / Qualification (panneau de filtres groupés) | OPEN | Avec la partie Tech / Objection du Bloc IA | Le panneau de filtres groupés (`SignalsGroupedFilterPanel`, accordéon par famille) rend **trois** familles : Qualification (REMPLIE) + Tech Stack + Objection (**sections placeholders, sans contrôle**). Les filtres de ces deux familles arriveront **AVEC** la partie Tech Stack / Objection du bloc — pas avant, faute de champs stabilisés à filtrer. Tant que ces sections sont vides, ne pas les présenter comme fonctionnelles à l'utilisateur (cf. TD-186 : l'architecture de l'onglet sera tranchée au même moment). **MAJ (Sprint Bloc IA / Tech Stack)** : reste **OPEN**, se ferme en **DEUX temps** — la partie **Objection** au **sprint Objection**, la partie **Tech Stack** au **sprint Competitors** (avec le câblage des filtres tech, cf. TD-196). **MAJ (Sprint Bloc IA / Contrainte)** : la partie **contrainte** avance — **filtre par nature** livré (partiel, **DC only**). **Réordonnancement PO (2026-08-28)** : la fermeture en deux temps est remplacée par un **sprint Filtres transverse** UNIQUE qui regroupe **TOUS** les filtres (Tech `usage_scope`, Objection, complétion contrainte) et **ferme TD-189** (cf. **TD-202**). Reste **OPEN**. **MAJ (Sprint Competitors)** : reste **OPEN** (fermeture via sprint Filtres transverse, cf. TD-202). Le sprint a livré un **filtre competitor par NOM (DC-only)**, **distinct** des filtres de famille Tech attendus. |
| TD-190 | [IA/Backend] Clustering des signaux tech à construire (les signaux tech ne clusterisent pas) | Sprint Bloc IA / Qualification (constat, hors périmètre) | RESOLVED | Sprint Bloc IA / Tech Stack (`feat/techstack-cluster`) | Les signaux tech ne passent PAS par `SignalClusterService` : leur seul mécanisme anti-doublon est la **dédup par `tech_name_normalized`** (posé au Sprint S10), pas un clustering domaine × dimension comme pain/impact/objectif. Cohérent avec S10 (`TechStack` n'a ni `canonical_key` ni service de cluster). Le clustering tech (regroupement de mentions récurrentes d'un même outil à travers activités/DC) reste **à construire** quand l'étape Tech du Bloc IA sera traitée. Lié à TD-189 (les filtres Tech arrivent avec cette même étape). **RÉSOLU (Sprint Bloc IA / Tech Stack, `feat/techstack-cluster`)** : clustering tech livré **READ-TIME** dans `SignalClusterService` — agrégation par `tech_name_normalized` (`_fetch_tech_signals` + `_build_tech_cluster`), format cluster **unifié** (clés `what`/`dimension` **neutres** = null, membre `TechStackSignalListSerializer` réutilisé) ; **aucune appartenance stockée, aucune migration** (éditer le `tech_name` recolle les doublons au refetch). Drawer cluster (champs techno via `TechDetailBlock` puis signaux sources) + ligne épurée (badge priorité masqué pour tech) + colonne droite Qualification (Compte **et** DC) branchée sur le pipeline cluster. Voir la fiche « Sprint Bloc IA / Tech Stack ✅ » dans ROADMAP.md. |
| TD-191 | [Données] Symétrie de validation manquante : `dimension` non validé à l'écriture comme `what` | Sprint Bloc IA / Qualification (garde COST, Volet 2) | OPEN | Chore garde IA (avec la garde `what`) | Le Volet 2 de la garde COST valide `what` à la PERSISTANCE contre `SignalWhat` (hors-liste → journalisé, `is_domain_valid=False`, exclu de toutes les surfaces, migration `0025_signals_is_domain_valid`). **La `dimension` n'a PAS reçu la garde symétrique** : une valeur de dimension hors `SignalDimension` n'est ni journalisée ni marquée ni exclue — elle passe. Appliquer **le même garde** à `dimension` (validation à l'écriture + flag + exclusion), en réutilisant le mécanisme de `is_domain_valid` plutôt qu'en le ré-implémentant. À faire dans le même chore que le durcissement `what`, pour une règle unique « le domaine ET la dimension d'un signal sont dans la liste ou le signal est écarté ». |
| TD-192 | [Produit] Narratif IA logé en Overview — fonctionnalité IA codée dans le Bloc IA mais housée dans la surface Overview | Sprint Bloc IA / Qualification (fin de bloc) | OPEN | Fin Bloc IA / Sprint Overviews | Une fonctionnalité IA (narratif) a été **codée pendant le Bloc IA** mais **vit dans la surface Overview**, pas dans les surfaces Qualification du sprint. Reportée en **fin de bloc** : la brancher/finaliser une fois le reste du Bloc IA (Tech Stack, Objection, Deal health) stabilisé, ou la rattacher au **Sprint Overviews (Sprint B)** selon l'arbitrage PO. Tracer pour ne pas la perdre entre deux surfaces (codée ici, affichée ailleurs). |
| TD-193 | [Refactor] Deux blocs de `_build_*_cluster` copiés-collés à travers les 3 builders (distinct_contacts_count ; decision_cycle_ids/campaign_ids) | Audit de réutilisation backend (Bloc IA / Qualification) | OPEN | Pré-launch | Les helpers de méta de cluster sont déjà factorisés en UNE méthode partagée (`_compute_span_days`, `_compute_departments`, `_compute_max_scope_level`, `_compute_lifecycle`, `_member_statuses`, `_apply_member_filters`) et réutilisés par les 3 builders. Restent **deux** calculs copiés-collés à l'identique dans `_build_pain_cluster` / `_build_objective_cluster` / `_build_impact_cluster` : (1) le comptage `distinct_contacts_count` via `source_activity.contacts` (`signal_cluster_service.py:945-950, 1086-1091, 1244-1249`) et (2) la construction des liens `decision_cycle_ids` / `campaign_ids` (`:981-986, 1128-1133, 1284-1289`). Les extraire en deux helpers partagés (`_compute_distinct_contacts_count`, `_compute_linking`) comme les autres calculs de méta. Grep-zéro-duplication : pas un bug, une dérive potentielle si un builder évolue sans les autres. Découvert à l'audit de réutilisation pré-merge du bloc. |
| TD-194 | [Refactor] Garde `is_domain_valid=True` (exclusion en lecture) copiée en 7 littéraux — à centraliser en UNE méthode de queryset/manager | Audit de réutilisation backend (Bloc IA / Qualification) | OPEN | Pré-launch | La garde COST côté ÉCRITURE est en un point unique (`SignalManager._flag_invalid_domain`). Son pendant côté LECTURE — `.filter(is_domain_valid=True)` qui exclut les signaux hors-taxonomie de toute surface utilisateur — est en revanche **répété en 7 littéraux** : `base_views.py:190` (couvre les listes/détail par type + l'endpoint agrégé), `signal_cluster_service.py:742/786/845`, `signal_counts_view.py:90`, `signal_data_service.py:190/263`. Aucune surface n'a été oubliée (couverture complète vérifiée à l'audit), mais il n'existe **aucune source unique** : si la sémantique du drapeau change, 7 sites sont à modifier. Centraliser en UNE méthode partagée — manager `BaseSignal.objects.valid_domain()` ou mixin de queryset — analogue lecture du `_flag_invalid_domain` unique côté écriture. Découvert à l'audit de réutilisation pré-merge du bloc. |
| TD-195 | [Refactor] Deux tables type→serializer parallèles (`_SERIALIZER_BY_SLUG` / `_get_member_serializer_class`) — unifier derrière un registre | Audit de réutilisation backend (Bloc IA / Qualification) | OPEN | Pré-launch (priorité basse) | Deux petites tables de dispatch type→serializer coexistent : `_SERIALIZER_BY_SLUG` (8 slugs frontend, endpoint agrégé, `aggregated_signal_view.py:95`) et `_get_member_serializer_class` (3 types clusterables, membres de cluster, `cluster_serializer.py:211-217`). Les deux résolvent vers les **mêmes** `*ListSerializer`, avec des vocabulaires de clés et des sous-ensembles de types différents. Pas une dérive (sortie identique par type) ; opportunité de les unifier derrière UN registre partagé pour qu'un nouveau type ne s'ajoute qu'à un endroit. Priorité basse. Découvert à l'audit de réutilisation pré-merge du bloc. |
| TD-196 | [Backend] Filtre tech FANTÔME — `signals/filters.py` documente des filtres tech NON implémentés | Sprint Bloc IA / Tech Stack (audit filtres) | OPEN | Sprint Competitors (câblage filtres tech) | `signals/filters.py:27-29` documente en en-tête un jeu de filtres TechStack (`is_competitor`, `is_integration_target`, `tech_name_normalized`, `usage_scope`, `is_discontinued`, `renewal_date_*`) — mais **AUCUN** attribut `BooleanFilter`/`CharFilter` correspondant n'est déclaré dans la `SignalFilter` (`BooleanFilter` est importé et jamais utilisé). Fonctionnalité **fantôme** + **doc trompeuse** : le back n'a aucun filtre queryable sur les 3 booléens tech. À nettoyer **avec le câblage des filtres de la famille Tech** (sprint Competitors) — implémenter les filtres réels OU retirer la doc. Relié à TD-189 (filtres Tech/Objection placeholders). **MAJ (Sprint Competitors)** : la doc fantôme `filters.py:27-29` cite encore **`is_competitor`** — **colonne droppée (0033)** — et `is_integration_target` ; encore plus trompeuse. **Aucun** filtre booléen tech câblé ce sprint (livré : filtre competitor **par NOM**, DC-only). Nettoyage avec les filtres tech (Filtres transverse) ou **TD-206**. |
| TD-197 | [Front] Double vocabulaire de slug tech `tech_stack` (cluster) / `tech-stack` (flat) sans convertisseur central | Sprint Bloc IA / Tech Stack (vérif slug) | OPEN | Sprint Competitors (au câblage filtres/surfaces tech) | Deux vocabulaires de type coexistent côté front sans pont : **cluster** (underscore `tech_stack`, valeur `SignalClusterType`) et **flat** (hyphen `tech-stack`, slug frontend hardcodé dans `SignalTypeChip`, `SignalCard`, `SignalLine`, `SignalDetailContent`, les hooks `useDCAllSignals`/`useActivityAllSignals`, et l'endpoint agrégé back `aggregated_signal_view.py:89`). La seule traduction `tech_stack→tech-stack` vit **localement dans le drawer** (`SignalClusterDetailDrawer.jsx:604-605`). **Non bloquant** tant qu'un cluster tech ne traverse que `ClusterRow` + drawer (le drawer traduit). À **centraliser** (util `clusterTypeToSlug` dans `signalClusters.js`) dès qu'une surface FLAT devra rendre un cluster tech — probablement au **sprint Competitors** (filtres tech). Lié à TD-195 (deux tables type→serializer parallèles côté back). |
| TD-198 | [Docs] Commentaires backend périmés révélés à l'audit (canonical_key tech, propagation decision_cycle tech) | Sprint Bloc IA / Tech Stack (audit) | OPEN | Chore docs backend | Deux docstrings/commentaires backend contredits par le code réel : (1) `base_model.py:149-151` prétend que `canonical_key` est **auto-calculé au `save()` pour `TechStackSignal`** — **FAUX** : le `save()` tech ne calcule que `tech_name_normalized` et laisse `canonical_key` à None (le clustering tech est read-time sur le nom normalisé). (2) `signal_manager.py:34-35` décrit la propagation `decision_cycle`/`campaign` comme **overridés à None sur `TechStackSignal`** — **OBSOLÈTE** : `decision_cycle` est désormais **hérité** (réel) sur le modèle tech (seul `campaign` reste shadow=None) ; le code de propagation s'auto-corrige au runtime via `getattr`, mais le commentaire ment. Corriger les deux commentaires (docs uniquement, aucun changement de comportement). **MAJ (Sprint Competitors)** : ajouter le docstring `TechStackSignal` disant `source_activity` **NOT NULL** — **faux** (le champ est nullable) — à balayer dans la même passe. |
| TD-199 | [Produit] Passe cluster — décider les infos affichées par cluster (fin bloc Signaux) | Sprint Bloc IA / Tech Stack (cadrage PO) | OPEN | Passe cluster — fin bloc Signaux (AVANT Prep call) | Reprendre **TOUS** les clusters (pain/impact/objectif/tech) et décider, par type, les infos **pertinentes et faciles à afficher** au niveau cluster. Pour le **tech** : piste **« remplacement envisagé »** — l'account utilise une techno mais **songe à en changer** (non acté), **info de NIVEAU CLUSTER dérivée des signaux, PAS un booléen** — elle **remplace** l'ancien champ `is_to_replace` (supprimé au sprint Competitors) + toute autre info pertinente à la vente. À faire **AVANT Prep call** (fermeture du bloc Signaux). Lié à la note de cadrage #4 (chaque fermeture = production-ready). **MAJ (Sprint Competitors)** : `is_to_replace` **N'A PAS** été supprimé au sprint Competitors (contrairement au plan) — il est **CONSERVÉ** comme seul booléen de qualification survivant ; son **drop est reporté à CETTE passe cluster** (info « remplacement envisagé » de niveau cluster). |
| TD-200 | [Front/UI] Filtres DC « inclure les signaux de tout le compte » par section | Sprint Bloc IA / Tech Stack (cadrage PO) | OPEN | Sprint Filtres (APRÈS Objection/contrainte) | Pendant un DC, pouvoir **exploiter le contexte COMPTE** relevé lors d'AUTRES DC : un filtre par section « inclure les signaux de tout le compte » (au-delà du seul périmètre du DC courant). Sujet **filtres transverse**, à traiter **APRÈS** le sprint Objection/contrainte. **Touche les 3 surfaces de filtres** (Compte / DC / Activité). Distinct des filtres de famille Tech/Objection (TD-189, TD-196). |
| TD-201 | [IA/Backend] Scope tech (usage) faux — l'extraction tech ne capte pas le département d'usage | Sprint Bloc IA / Contrainte (smoke PO) | RESOLVED | Sprint Bloc IA / Tech scope (`feat/techstack-usage-scope`) | L'extraction tech ne remplissait **jamais** le département d'usage : « **équipe marketing** sur HubSpot » ressortait **company-wide** au lieu de **Marketing**. `usage_scope` était limité à `TEAM` / `COMPANY` / `UNKNOWN` et le **département d'usage jamais rempli** (le tech ne passait pas par le même chemin scope que pain/objective/impact/constraint). **RÉSOLU (sprint Tech scope, `feat/techstack-usage-scope`)** : nouveau **M2M `usage_departments` → `StandardDepartment`** (multi-département) remplaçant le **FK singulier `usage_department`** (droppé, migration 0028) ; extraction avec **règle de DÉSIGNATION EXPLICITE** calquée sur le scope contrainte ; **résolution de nom robuste** (« support » → « Customer Support » — match exact insensible à la casse + mot-unique non ambigu). **Verdict de l'audit « À VÉRIFIER »** : c'était un **MANQUE PRÉ-EXISTANT** de l'extraction tech (le tech ne passe **PAS** par le bloc scope partagé), **PAS** une sur-correction du durcissement contrainte — **prouvé** (le durcissement `feat/constraint-scope-guard` n'a touché aucun fichier du chemin tech). Voir la fiche « Sprint Bloc IA / Tech scope ✅ » dans ROADMAP.md. |
| TD-202 | [Front/UI] Filtres non exhaustifs — contrainte partiel, tech jamais fait, objection à faire | Sprint Bloc IA / Contrainte (cadrage PO) | OPEN | Sprint Filtres transverse | Le filtre **contrainte (nature)** est livré **partiel** (multi-select Nature, **DC only**) ; le filtre **tech (`usage_scope`) n'a JAMAIS été fait** ; le filtre **objection reste À FAIRE**. À traiter **d'un bloc** au **sprint Filtres transverse** (TOUS les filtres, regroupables). **Relié à TD-189** (familles Tech/Objection en placeholders) et **TD-196** (filtre tech FANTÔME côté back) — **ne pas dupliquer**. |
| TD-203 | [Bug latent] Badge « X signals » de l'onglet Signals incohérent avec le nombre réel de signaux du DC | Sprint Bloc IA / Contrainte (smoke PO) | OPEN | À élucider AVANT clôture du bloc Signaux | Le badge « **X signals** » de l'onglet Signals affiche un compte (ex. **4**) **incohérent** avec le nombre réel de signaux du DC (**10+**). À **ÉLUCIDER avant la clôture du bloc** : compte-t-il une **sous-catégorie** ? une **seule activité** ? est-il tout simplement **cassé** ? Déterminer la source du compte avant de trancher le correctif. |
| TD-204 | [Front/UI] Homogénéisation UI Activity/DC — la vue Activity n'est pas présentée comme le DC | Sprint Bloc IA / Contrainte (cadrage PO) | OPEN | Sprint UX Signals | La vue **Activity** n'est **pas présentée comme le DC** (sections par thème/nature). **Règle PO** : **même UI**, seule la **donnée exposée** diffère (Activity = liste de provenance ; DC = agrégat/cluster). Vaut pour **contraintes ET qualif**. À traiter au **sprint UX Signals**. Lié à **TD-186** (architecture onglet Flat/Grouped) et à la famille « homogénéisation UI » (**TD-188**) — **ne pas dupliquer**, rattacher. **MAJ (Sprint People)** : inclut le rendu de la section People (rôle + `full_name` + contact / département) — même UI qu'ailleurs, seule la donnée diffère. Voir **TD-211**. **MAJ (sprint Next steps)** : rendu soigné de l'OBJECTIF du next step sur AISuggestionCard/drawer (affichage fonctionnel livré, présentation à peaufiner au sprint UX Activity). |
| TD-205 | [Front/UI] Modaux Edit des signaux non faits — requis avant Prep call | Sprint Bloc IA / Contrainte (cadrage PO) | OPEN | Sprint UX Signals | Les **modaux d'édition** des signaux ne sont **pas faits** ; **requis AVANT Prep call**. À traiter au **sprint UX Signals**. **Élargit TD-30** (pas d'édition inline pour people/constraints, `SignalQuickDrawer.renderDetails` vide sur ces types) — **rattacher, ne pas dupliquer**. **MAJ (Sprint People)** : le drawer d'édition people doit exposer le champ `full_name` (nouveau) + l'accès à la réconciliation contact — cf. **TD-211**. **MAJ (sprint Next steps)** : présentation soignée de l'objectif du next step sur le drawer au sprint UX Activity (affichage fonctionnel livré). |
| TD-206 | [Refactor] Nettoyage code mort AI/Signals — passe clean code de fin de bloc | Sprint Bloc IA / Contrainte (cadrage PO) | OPEN | Sprint Nettoyage code mort AI/Signals (fin bloc Signaux) | **Passe clean code dédiée** de fin de bloc sur **AI / Signals** : code mort, chemins morts, imports/branches/enums inutilisés. **Point de regroupement** des dettes de code mort du bloc — cadrer **ensemble** avec **TD-187** (`ScopeLevel.PERSONAL` inutilisé), **TD-194** (garde `is_domain_valid` en 7 littéraux), **TD-195** (deux tables type→serializer parallèles) et **TD-198** (commentaires backend périmés) — **sans les dupliquer**. **Ajouté (sprint Tech scope)** : les **docstrings/commentaires résiduels `usage_scope` / `UsageScope`** laissés après le **drop du FK singulier `usage_department`** (le champ est retiré mais quelques docstrings modèle/serializer/constants mentionnent encore l'ancien couplage scope↔département) — à balayer dans cette passe. **Ajouté (Sprint People)** : garde morte `if not people_key: continue` (`app_modules/signals/services/signal_cluster_service.py:779-781`) **inatteignable** — `_people_cluster_key` retourne toujours une clé (fallback `signal:<id>`) — + commentaire « no cluster » **faux** depuis la sous-étape 2-bis — à retirer. |
| TD-207 | [IA/Prompt] Resserrement prompt tech — techno UTILISÉE vs ENVISAGÉE + incohérence `usage_scope=COMPANY` avec département désigné | Sprint Bloc IA / Tech scope (constats post-livraison) | OPEN | Sprint Competitors | Deux resserrements du prompt tech, **au sprint Competitors** (qui retouche déjà le prompt tech) : **(1) distinguer techno UTILISÉE vs ENVISAGÉE** — une techno seulement **évaluée** ne doit **pas** ressortir en tech-stack (ex. Salesforce « **on commence à regarder** » ressort **à tort**, avec `is_to_replace` / `is_competitor` déjà posés) ; c'est la distinction **PASSÉ / PRÉSENT / FUTUR** laissée en ancre `TODO(S10→AI-sprint)` dans `techstack_v1.py`. **(2) Corriger l'incohérence `usage_scope=COMPANY` alors qu'un département est désigné** : « **toute l'équipe marketing** » devrait être **`TEAM` + Marketing**, pas **`COMPANY` + Marketing** (l'échelle et le département doivent rester cohérents ; la désignation d'un seul département implique une échelle ≠ company-wide). **Relié au sprint Competitors** (routage `is_competitor`, suppression `is_to_replace`, drop `is_integration`) — cf. TD-196 / TD-199. **MAJ (Sprint Competitors)** : les deux resserrements (UTILISÉE vs ENVISAGÉE ; `usage_scope=COMPANY` fallback) **NON livrés** au sprint Competitors (périmètre = signal détaché + drops). **Rattachés à la passe nettoyage / resserrement prompt AVANT la fin d'Activity** (avec **TD-206**), **PAS** à People ni Next steps. |
| TD-208 | [Test] Test tech multi-département + company-wide à couvrir au smoke A→Z | Sprint Bloc IA / Tech scope (constats post-livraison) | OPEN | Smoke Signals A→Z (fin bloc Signaux) | Le **smoke de bout en bout** du bloc Signaux (transcript d'un **cycle de vente COMPLET**) doit **inclure des transcripts riches multi-département** couvrant le **tech scope d'usage MULTI** (« **Sales et Marketing sur HubSpot** » → `usage_departments=[Sales, Marketing]`) **ET** le cas **company-wide sans département désigné** (« **toute la boîte est sur Slack** » → M2M **vide** + `usage_scope=COMPANY`). Vérifie de bout en bout **extraction → résolution de nom → affichage une seule ligne**. Rattaché à la sous-étape **9. Smoke Signals A→Z** de la séquence PO (ROADMAP.md). |
| TD-209 | [Produit/IA] Chantier DC deal health — lire CompetitorSignal DIRECTEMENT + enrichissement + cluster « Who we're up against » + reconstruction DealHealthSnapshot | Sprint Competitors (livraison) | OPEN | Sprint Deal health (bloc Commandes IA / S14) | Ce sprint a recâblé deal_health sur un **match read-time par nom** (`competitor_name_normalized ∈ competitor_norms`) — angle mort **transitoire** : deal health devra lire le **CompetitorSignal directement** (concurrents nommés + situation), rendre un **cluster « Who we're up against »**, et **reconstruire `DealHealthSnapshot`** sur les **variables d'équation**. Pas de **statut de menace stocké** (analyse read-time reportée à ce chantier — décision PO). À traiter à l'étape **Deal health (prompt + UI)** du bloc. Distinct de TD-39 (thèmes) / TD-192 (narratif). **MAJ (Sprint People)** : le « missing elements » du deal health couvre AUSSI le **People sans nom ni contact** (cluster solitaire `signal:<id>`) → à remonter comme information manquante côté deal health (incitation à nommer / lier, reliée à **TD-211**). **MAJ (sprint Next steps)** : voir aussi **TD-215** (lien next step → étape DC que le next step fait progresser — chantier DC connexe, périmètre distinct). |
| TD-210 | [IA/Prompt] Grounding extraction competitor sur catalogue produit — décider l'overlap sans catalogue vendeur | Sprint Competitors (constat) | OPEN | Sprint resserrement prompt (à cadrer PO) | L'extraction competitor décide qu'un outil « **chevauche ce qu'on vend** » **sans catalogue produit vendeur** de référence. Grounding à définir (catalogue produit interne ? liste maintenue ?) pour fiabiliser la détection d'overlap. Relié au resserrement prompt tech (**TD-207**) — **ne pas dupliquer**. |
| TD-211 | [Front/UI] Surface People — réconciliation contact (modale suggest/create/link) + split full_name→first/last + rendu/édition manuelle | Sprint People (livraison) | OPEN | Sprint UX Signals | Le backend People est complet E2E (flux réconciliation prouvé **8/8**, migration **0035**). Reste le **FRONT** : (1) **modale de réconciliation** — `GET /contacts/?account_id=&standard_department=&search=` (suggest) → `POST /contacts/` (create, renvoie l'objet, cf. **TD-212**) → `PATCH module-signals/people/{id} {target_contact}` (link / unlink) ; (2) **split `full_name` → `first_name`/`last_name`** au create contact ; (3) champ `full_name` + **incitation à nommer** dans le drawer d'édition manuelle (un People sans nom ni contact reste un cluster solitaire `signal:<id>`). **RELIÉ** à **TD-204** (rendu Activity/DC) et **TD-205** (drawer edit people, déjà cité) — **ne pas dupliquer**, la partie NOUVELLE ici = réconciliation + split. L'incitation à nommer est **aussi** portée par le deal health / missing elements (**TD-209**) — les deux reliés, pas contradictoires. |
| TD-212 | [Bug] POST /contacts/ ne renvoyait pas l'objet créé (id absent) | Sprint People (sous-étape 5.3) | RESOLVED | Sprint People (`feat/people-full-name`) | L'action create utilisait `ContactCreateSerializer` (write-only, `id` omis), donc le 201 ne renvoyait pas la ressource → impossible d'enchaîner create-contact → link sans re-query. **RÉSOLU** : `ContactViewSet.create()` re-sérialise l'instance via `ContactSerializer` (lecture, id inclus) ; champs acceptés / défauts **inchangés** (`app_modules/contacts/views/views.py:149-169`). Prouvé par le flux d'intégration (`tests/signals/test_people_reconciliation_flow.py`). |
| TD-213 | [Produit] Influence mal placée — `Contact.influence_level` / `has_buying_authority` (global compte) vs `PeopleSignal.influence` (per-DC / rôle) | Sprint People (constat) | OPEN | À cadrer PO / passe cleaning | L'influence est une propriété de la **RELATION** rôle↔DC (déjà portée par `PeopleSignal.influence`), pas du contact ; `Contact.influence_level` la fige au niveau **COMPTE**. **DÉCISION PRODUIT non tranchée** : conserver les deux (sens distincts) ? migrer l'influence vers People ? Consigné pour cadrage — **aucun schéma décidé ici**, non bloquant. |
| TD-214 | [Logs] Discipline de logging — tout le code doit logger via les méthodes de logs EXISTANTES du projet (pattern standard) et gérer les erreurs via le système standard | Clôture sprint Next steps (injonction PO) | OPEN | À auditer (récent d'abord, puis transverse) | Règle **TRANSVERSE** (injonction PO, non optionnelle). Auditer que chaque log passe par `get_logger`/le pattern projet (**pas** de `logging.getLogger` ad hoc, pas de `print` / `console.log`) et que chaque erreur passe par le **système standard** (pas d'exception brute exposée). **Audit priorisé sur le code récent** — signaux, extraction (`next_step_extractor`, `transcript_signal_extractor`), réconciliation People — **puis étendu** au reste du produit. Tracer les écarts et corriger. **Voir aussi** TD-37 (faux positif PII logging) et TD-45 (verbose boot logs) — cas logging spécifiques distincts. |
| TD-215 | [Produit/IA] Enrichissement objectif du next step — lien vers l'ÉTAPE DC que le next step fait progresser (dimension « progression DC » en plus de prospect/commercial) | Clôture sprint Next steps | OPEN | Chantier DC / UX Activity | L'objectif TEXTE du next step est livré (angles prospect + commercial). **SUITE** : rattacher le next step à l'étape du `decision_cycle` qu'il fait avancer (dimension « progression DC » de l'objectif). À construire au chantier DC / UX Activity. **Voir aussi** TD-209 (deal health — **même chantier DC, périmètre distinct** : competitor / DealHealthSnapshot). |

## Sprint BI Foundation — récap (Paliers 1a → 4b)

Fondation Business Intelligence pour SalesCommands (dashboards = action/motivation, pas reporting comptable). Dette produite/résolue ce sprint tracée ci-dessus (TD-59 résolu ; TD-71→75 ouverts).

| Palier | Livrable | Note |
| ------ | -------- | ---- |
| **1a** | `apply_role_scope` extrait de `ScopedQuerysetMixin` (source unique mine/team/client + C6) | A payé au-delà de la BI — a permis le balayage OWNERSHIP_MAP (TD-71/72) |
| **1b→1e + canary** | Registry `KPIDefinition` déclaratif + compute DB-level + cache + invalidation par signaux + KPI fixture e2e | Ajouter un KPI = déclarer, pas coder |
| **2** | 9 KPIs MVP (5 DC counters, 1 todo, 6 leads, 4 couverture territoire, 2 avancement campagne, 3 couverture campagne, 8 quota, 7 $ pipeline/result, 9 état DC) | compute_fn escape hatch pour 3/4/8/9 + réparation progression DC (BUG 4) |
| **Assainissement scope** | 3 divergences dormantes attname `_id` corrigées : `territories`, `sales_quotas`, `accounts` (iso-comportement prouvé + test dédié à chaque fois) | Legacy/inertes documentés TD-71/72 ; atteintes restantes (`teams` sévère, `users`/`contacts` mineurs) TD-76 |
| **3 (C2)** | Invitations actionnables : `response_status` (3a) + endpoint respond (3b) + todo étend invitations ACCEPTED (3c, boucle fermée) | Option A (invité-path personnel) ; PENDING/DECLINED exclus |
| **4a** | Index perf C5 (Activity.outcome/completed_at, DecisionCycle.(outcome,outcome_date)) + index todo `Notification(recipient,response_status,related_object_id)` | Note déploiement TD-73 |
| **4b** | Fix TD-59 filtre département contacts (rename param) | Impact isolé confirmé |

## Sprint 5 — Home & Overviews — récap (items 0 → 6)

Rendre la BI visible et motivante (dashboards = action/motivation, pas reporting). Dette produite/résolue tracée ci-dessus (TD-76 `teams` RESOLVED ; TD-77/78/79 ouverts).

| Item | Livrable | Note |
| ---- | -------- | ---- |
| **0** | TD-76 `teams` : scope resserré (`mine`=équipe(s) du manager, `team`=hiérarchie), sur-exposition intra-tenant fermée | `permissions/` → iso prouvé + guardrail `TestTeamOwnerScope` ; PAUSE avant commit respectée |
| **1** | Endpoint BI `GET /bi/kpi/<key>/` + `POST /bi/kpi/batch/` (BaseAPIView, pattern notifications, envelope `{success,data}`) | scope = INPUT utilisateur → borné par rôle via `check_permission` ; batch N'AGRÈGE PAS (erreurs par item, cap 20) |
| **2** | KPI `todo_team_by_owner` (pipeline standard BREAKDOWN `dimension=owner` + `dimension_labels`) + fix `dc_pipeline_value` (`default_period='all'`, métrique STOCK) | per-personne = UNE déclaration — l'archi S4 a tenu sa promesse |
| **3** | Home vue Rep (todo / progression campagnes actives + territoires top-3 / quota) + hooks `useKpi`/`useKpiBatch` + `useGetMyActiveQuotas` | cadrage smallest-number (`goalGradient`) ; todo standalone (peint en premier) ; batch chunké au cap |
| **4** | Home vue Manager (tâches du jour par personne AVEC NOMS, today+overdue ; attainment par membre) | rôle observateur (pas de todo perso) ; fusion équipes multiples (hooks dans enfant stable) ; roster non nécessaire (labels + `user_name`) |
| **5** | Boutons accept/decline dans la cloche (`respondToNotification`) → boucle invitation FERMÉE | `stopPropagation` en premier (le row navigue, aucune garde avant) ; PENDING-only ; `NotificationRow` extrait pour testabilité |
| **6** | Cleanup masque Overview DC : garde 403 dédupliquée, titre "Coaching Notes" déplacé dans `ManagerNotesThread` | pas de redesign — TD-29 reste ouvert (question produit sprint Signals/DC UX) |
| **Contrat (RESOLVED)** | `SalesQuota` list serializer expose `user_id` (commit `dd08260`) | omission de contrat, PAS de schéma (le FK `user` existe déjà) — 1 ligne, zéro migration, non-cassant (tests quota OK, aucun consommateur ne fige l'ancien set) |
| **Note archi** | Le bornage de scope de l'endpoint BI ne MORD que sur les modules *enabled* à read restreint (`teams`/`users`) ; les modules des KPIs livrés sont read=`client` (`activities`/`decision_cycles`/`territories`/`accounts`, décision Sprint 3) ou disabled→`client` (`sales_quotas`). Testé honnêtement avec un KPI JETABLE sur `teams` (aucun KPI livré n'utilise un module restricted). Conséquence : un rep appelant `todo_team_by_owner?scope=team` verrait un breakdown d'activités qu'il peut DÉJÀ lister (même info agrégée, pas une fuite) ; le bloc manager est gaté FRONTEND pour la PERTINENCE, pas la sécurité. **Invariant garanti : l'endpoint ne rend jamais plus que ce que le rôle peut déjà lire pour ce module.** |
| **Note outillage** | Alias vitest `config/formatters` manquant : `config` (bare) mappait le fichier theme-config ; Next résout `config/formatters` via `baseUrl`, pas vitest → AUCUN composant `layout/` l'important n'était testable (d'où l'absence de test sur la cloche avant l'item 5, et donc le respond jamais câblé jusqu'ici). Comblé ce sprint (alias spécifique avant `config`). Trou d'outillage qui bloquait la couverture sur toute une zone. |
